## 54. **Debugging Filter Context**  
    ```DAX
    Debug Filters = 
    CONCATENATEX(
      FILTERS('Date'[Year]), 
      'Date'[Year] & ": " & [Total Sales], 
      ", "
    )
    ```
    *Outputs active filters for troubleshooting.*
