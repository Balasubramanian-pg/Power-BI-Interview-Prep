# Replay Transformations with Query Folding Considerations

**Description:**
This transformation enables re-execution of previous data transformations while considering query folding. It allows for efficient re-processing of data by re-applying transformations, taking into account optimizations through query folding.

### Typical Scenario
In a real data pipeline, this transformation is crucial when data sources change frequently, and previous transformations need to be re-applied. For instance, in financial data analysis, daily stock prices need to be re-processed with transformations to reflect current market trends.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Age"},
        {1, "John", 25},
        {2, "Jane", 30},
        {3, "Bob", 35}
    }),
    // Apply transformation with query folding consideration
    TransformedData = Table.SelectRows(Source, each "Age" > 25)
    in
    TransformedData
```

### Example Execution Logic Step-By-Step
The transformation works by first loading data into a table. Then, it applies a filtering transformation using Table.SelectRows, which considers query folding. This means that the filtering operation is pushed back to the source if possible, optimizing performance. The result is a new table with only the rows where the age is greater than 25.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
