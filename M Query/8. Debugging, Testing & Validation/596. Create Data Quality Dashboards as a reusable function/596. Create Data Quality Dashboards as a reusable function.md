# Create Data Quality Dashboards as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to create data quality dashboards as a reusable function. It allows users to evaluate and visualize data quality metrics in a standardized and repeatable manner.

### Typical Scenario
In a real data pipeline, this transformation would be used when data quality checks need to be performed regularly on various datasets. For instance, a data engineer might use this function to create a dashboard that monitors data quality metrics such as data completeness, accuracy, and consistency across different data sources.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
No M code example provided in the source.
```

### Example Execution Logic Step-By-Step
The transformation typically works by first defining a function that takes a table as input and performs data quality checks on it. This function might use various M functions such as Table.SelectRows to filter and aggregate data, and then calculate quality metrics. The results are then visualized in a dashboard to provide insights into data quality.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
