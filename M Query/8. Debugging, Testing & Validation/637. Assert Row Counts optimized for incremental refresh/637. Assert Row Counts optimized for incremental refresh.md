# Assert Row Counts Optimized for Incremental Refresh

**Description:**
This transformation ensures data integrity by asserting row counts in a Power Query pipeline, particularly useful in incremental refresh scenarios. It helps verify that the expected number of rows is present in the data set after refresh, optimizing the process for performance.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial during incremental data refreshes. For instance, when daily sales data is appended to a historical database, asserting row counts ensures that the correct number of transactions has been added, helping to catch any discrepancies early in the data processing workflow.

### Core M Functions Used
`Table.RowCount`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Product", "Sales"},
        {"Product A", 100},
        {"Product B", 200}
    }),
    RowCount = Table.RowCount(Source)
    in
    if RowCount = 3 then Source else error "Unexpected row count"

```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Source Data**: Begin with a table containing sales data.
    2. **Row Count Assertion**: Use `Table.RowCount` to get the number of rows in the table.
    3. **Conditional Check**: Implement a conditional check to ensure the row count matches the expected number (e.g., 3 rows including the header).
    4. **Error Handling**: If the row count does not match the expectation, an error is raised to alert the data engineer or analyst.
    5. **Pass or Fail**: If the row count matches, the table is passed through for further processing; otherwise, the process stops with an error.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
