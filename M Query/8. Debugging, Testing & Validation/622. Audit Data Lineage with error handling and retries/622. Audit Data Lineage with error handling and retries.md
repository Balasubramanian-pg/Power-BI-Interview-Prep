# Audit Data Lineage with Error Handling and Retries

**Description:**
This transformation achieves audit data lineage by tracking data sources, transformations, and errors. It provides a robust pipeline with error handling and retries. The use case ensures data quality and reliability. It is useful for auditing and troubleshooting data pipelines.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from multiple sources, applying complex transformations, and requiring strict auditing and error handling. For instance, when integrating data from various departments, ensuring each step's lineage and handling potential errors is crucial.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `Number.Retries`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Source", "Transformation", "Error"},
        {"DataSource1", "Transformation1", ""},
        {"DataSource2", "Transformation2", "Error1"}
    }),
    // Attempt data retrieval with retries
    AttemptWithRetries = (attemptCount as number) =>
        try
            // Simulating data retrieval
            data = Table.SelectRows(Source, each [Source] = "DataSource1"),
            // Simulating transformation
            transformedData = Table.TransformColumns(data, {{"Transformation", each _ & "_transformed"}}),
            // Handling potential errors
            result = try
                transformedData,
            catch
                (e) =>
                    if attemptCount > 0 then
                        // Retry on error
                        AttemptWithRetries(attemptCount - 1)
                    else
                        // Return error if all retries fail
                        Error.Record("Transformation Error", e.Message, e.StackTrace)
    in
    AttemptWithRetries(3)
```

### Example Execution Logic Step-By-Step
The script works as follows:
1. **Define Source Data**: A sample table `Source` is created with columns for data sources, transformations applied, and any errors encountered.
2. **Attempt with Retries Function**: A function `AttemptWithRetries` is defined to attempt data retrieval and transformation with a specified number of retries on error.
3. **Data Retrieval and Transformation**: Inside `AttemptWithRetries`, it attempts to select data from `Source` based on a condition and then applies a transformation.
4. **Error Handling**: If an error occurs during transformation, it checks if retries are remaining. If yes, it calls itself with decremented retry count. If not, it captures and returns the error.
5. **Execution**: The function `AttemptWithRetries` is called with a specified number of retries (in this case, 3 retries).

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*