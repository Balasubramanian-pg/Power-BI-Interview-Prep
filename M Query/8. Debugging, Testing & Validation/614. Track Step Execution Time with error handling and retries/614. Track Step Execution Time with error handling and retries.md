# Track Step Execution Time with Error Handling and Retries

**Description:**
This transformation tracks the execution time of each step in a Power Query pipeline, handling errors and implementing retries for robust data processing. It ensures that data pipelines are resilient and provide insights into performance bottlenecks. By monitoring step execution times, data engineers can optimize queries for better performance.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with slow or unreliable data sources. For instance, when fetching data from an external API that frequently times out or returns errors, tracking execution times and implementing retries helps in diagnosing issues and improving the overall reliability of the data ingestion process.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `Duration`

### Code Source Execution
```powerquery
let
    Source = try
        // Your data source here
        #"Sample Table",
    CatchError = try
        // Implement retry logic here
        Source,
    CatchError2 = if CatchError[Error] <> null then
        // Retry or handle error here
        #"Error Occurred"
    else
        CatchError[Value],
    ExecutionTime = Duration.ToString(Timer.End(Timer.Start()))
    in
    CatchError2
```

### Example Execution Logic Step-By-Step
This transformation works by wrapping each step in a try-catch block to handle any errors that may occur. The execution time of each step is tracked using the Duration.ToString and Timer functions. If an error occurs, the transformation can be configured to retry the step or handle the error gracefully. The key steps are: 1) Wrap the data source or each step in a try-catch block. 2) Implement retry logic in the catch block. 3) Track the execution time of each step using timer functions.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*