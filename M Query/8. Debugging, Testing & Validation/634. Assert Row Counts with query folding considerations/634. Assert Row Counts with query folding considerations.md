# Assert Row Counts with Query Folding Considerations

**Description:**
This transformation ensures data integrity by verifying that the expected number of rows is present in a dataset. It helps detect any data inconsistencies or errors during the data pipeline process. The transformation takes into account query folding considerations to optimize performance.

### Typical Scenario
In a real-world data pipeline, this transformation would be used to validate the number of rows in a dataset after data ingestion or data processing. For instance, when loading customer data from a source system, the pipeline should assert that the expected number of customer records is present to ensure data accuracy.

### Core M Functions Used
`Table.SelectRows`, `Table.RowCount`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25},
        {"Jane", 30},
        {"Bob", 35}
    }, {"Name", "Age"}),
    ExpectedRowCount = 3,
    ActualRowCount = Table.RowCount(Source),
    Assert = if ActualRowCount = ExpectedRowCount then Source else error "Row count mismatch"
    in
    Assert
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Load the dataset into a Power Query table.
    2. Define the expected row count.
    3. Use the Table.RowCount function to get the actual row count.
    4. Compare the actual row count with the expected row count.
    5. If the counts match, return the original table; otherwise, raise an error.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
