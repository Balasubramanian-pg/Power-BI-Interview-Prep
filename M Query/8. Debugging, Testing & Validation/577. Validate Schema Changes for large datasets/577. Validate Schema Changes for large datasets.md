# Validate Schema Changes for large datasets

**Description:**
This transformation validates schema changes for large datasets, ensuring data consistency and accuracy. It helps detect changes in the schema, such as added or removed columns, and verifies data types.

### Typical Scenario
In a real data pipeline, this transformation would be used when ingesting large datasets from various sources, such as databases, files, or APIs. For instance, when merging data from multiple sources, it's crucial to validate the schema to ensure consistency and accuracy.

### Core M Functions Used
`Table.SelectRows`, `Table.ColumnNames`, `Table.Schema`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, "a"},
        {"B", 2, "b"},
        {"C", 3, "c"}
    }, {"Column1", "Column2", "Column3"}),
    Schema = Table.Schema(Source),
    ColumnNames = Table.ColumnNames(Source)
    in
    Source
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Load the dataset into a Power Query table.
    2. Use the Table.Schema function to get the schema of the table.
    3. Use the Table.ColumnNames function to get the column names of the table.
    4. Compare the schema and column names with the expected schema and column names.
    5. If there are any discrepancies, handle them accordingly, such as by logging an error or alerting a data engineer.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*