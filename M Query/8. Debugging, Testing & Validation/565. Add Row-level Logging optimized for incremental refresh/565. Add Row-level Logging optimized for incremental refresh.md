# Add Row-level Logging optimized for incremental refresh

**Description:**
This transformation adds row-level logging to a table, which is particularly useful when working with incremental refresh. It allows for tracking changes at the row level, enabling more granular auditing and debugging capabilities. This is especially valuable in data pipelines where data is constantly being updated or appended.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when building a data warehouse or a business intelligence system that relies on incremental data refreshes. For instance, when loading customer data from a source system into a data warehouse, you might want to track which rows have been added, updated, or deleted over time. This logging capability helps in maintaining data integrity and facilitates troubleshooting.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25},
        {"Mary", 31},
        {"David", 42}
    }, {"Name", "Age"}),
    AddLogColumn = Table.AddColumn(Source, "Log", each "Row added"),
    IncrementalRefresh = // Assume this is where incremental refresh logic is applied
    Result = IncrementalRefresh
in
    Result
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Source Data**: Start with a table of data. For example, a simple table with names and ages.
    2. **Adding a Log Column**: Use `Table.AddColumn` to add a new column to the table. This column will contain logging information. For simplicity, the example assumes every row is logged as "Row added".
    3. **Incremental Refresh Logic**: Apply incremental refresh logic to the table. This typically involves identifying which rows are new, updated, or deleted since the last refresh. The exact implementation depends on the specifics of the data source and the requirements of the pipeline.
    4. **Logging Integration**: Integrate the logging into the incremental refresh process. This could involve updating the log column based on the action taken (e.g., "Row updated", "Row deleted").
    5. **Final Result**: The final output is a table that includes the original data plus the additional logging information.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*