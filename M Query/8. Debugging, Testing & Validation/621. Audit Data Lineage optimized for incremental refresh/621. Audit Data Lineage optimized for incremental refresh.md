# Audit Data Lineage optimized for incremental refresh

**Description:**
This transformation achieves audit data lineage optimized for incremental refresh, enabling efficient tracking of data changes and updates.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when auditing data changes and updates in an incremental refresh scenario, such as in financial data processing where every transaction needs to be tracked and verified.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumn`, `Table.Group`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Date"},
        {1, "John", #date(2022,1,1)},
        {2, "Jane", #date(2022,1,2)}
    }),
    AddedCustom = Table.AddColumn(Source, "Custom", each [Date]),
    GroupedRows = Table.Group(AddedCustom, "Date", {{ "Count", each Table.RowCount(_), type nullable number}}),
    SelectRows = Table.SelectRows(GroupedRows, each [Date] = #date(2022,1,1))
    in
    SelectRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. Source data is ingested into a table.
2. A custom column is added to the table.
3. The table is then grouped by date.
4. Finally, specific rows are selected based on a condition.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
