# Detect Null Injection with Query Folding Considerations

**Description:**
This transformation detects null injection attacks while considering query folding. It helps prevent malicious null values from being injected into the data pipeline, ensuring data integrity and reliability.

### Typical Scenario
In a real-world data pipeline, this transformation can be used to validate and sanitize user input data, preventing null injection attacks that could compromise data analysis or processing.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, "John", "Doe"},
        {2, null, "Doe"},
        {3, "Jane", null}
    }, {"ID", "FirstName", "LastName"}),
    DetectNullInjection = Table.SelectRows(Source, each [FirstName] <> null and [LastName] <> null)
    in
    DetectNullInjection
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The Source table is created with sample data.
    2. The Table.SelectRows function is used to filter out rows where FirstName or LastName is null.
    3. The resulting table, DetectNullInjection, contains only the rows where both FirstName and LastName are not null.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
