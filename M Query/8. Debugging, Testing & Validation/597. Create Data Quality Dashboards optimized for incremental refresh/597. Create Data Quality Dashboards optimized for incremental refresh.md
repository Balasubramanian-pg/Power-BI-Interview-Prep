# Create Data Quality Dashboards optimized for incremental refresh

**Description:**
This use case demonstrates how Power Query (M) can be used to create data quality dashboards that are optimized for incremental refresh. It enables efficient data processing and refreshing of dashboards, ensuring that the data remains up-to-date without requiring a full data reload.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when there is a need to regularly update data quality dashboards with new data. For instance, a company might have daily or weekly data influx that needs to be reflected in their dashboards without manually reloading all the data. This is particularly useful in scenarios where data volumes are large and refreshing the entire dataset is inefficient or impractical.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumn`, `Table.Group`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Age"},
        {1, "John", 25},
        {2, "Jane", 30},
        {3, "Bob", 35}
    }),
    // Incremental refresh example
    IncrementalRefresh = Table.SelectRows(Source, each [Age] > 25)
    in
    IncrementalRefresh
```

### Example Execution Logic Step-By-Step
The transformation works by first loading the necessary data into Power Query. Then, it applies a series of steps to optimize the data for incremental refresh. This typically involves selecting specific rows or columns that are relevant for the data quality dashboard, grouping data for summary calculations, and applying filters for incremental loading. The key M functions like Table.SelectRows, Table.AddColumn, and Table.Group are used to manipulate the data. The final output is a table that can be easily refreshed with new data, updating the dashboard without requiring a full reload of the data.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
