# Detect Null Injection as a Reusable Function

**Description:**
This transformation detects and handles null injection in Power Query (M) by identifying and managing null values within the data. It ensures data integrity by providing a mechanism to detect and potentially remove or replace null values. This is particularly useful in data pipelines where null values could disrupt downstream processes or analytics. By using this transformation, users can ensure their data is clean and consistent.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from various sources that might contain null values. For instance, when combining data from multiple databases or files, null values might be represented differently or might be used to indicate missing information. A data engineer could use this transformation to standardize how null values are handled across the pipeline, ensuring that data is consistent and reliable for analysis or further processing.

### Core M Functions Used
`Table.SelectRows`, `List.Contains`, `Value.IsNull`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, "John", "2022-01-01"},
        {2, null, "2022-01-02"},
        {3, "Jane", null}
    }, {"ID", "Name", "Date"}),
    DetectNulls = (data as table) =>
        let
            Columns = Table.ColumnNames(data),
            HasNull = List.Contains(Columns, "Name"),
            RowsWithNulls = Table.SelectRows(data, each List.Contains({"Name"}, null))
        in
            if HasNull then
                RowsWithNulls
            else
                null
in
    DetectNulls(Source)
```

### Example Execution Logic Step-By-Step
The provided M code defines a reusable function `DetectNulls` that takes a table as input and detects rows containing null values in a specific column (in this case, 'Name'). Here's a step-by-step breakdown:
    1. The `Source` table is created with sample data, including a row with a null value in the 'Name' column.
    2. The `DetectNulls` function iterates over the columns of the input table to identify if any column matches the one to check for nulls (hardcoded here as 'Name').
    3. It uses `Table.SelectRows` to select rows where the specified column contains a null value.
    4. If the specified column exists and contains null values, the function returns a table with those rows; otherwise, it returns null.
    5. The function is then applied to the `Source` table to demonstrate its usage.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*