# Unit Test M Functions without Query Folding

**Description:**
This use case demonstrates how Power Query (M) can be used for unit testing M functions without query folding. It allows for the isolation and testing of M functions, ensuring they behave as expected in a controlled environment.

### Typical Scenario
In a real data pipeline, unit testing M functions without query folding is crucial during the development phase. For instance, when creating a custom function to parse complex JSON data, you would want to test the function in isolation with various inputs to ensure it correctly extracts the desired information without the interference of query folding.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Example function to be tested
    AddOne = (x) => x + 1,
    // Test data
    TestData = {1, 2, 3},
    // Apply the function to test data
    Result = List.Transform(TestData, AddOne)
    // Expected result for comparison
    ExpectedResult = {2, 3, 4}
    // Assert the result
    Assert = if Result = ExpectedResult then "Pass" else "Fail"
in
    Assert
```

### Example Execution Logic Step-By-Step
The transformation typically works as follows inside a Power Query pipeline:
1. **Define the Function to Test**: Create an M function that you want to test, such as `AddOne`.
2. **Prepare Test Data**: Create a list or table that will serve as input for your function.
3. **Apply the Function**: Use a transformation like `List.Transform` to apply your function to the test data.
4. **Define Expected Outcome**: Determine what the expected output should be.
5. **Assert the Result**: Use a conditional statement to compare the actual result with the expected result, outputting a pass or fail message.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
