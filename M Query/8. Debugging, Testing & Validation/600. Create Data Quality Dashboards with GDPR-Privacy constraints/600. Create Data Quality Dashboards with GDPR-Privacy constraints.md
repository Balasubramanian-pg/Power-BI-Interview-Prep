# Create Data Quality Dashboards with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to create data quality dashboards while adhering to GDPR and privacy constraints. It involves transforming and analyzing data to provide insights into data quality while ensuring sensitive information is protected.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when organizations need to monitor data quality across various data sources while ensuring that personally identifiable information (PII) and other sensitive data are handled in accordance with GDPR and privacy regulations. For instance, a company might use this approach to identify duplicate records, incomplete data, or inconsistent data entries without exposing sensitive information.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformColumns`, `Table.Group`

### Code Source Execution
```powerquery
let
    Source = YourTableSource,
    // Apply GDPR/Privacy constraints
    ProtectedData = Table.SelectRows(Source, each not IsBlank('_privacyFlag') and _privacyFlag = "Protected"),
    // Data Quality Analysis
    DataQualityIssues = Table.Group(ProtectedData, "ColumnName", {{"Count", each Table.RowCount(_), type number}}),
    // Further transformations and analysis
    FinalOutput = DataQualityIssues
in
    FinalOutput
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Source**: Start with a data source that contains information which may include sensitive data.
2. **Apply GDPR/Privacy Constraints**: Use `Table.SelectRows` to filter out rows that are marked as protected or sensitive, ensuring that only authorized data is processed further.
3. **Data Quality Analysis**: Apply various data quality checks such as identifying duplicates, incomplete records, or inconsistent data entries using functions like `Table.Group` and `Table.TransformColumns`.
4. **Visualization**: The results are then used to create dashboards that provide insights into data quality issues without compromising on privacy and GDPR compliance.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
