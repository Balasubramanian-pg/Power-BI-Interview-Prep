# Simulate Failure Modes optimized for incremental refresh

**Description:**
This use case demonstrates how Power Query (M) can be used to simulate failure modes optimized for incremental refresh. It helps in identifying potential failure points in a data pipeline and testing the robustness of the pipeline. The transformation allows for the simulation of various failure scenarios, enabling data engineers to optimize the pipeline for incremental refresh.

### Typical Scenario
In a real-world data pipeline, this transformation would be used to test the reliability and resilience of the pipeline. For instance, a data engineer might use this transformation to simulate a scenario where a data source is temporarily unavailable or returns incorrect data. By doing so, they can ensure that the pipeline can handle such failures and continue to function correctly.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumn`, `Table.TransformRows`

### Code Source Execution
```powerquery
let
    // Sample table
    data = Table.FromList({
        {"ID", "Name", "Value"},
        {1, "John", 10},
        {2, "Jane", 20},
        {3, "Bob", 30}
    }, null, null, ExtraValues.Error),

    // Simulate failure mode
    failedData = Table.SelectRows(data, each [Value] > 20),

    // Handle failure
    handledFailedData = Table.AddColumn(failedData, "Status", each "Failed")

in
    handledFailedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. Start with a sample table containing data.
2. Use the Table.SelectRows function to simulate a failure mode, for example, select rows where the 'Value' column is greater than 20.
3. Handle the failed data by adding a new column 'Status' with the value 'Failed' using Table.AddColumn.
4. The resulting table contains only the rows that simulate a failure, with an additional 'Status' column indicating the failure.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
