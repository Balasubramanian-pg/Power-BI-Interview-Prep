# Add Row-level Logging as a reusable function

**Description:**
This transformation adds row-level logging to a data pipeline, allowing for the tracking of changes or events at the row level. It enables the creation of a log that can be used for auditing or debugging purposes. The transformation can be reused across different data pipelines.

### Typical Scenario
In a real-world data pipeline, this transformation can be used to track changes to customer data. For example, when updating customer information, the transformation can log the old and new values, as well as the timestamp and user who made the change.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumns`, `Text.Now`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25},
        {"Jane", 30}
    }, {"Name", "Age"}),
    AddLogColumns = Table.AddColumns(Source, {
        {"Log Timestamp", each Text.Now()},
        {"Log User", each "Current User"}
    })
    in
    AddLogColumns
```

### Example Execution Logic Step-By-Step
The transformation works by first creating a table with the relevant data. Then, it uses the Table.AddColumns function to add two new columns: Log Timestamp and Log User. The Log Timestamp column is populated with the current timestamp using the Text.Now function, and the Log User column is populated with a static value representing the current user. The resulting table now includes row-level logging information.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
