# Assert Row Counts with Error Handling and Retries

**Description:**
This transformation ensures data integrity by verifying the row count of a table, handling potential errors, and implementing retries to achieve reliability in data pipelines.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when data is sourced from unreliable or slow systems. For instance, when fetching data from an API that occasionally returns incomplete data or times out, asserting row counts with error handling and retries guarantees that the pipeline can recover from such transient issues, ensuring data quality and consistency.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `Number.Retries`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25},
        {"Jane", 30}
    }, {"Name", "Age"}),
    AssertRowCount = (table as table, expectedCount as number) as any =>
        try
            if Table.RowCount(table) <> expectedCount then
                error "Row count mismatch"
            else
                table,
        catch
            [error] as Error =>
                if Number.Retries() < 3 then
                    AssertRowCount(table, expectedCount)
                else
                    error "Max retries exceeded"
    in
        AssertRowCount(Source, 2)
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Define the Source Table**: A sample table named `Source` is created with two rows.
    2. **Define the AssertRowCount Function**: This function takes a table and an expected row count as input.
    3. **Error Handling**: Inside the function, it checks if the row count of the table matches the expected count. If not, it raises an error.
    4. **Retry Mechanism**: If an error occurs, the function catches it and retries the row count assertion up to a maximum of 3 retries.
    5. **Execution**: The `AssertRowCount` function is applied to the `Source` table with an expected row count of 2.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*