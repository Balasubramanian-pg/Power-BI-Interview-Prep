# Optimize Merge Strategies for large datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize merge strategies for large datasets, improving performance and efficiency in data processing.

### Typical Scenario
In a real data pipeline, this transformation would be used when dealing with large datasets from different sources that need to be merged efficiently, such as combining sales data from various regions while ensuring optimal performance.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Age"},
        {1, "John", 25},
        {2, "Jane", 30},
        {3, "Bob", 35}
    }),
    MergeTable = Table.FromRows({
        {"ID", "City"},
        {1, "New York"},
        {2, "Los Angeles"},
        {3, "Chicago"}
    }),
    MergedTable = Table.Join(Source, MergeTable, "ID", "ID", JoinKind.LeftOuter)
    in
    MergedTable
```

### Example Execution Logic Step-By-Step
The transformation typically works as follows: 
1. **Source Data Tables**: Two tables are defined: one for source data (e.g., user information) and another for merge data (e.g., city information).
2. **Merging Tables**: The `Table.Join` function is used to merge these tables based on a common column (e.g., ID).
3. **Specifying Join Kind**: The type of join (e.g., left outer join) is specified to determine how to handle rows without matches.
4. **Result**: The merged table contains columns from both source and merge tables, optimized for large datasets.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
