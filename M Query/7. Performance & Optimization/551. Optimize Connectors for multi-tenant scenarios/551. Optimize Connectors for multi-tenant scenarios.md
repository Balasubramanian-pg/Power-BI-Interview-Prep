# Optimize Connectors for multi-tenant scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize connectors for multi-tenant scenarios, allowing for more efficient data retrieval and processing across multiple tenants.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with multi-tenant data sources, such as a cloud-based service that hosts data for multiple customers. For instance, a company providing a SaaS solution might need to aggregate data from various tenants, requiring an optimized connector to handle the diverse data sources efficiently.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
// no snippet mapped
```

### Example Execution Logic Step-By-Step
The transformation typically works by first defining a function that takes tenant-specific parameters, then using Table.SelectRows to filter data based on these parameters. For example, you might create a function that selects data for a specific tenant by filtering on a 'TenantId' column. This approach allows for dynamic querying of multi-tenant data sources, optimizing performance by only retrieving relevant data for each tenant.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*