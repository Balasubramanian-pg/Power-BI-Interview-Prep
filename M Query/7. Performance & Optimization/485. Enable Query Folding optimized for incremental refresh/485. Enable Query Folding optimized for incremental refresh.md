# Enable Query Folding optimized for incremental refresh

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize query folding for incremental refresh, improving performance by reducing the amount of data processed.

### Typical Scenario
In a real data pipeline, this transformation would be used when dealing with large datasets that are updated incrementally, such as daily sales data. By optimizing query folding, the pipeline can efficiently refresh only the new or changed data, rather than reprocessing the entire dataset.

### Core M Functions Used
`Table.SelectRows`, `Table.Folder`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Age"},
        {1, "John", 25},
        {2, "Jane", 30},
        {3, "Bob", 35}
    }),
    IncrementalRefresh = Table.Folder(Source, "ID", "DateModified")
    in
    IncrementalRefresh
```

### Example Execution Logic Step-By-Step
The transformation works by first defining a source table with sample data. Then, it applies the Table.Folder function to enable query folding optimized for incremental refresh. The Table.Folder function takes three arguments: the source table, the key column ("ID"), and the date modified column ("DateModified"). This allows Power Query to efficiently refresh only the changed data.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*