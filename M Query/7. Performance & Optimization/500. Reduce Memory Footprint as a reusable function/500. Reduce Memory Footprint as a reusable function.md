# Reduce Memory Footprint as a Reusable Function

**Description:**
This transformation reduces the memory footprint of a Power Query by selectively loading data into memory, improving performance and efficiency. It achieves this by filtering and optimizing data processing. This is particularly useful for handling large datasets.

### Typical Scenario
In a real-world data pipeline, this transformation is used when dealing with massive datasets that do not fit into memory. For instance, when processing large log files or big data extracts, applying a reduce memory footprint function helps prevent memory overflow errors and speeds up data processing.

### Core M Functions Used
`Table.SelectRows`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"David", 42, "Mexico"}
    }, {"Name", "Age", "Country"}),
    // Apply filter to reduce memory footprint
    FilteredRows = Table.SelectRows(Source, each [Age] > 30)
    in
    FilteredRows
```

### Example Execution Logic Step-By-Step
The transformation works by first importing the necessary data into Power Query. Then, it applies a selective loading function, such as Table.SelectRows or Table.SelectColumns, to filter and optimize the data. For example, you can use Table.SelectRows to select only rows where a certain condition is met, significantly reducing the amount of data in memory. This step-by-step process ensures efficient data handling and transformation.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
