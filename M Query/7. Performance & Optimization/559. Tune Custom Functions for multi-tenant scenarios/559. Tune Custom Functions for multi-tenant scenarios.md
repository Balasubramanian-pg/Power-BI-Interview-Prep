# Tune Custom Functions for multi-tenant scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to tune custom functions for multi-tenant scenarios, enabling flexible and scalable data transformations.

### Typical Scenario
In a real data pipeline, this transformation would be used when dealing with multiple tenants or clients who require customized data processing. For instance, a company providing data analytics services to various clients might use this approach to apply client-specific data transformations.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
// no snippet mapped
```

### Example Execution Logic Step-By-Step
The transformation typically works by defining a custom function that takes parameters specific to each tenant. This function is then applied to the data for each tenant, using parameters like Table.SelectRows to filter or transform the data according to the tenant's requirements. The custom function can be designed to handle variations in data structure or processing rules across different tenants, making it a flexible solution for multi-tenant environments.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*