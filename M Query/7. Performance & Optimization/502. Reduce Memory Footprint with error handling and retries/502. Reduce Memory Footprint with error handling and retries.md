# Reduce Memory Footprint with Error Handling and Retries

**Description:**
This transformation optimizes memory usage in Power Query by implementing efficient data processing techniques, error handling, and retry mechanisms. It ensures data reliability and robustness in the pipeline. By minimizing memory footprint, it enhances performance and reduces the risk of data processing failures.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with large datasets that need to be processed within limited memory resources. For instance, when extracting data from an API that has rate limits or when handling big data from a database, implementing retries and efficient memory usage helps prevent pipeline failures and ensures data consistency.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `Number.Retries`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, "a"},
        {"B", 2, "b"},
        {"C", 3, "c"}
    }, {"Column1", "Column2", "Column3"}),
    // Applying a retry mechanism for error handling
    RetryCount = 3,
    Attempt = (attemptNumber) =>
        try
            // Simulating an operation that might fail
            Data = Table.SelectRows(Source, each [Column2] > attemptNumber),
            Success = true,
            Result = Data
        catch
            as Error
            if attemptNumber < RetryCount then
                Attempt(attemptNumber + 1)
            else
                Error.Record("Max retries exceeded", "", [Error = Error]),
    DataWithRetry = Attempt(0)
    in
    DataWithRetry
```

### Example Execution Logic Step-By-Step
The transformation works by first defining a source table. It then attempts to filter this table based on a condition that might fail (e.g., due to external dependencies or data inconsistencies). The retry mechanism is implemented using a recursive function `Attempt`. This function tries to execute the operation and, if it fails, retries up to a specified number of times (`RetryCount`). If all attempts fail, it captures the error and returns an error record. The final result, with retries, is then returned and can be used in the Power Query pipeline.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*