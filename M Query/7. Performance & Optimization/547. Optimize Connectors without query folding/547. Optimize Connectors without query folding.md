# Optimize Connectors without Query Folding

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize connectors without query folding, enhancing data retrieval efficiency by minimizing the data processed.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with large datasets where direct query folding isn't possible or efficient. For instance, when fetching data from an API that doesn't support query folding natively, optimizing connectors can significantly reduce the amount of data transferred and processed.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Age"},
        {1, "John", 25},
        {2, "Jane", 30},
        {3, "Bob", 35}
    }),
    OptimizedRows = Table.SelectRows(Source, each "Age" > 25)
    in
    OptimizedRows
```

### Example Execution Logic Step-By-Step
The transformation typically works by first establishing a source connection or table. Then, instead of pulling the entire dataset, it applies filters or optimizations directly within the connector or source query (if supported), or applies post-processing filters like `Table.SelectRows` to minimize data transfer and processing. This step-by-step approach ensures that only relevant data is fetched or processed, optimizing performance.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
