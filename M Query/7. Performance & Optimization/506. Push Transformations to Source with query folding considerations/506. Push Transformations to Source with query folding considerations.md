# Push Transformations to Source with Query Folding Considerations

**Description:**
This transformation pushes data processing back to the source, reducing data movement and improving performance. It leverages query folding to translate Power Query operations into source-native queries. This approach is particularly useful when dealing with large datasets and optimizing data pipelines for better efficiency.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when integrating data from various sources, such as databases or cloud storage, into a centralized data warehouse or data lake. For instance, when extracting data from a SQL Server database, pushing transformations back to the source can significantly reduce the amount of data transferred over the network, thus improving performance.

### Core M Functions Used
`Table.SelectRows`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Sql.Database("server", "database"),
    // Folding occurs when functions like Where, SelectRows, SelectColumns are used
    FilteredRows = Table.SelectRows(Source, each [column] > 0),
    // Additional transformations can be added here
    Result = FilteredRows
in
    Result
```

### Example Execution Logic Step-By-Step
The transformation works by applying Power Query functions that can be 'pushed back' or folded into the source query. Here's a step-by-step breakdown:
    1. **Connect to Source**: Establish a connection to the data source (e.g., a SQL database) using a function like `Sql.Database`.
    2. **Apply Transformations**: Use functions like `Table.SelectRows` or `Table.SelectColumns` to filter or select specific data. These operations are designed to fold back to the source.
    3. **Execution**: When the query is executed, Power Query analyzes the operations and translates them into a source-native query (e.g., SQL) whenever possible.
    4. **Result**: The source executes the translated query, and the results are returned back to Power Query, minimizing data transfer and processing overhead.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*