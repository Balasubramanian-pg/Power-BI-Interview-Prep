# Enable Query Folding with Query Folding Considerations

**Description:**
This use case demonstrates how Power Query (M) can be used to enable query folding with query folding considerations. Query folding is a process where the data source can handle the data transformation, reducing the amount of data that needs to be transferred. This transformation achieves efficient data retrieval by leveraging the data source's capabilities.

### Typical Scenario
In a real data pipeline, this transformation would be used when connecting to a database that supports query folding, such as SQL Server. For instance, when retrieving sales data for a specific region, the query can be folded to the database level, reducing the amount of data transferred over the network.

### Core M Functions Used
`Table.SelectRows`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Sql.Database("server", "database"),
    // Assume 'Source' has columns: [Region, SalesAmount]
    FilteredRows = Table.SelectRows(Source, each [Region] = "North")
    // Further transformations can be applied here
in
    FilteredRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. Connect to a data source (e.g., a SQL Server database) using the `Sql.Database` function.
2. Assume the source table has columns like `Region` and `SalesAmount`.
3. Apply a filter to the data using `Table.SelectRows`, folding the filter to the source database.
4. The data source (database) applies the filter and returns only the relevant data.
5. Further transformations can be applied within Power Query if needed.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
