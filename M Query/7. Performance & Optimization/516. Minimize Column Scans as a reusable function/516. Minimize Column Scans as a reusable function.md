# Minimize Column Scans as a reusable function

**Description:**
This transformation minimizes column scans by selecting only required columns, improving performance in Power Query data pipelines.

### Typical Scenario
In a real data pipeline, this transformation is used when dealing with large datasets where not all columns are necessary for analysis, reducing computation time and memory usage.

### Core M Functions Used
`Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, 1},
        {"B", 2, 2},
        {"C", 3, 3}
    }, {"Column1", "Column2", "Column3"}),
    SelectColumns = Table.SelectColumns(Source, {"Column1", "Column2"})
    in
    SelectColumns
```

### Example Execution Logic Step-By-Step
The transformation works by first loading a table with multiple columns. It then uses the Table.SelectColumns function to select only the required columns (e.g., Column1 and Column2), effectively minimizing column scans by excluding unnecessary columns from further processing.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
