# Enable Query Folding without Query Folding

**Description:**
This use case demonstrates how Power Query (M) can be used to enable query folding even when the native query folding is not supported. It allows for efficient data processing by pushing down operations to the source.

### Typical Scenario
In a real data pipeline, this transformation would be used when dealing with data sources that do not natively support query folding for certain operations. For instance, when fetching data from a database that doesn't support complex filtering or sorting natively, but these operations can be simulated or are handled through specific functions.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Sql.Database("server", "database"),
    // Assume native query folding is not supported for this operation
    Data = Table.SelectRows(Source, each [column] > 0)
    in
    Data
```

### Example Execution Logic Step-By-Step
The transformation works by first establishing a connection to the data source (e.g., a SQL database). Then, instead of relying on the data source's native query folding capabilities, it uses M functions like Table.SelectRows to apply filters or transformations directly in the Power Query environment. This approach enables query folding-like behavior without relying on the source's native support, optimizing data retrieval and processing.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
