# Reduce Memory Footprint for large datasets

**Description:**
This transformation reduces the memory footprint of large datasets by optimizing data processing and storage in Power Query (M). It achieves this by implementing efficient data transformation and filtering techniques. By applying these techniques, users can handle larger datasets without running into memory constraints.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with massive datasets that do not fit into memory. For instance, when processing large log files or handling big data from IoT devices, reducing the memory footprint allows for smoother data processing and analysis.

### Core M Functions Used
`Table.SelectRows`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"David", 42, "Mexico"}
    }, {"Name", "Age", "Country"}),
    // Selecting specific rows and columns to reduce memory footprint
    ReducedTable = Table.SelectRows(Source, each "Country" = "USA"),
    FinalTable = Table.SelectColumns(ReducedTable, {"Name", "Age"})
in
    FinalTable
```

### Example Execution Logic Step-By-Step
The transformation works by first importing the large dataset into Power Query. Then, it applies a series of operations to reduce the memory footprint. This includes selecting specific rows and columns that are relevant to the analysis using functions like Table.SelectRows and Table.SelectColumns. By filtering out unnecessary data early in the process, the memory required to process the data is significantly reduced. This approach enables the efficient handling of large datasets within the constraints of available memory.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*