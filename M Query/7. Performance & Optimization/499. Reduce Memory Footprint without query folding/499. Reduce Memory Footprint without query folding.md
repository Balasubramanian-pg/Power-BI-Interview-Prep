# Reduce Memory Footprint without query folding

**Description:**
This use case demonstrates how Power Query (M) can be used to reduce memory footprint without relying on query folding. It is particularly useful when dealing with large datasets that do not support query folding, allowing for efficient data processing and reduced memory usage.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when handling massive datasets that need to be processed in-memory. For instance, when loading large datasets from a data warehouse into Power BI or Excel for analysis, reducing the memory footprint can prevent out-of-memory errors and improve performance.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
// no snippet mapped
```

### Example Execution Logic Step-By-Step
The transformation typically works by selecting specific rows or columns from a table that are necessary for analysis, thereby reducing the amount of data in memory. For example, using Table.SelectRows allows you to filter data based on conditions, and by doing so, it helps in minimizing the memory footprint without relying on the query folding mechanism.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*