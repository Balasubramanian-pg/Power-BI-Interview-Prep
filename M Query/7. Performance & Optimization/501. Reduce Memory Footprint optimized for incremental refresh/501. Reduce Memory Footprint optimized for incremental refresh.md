# Reduce Memory Footprint Optimized for Incremental Refresh

**Description:**
This transformation optimizes memory usage in Power Query for incremental data refreshes. It helps in reducing the memory footprint by efficiently handling large datasets. This is particularly useful when dealing with big data that needs to be refreshed periodically.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when working with large datasets that are updated frequently, such as daily sales data or hourly IoT sensor readings. For instance, a company might have a dataset of daily sales figures that is updated every night. By optimizing the memory footprint for incremental refreshes, the company can ensure that the data is processed efficiently without consuming too much memory.

### Core M Functions Used
`Table.SelectRows`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, 1}, {"B", 2, 2}, {"C", 3, 3}
    }, {"Column1", "Column2", "Column3"}),
    SelectRows = Table.SelectRows(Source, each Column2 > 1),
    SelectColumns = Table.SelectColumns(SelectRows, {"Column1", "Column2"})
    in
    SelectColumns
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: Start with a data source, for example, a table with multiple columns.
    2. **Select Rows**: Use `Table.SelectRows` to filter the data based on specific conditions, reducing the number of rows.
    3. **Select Columns**: Use `Table.SelectColumns` to select only the required columns, further reducing the memory footprint.
    4. **Incremental Refresh**: Implement incremental refresh by only loading new or changed data, rather than the entire dataset.
    This approach ensures that memory usage is optimized during the data refresh process.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
