# Minimize Column Scans Optimized for Incremental Refresh

**Description:**
This transformation optimizes data processing by minimizing column scans, particularly useful in incremental refresh scenarios. It reduces computational load and improves performance by selectively accessing only necessary data.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with large datasets that are frequently updated. For instance, in a sales database where daily sales data is appended, minimizing column scans during incremental refreshes can significantly speed up the data loading process, allowing for more timely insights and reports.

### Core M Functions Used
`Table.SelectColumns`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, 10},
        {"B", 2, 20},
        {"C", 3, 30}
    }, {"Column1", "Column2", "Column3"}),
    // Select specific columns to minimize scans
    SelectedColumns = Table.SelectColumns(Source, {"Column1", "Column3"}),
    // Apply filter to only include rows where Column2 is even
    FilteredRows = Table.SelectRows(SelectedColumns, each [Column2] mod 2 = 0)
    in
    FilteredRows
```

### Example Execution Logic Step-By-Step
This transformation works by first selecting specific columns from a table to minimize the data scanned. Then, it applies a filter to include only the rows that meet certain conditions. The process involves:
    1. Importing necessary tables or queries.
    2. Using 'Table.SelectColumns' to choose only the required columns, thus reducing the data to be processed.
    3. Applying 'Table.SelectRows' to filter rows based on specific conditions, further optimizing the data.
    4. The final output is a table with minimized column scans and optimized for incremental refresh.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*