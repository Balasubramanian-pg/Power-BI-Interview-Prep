# Optimize for Incremental Refresh

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize data refresh by incrementally loading new data. It reduces the amount of data to be refreshed, improving performance. This transformation is useful when dealing with large datasets that are frequently updated.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a business needs to regularly update a large dataset, such as daily sales data, and only the new or changed data needs to be loaded.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
Not provided in the source.
```

### Example Execution Logic Step-By-Step
The transformation typically works by identifying a key column that can be used to determine new or changed data. It then uses this key to filter the data and only load the new or updated rows. For example, it might use a date column to determine which rows have been added or updated since the last refresh.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*