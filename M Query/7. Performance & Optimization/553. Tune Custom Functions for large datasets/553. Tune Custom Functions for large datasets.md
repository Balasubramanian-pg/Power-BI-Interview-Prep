# Tune Custom Functions for large datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize custom functions for handling large datasets efficiently. It highlights the importance of tuning custom functions to improve performance when working with big data. By applying specific M functions, users can significantly enhance the processing speed of their data pipelines.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with large datasets that require custom processing. For instance, when a company has to process millions of customer transactions daily, optimizing custom functions can help reduce processing time and increase productivity.

### Core M Functions Used
`Table.SelectRows`, `List.Accumulate`, `Function.Invoke`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"Peter", 42, "UK"}
    }, {"Name", "Age", "Country"}),
    CustomFunction = (inputTable as table) =>
        Table.SelectRows(inputTable, each Age > 30),
    Result = CustomFunction(Source)
    in
    Result
```

### Example Execution Logic Step-By-Step
The transformation works by first importing the data into a table. Then, a custom function is defined using the Table.SelectRows M function, which filters the table based on a condition (in this case, Age > 30). The custom function is then applied to the source table to produce the filtered result. This approach allows for efficient data processing by minimizing the amount of data being processed and leveraging optimized M functions.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*