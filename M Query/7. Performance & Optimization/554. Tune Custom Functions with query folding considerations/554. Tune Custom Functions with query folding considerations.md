# Tune Custom Functions with Query Folding Considerations

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize custom functions with query folding considerations, allowing for more efficient data processing and reduced computational load.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when there is a need to apply custom functions to large datasets while ensuring that the query folding is properly handled, such as when working with big data sources or complex data transformations.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformColumns`, `List.Transform`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"Peter", 42, "UK"}
    }, {"Name", "Age", "Country"}),
    CustomFunction = (inputTable as table) =>
        Table.TransformColumns(inputTable, {{"Age", each _ * 2}}),
    Result = CustomFunction(Source)
in
    Result
```

### Example Execution Logic Step-By-Step
The transformation works by first defining a custom function that takes a table as input and applies a transformation to one of its columns. In this case, the custom function doubles the values in the 'Age' column. The function is then applied to the source table, resulting in a new table with the transformed data. The query folding considerations are taken into account to ensure that the transformation is applied efficiently.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*