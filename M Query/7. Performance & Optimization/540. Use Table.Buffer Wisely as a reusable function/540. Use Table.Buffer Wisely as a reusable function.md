# Use Table.Buffer Wisely as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used for buffering tables efficiently. It explains when and how to use Table.Buffer to improve performance in data pipelines.

### Typical Scenario
In a real data pipeline, Table.Buffer can be used when you need to materialize a table in memory to improve performance, such as when working with large tables that need to be scanned multiple times.

### Core M Functions Used
`Table.Buffer`

### Code Source Execution
```powerquery
let Source = Table.FromRows({{"John", 25}, {"Mary", 31}, {"Bob", 42}}), BufferedTable = Table.Buffer(Source) in BufferedTable
```

### Example Execution Logic Step-By-Step
The Table.Buffer function is used to materialize a table in memory. When you use Table.Buffer, the entire table is loaded into memory, which can improve performance if the table is scanned multiple times. However, it can also increase memory usage, so it should be used wisely.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
