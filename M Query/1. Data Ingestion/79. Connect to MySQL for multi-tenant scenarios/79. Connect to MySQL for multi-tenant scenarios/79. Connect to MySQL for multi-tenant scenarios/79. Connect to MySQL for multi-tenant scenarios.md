# Connect to MySQL for multi-tenant scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to a MySQL database in a multi-tenant environment, enabling the retrieval of data from multiple tenants.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when integrating data from multiple tenants in a multi-tenant MySQL database, allowing for the consolidation of data from various clients or business units.

### Core M Functions Used
`MySql.Database`

### Code Source Execution
```powerquery
let
    mysqlDatabase = MySql.Database("server", "database", [user="username", password="password"]),
    schema = mysqlDatabase{"schema"},
    table = schema{"table"}
in
    table
```

### Example Execution Logic Step-By-Step
The transformation works by first establishing a connection to the MySql database using the MySql.Database function. It then navigates to the desired schema and table. The connection parameters such as server, database, username, and password are specified to authenticate and access the database.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*