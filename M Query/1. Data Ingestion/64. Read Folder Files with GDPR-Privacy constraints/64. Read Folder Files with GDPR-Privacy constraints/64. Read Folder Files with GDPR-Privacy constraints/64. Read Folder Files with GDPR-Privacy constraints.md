# Reading Folder Files with GDPR/Privacy Constraints in Power Query

**Description:**
This use case demonstrates how to use Power Query (M) to read files from a folder while adhering to GDPR/privacy constraints. It involves selectively processing files based on specific criteria to ensure compliance with data protection regulations.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with sensitive data that must be handled according to GDPR or other privacy laws. For instance, a company might need to process employee data stored in files within a folder, but only if the files are encrypted and contain specific metadata indicating compliance with GDPR.

### Core M Functions Used
`Table.SelectRows`, `Folder.Files`, `File.Contents`

### Code Source Execution
```powerquery
let
    Source = Folder.Files("C:\Path\To\Folder"),
    // Assuming a function to check GDPR compliance
    GDPRCompliantFiles = Table.SelectRows(Source, each [CustomProperty] = "GDPR_Compliant")
    // Further processing steps
in
    GDPRCompliantFiles
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Folder.Files**: This function is used to get a list of files in a specified folder. It returns a table with file metadata.
2. **Table.SelectRows**: This function filters the table to only include rows (files) that meet specific conditions, such as having a custom property indicating GDPR compliance.
3. **Further Processing**: After selecting compliant files, you can proceed with reading their contents using functions like `File.Contents` or other appropriate M functions, ensuring that only compliant data is processed further.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*