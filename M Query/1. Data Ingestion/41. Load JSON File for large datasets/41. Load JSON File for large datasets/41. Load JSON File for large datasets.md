# Load JSON File for Large Datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to efficiently load JSON files for large datasets. It enables data transformation and analysis by handling big data sources.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with large volumes of data stored in JSON format. For instance, loading log data from a web application or processing large-scale IoT sensor data.

### Core M Functions Used
`Json.Document`, `Table.FromList`

### Code Source Execution
```powerquery
let
    Source = Json.Document(File.Contents("C:\path\to\your\file.json"))
    in
    Source
```

### Example Execution Logic Step-By-Step
The transformation works by first using the Json.Document function to parse the JSON file into a Power Query table. Then, depending on the structure of the JSON data, you might need to use additional functions like Table.FromList or Table.SelectRows to reshape the data into a usable format. Finally, the transformed data can be loaded into a data model for analysis.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*