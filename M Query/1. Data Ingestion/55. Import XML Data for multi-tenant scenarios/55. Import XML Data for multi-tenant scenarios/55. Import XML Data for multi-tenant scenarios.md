# Import XML Data for Multi-Tenant Scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to import XML data in a multi-tenant environment, enabling the extraction and transformation of data from XML sources for multiple tenants.

### Typical Scenario
In a real data pipeline, this transformation would be used when integrating data from multiple tenants, each having their own XML data source. For instance, a service provider managing data for various clients can use this approach to import and process XML data for each client separately.

### Core M Functions Used
`Xml.Document`, `Table.FromList`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    xmlData = Xml.Document(File.Contents("C:\Path\To\Your\XMLFile.xml")),
    xmlTable = Table.FromList(xmlData, "Column1", {"Name", "Value"}),
    selectedRows = Table.SelectRows(xmlTable, each "Name" = "YourTargetNode")
in selectedRows
```

### Example Execution Logic Step-By-Step
The transformation typically works as follows:
1. **Read XML File**: Use `Xml.Document` to read the contents of an XML file.
2. **Convert to Table**: Convert the XML structure into a table format using `Table.FromList`.
3. **Select Relevant Rows**: Use `Table.SelectRows` to filter the table and select rows that match the target node or criteria.
4. **Process Data**: Further process the selected data as needed for the multi-tenant scenario.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*