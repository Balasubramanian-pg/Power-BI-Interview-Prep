# Connect to MySQL as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to a MySQL database as a reusable function, allowing for easy data retrieval and manipulation.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a data analyst or engineer needs to frequently connect to a MySQL database to retrieve data for reporting, data warehousing, or ETL processes.

### Core M Functions Used
`MySql.Database`

### Code Source Execution
```powerquery
let
    MySQLConn = MySql.Database("your_server", "your_database", [User="your_username", Password="your_password"]),
    YourTable = MySQLConn{[Name="your_table_name"]}[Data]
    in
    YourTable
```

### Example Execution Logic Step-By-Step
To connect to a MySQL database as a reusable function in Power Query, you would first create a function that takes server, database, username, and password as parameters. Then, use the MySql.Database function to establish a connection to the MySQL database. Finally, select the desired table from the database and retrieve its data.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*