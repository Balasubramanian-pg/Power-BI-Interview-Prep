# Connect to PostgreSQL for large datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to a PostgreSQL database for handling large datasets. It enables data retrieval and transformation from PostgreSQL databases.

### Typical Scenario
In a real data pipeline, this transformation would be used when a business needs to analyze large volumes of data stored in a PostgreSQL database. For instance, a company might use this to connect to their customer database to fetch sales data for analysis.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
The complete raw M query source extracted directly.
```

### Example Execution Logic Step-By-Step
The transformation typically works by first establishing a connection to the PostgreSQL database using the PostgreSQL connector in Power Query. Then, it uses M functions such as Table.SelectRows to filter or transform the data as needed. Finally, it loads the transformed data into the Power Query pipeline for further analysis or processing.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*