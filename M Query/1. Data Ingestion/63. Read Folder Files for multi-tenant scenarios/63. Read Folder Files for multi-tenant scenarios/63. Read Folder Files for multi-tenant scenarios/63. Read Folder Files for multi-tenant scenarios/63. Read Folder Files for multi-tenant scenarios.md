# Read Folder Files for Multi-Tenant Scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to read folder files in a multi-tenant environment, allowing for the retrieval of data from various tenants in a structured and organized manner.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with multi-tenant data sources, such as a cloud storage folder containing data for multiple clients or tenants. For instance, a data engineer might use this transformation to fetch data from a folder structure where each tenant's data is stored in a separate subfolder.

### Core M Functions Used
`Folder.Files`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Folder.Files("C:\\Path\\To\\Folder"),
    // Assuming a structure where each file represents a tenant's data
    FileContents = Table.SelectColumns(Source, {"Name", "Content"})
    // Further transformations can be applied here
in
    FileContents
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Folder.Files**: This function is used to get a table of files from a specified folder. It returns a table with information about each file, including its name, size, and content.
2. **Table.SelectColumns**: After retrieving the table of files, you can select specific columns to work with, such as the file name and content.
3. **Further Transformations**: Depending on the requirements, you might need to apply additional transformations to the content of each file, such as parsing JSON or CSV data.
4. **Combine Data**: Finally, you can combine the data from all files (tenants) into a single table for analysis or further processing.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*