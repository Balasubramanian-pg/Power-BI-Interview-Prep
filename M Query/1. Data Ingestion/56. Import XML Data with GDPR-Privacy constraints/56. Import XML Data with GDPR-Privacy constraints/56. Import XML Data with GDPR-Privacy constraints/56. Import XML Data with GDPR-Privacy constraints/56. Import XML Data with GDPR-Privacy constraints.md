# Import XML Data with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to import XML data while adhering to GDPR/privacy constraints. It ensures that sensitive information is handled appropriately during the data import process.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when importing customer data from an XML source, ensuring that personally identifiable information (PII) is properly masked or anonymized to comply with GDPR regulations.

### Core M Functions Used
`Xml.Document`, `Table.SelectRows`, `Table.TransformColumns`

### Code Source Execution
```powerquery
let
    Source = Xml.Document(File.Contents("C:\Path\To\Your\File.xml")),
    Root = Source{[Content]}[Content],
    Data = Xml.Table(Root, "{}", {{"Person", "Person", "table"}}),
    Person = Data[Person],
    // Apply GDPR/Privacy transformations here
    AnonymizedData = Table.TransformColumns(Person, {{"Name", each "Anonymous"}})
    in
    AnonymizedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. Load the XML file using `Xml.Document`.
2. Extract the root content.
3. Convert the XML structure into a table using `Xml.Table`.
4. Select and transform the required data, applying GDPR/privacy rules (e.g., masking PII).
5. Return the anonymized data.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*