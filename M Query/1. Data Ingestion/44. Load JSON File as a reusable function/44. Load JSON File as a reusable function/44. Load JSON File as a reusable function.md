# Load JSON File as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to load a JSON file as a reusable function, allowing for easy data transformation and integration into a data pipeline.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when there is a need to extract data from a JSON file, such as loading configuration data or importing data from a web service.

### Core M Functions Used
`Json.Document`

### Code Source Execution
```powerquery
let
    Source = Json.Document(File.Contents("C:\path\to\file.json")),
    // ... rest of the M code ...
    in
    Source
```

### Example Execution Logic Step-By-Step
The transformation works by first using the Json.Document function to read the contents of the JSON file into a Power Query table. The File.Contents function is used to read the file contents. The resulting table can then be transformed and manipulated as needed using various Power Query functions.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*