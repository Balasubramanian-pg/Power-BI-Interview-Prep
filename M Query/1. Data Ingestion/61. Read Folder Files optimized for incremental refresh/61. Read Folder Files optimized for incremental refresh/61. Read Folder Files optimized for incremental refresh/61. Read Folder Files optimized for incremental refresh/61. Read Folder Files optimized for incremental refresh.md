# Read Folder Files optimized for incremental refresh

**Description:**
This transformation allows for efficient data loading from a folder by optimizing the incremental refresh process. It enables Power Query to read files from a folder while minimizing the amount of data that needs to be refreshed. This approach is particularly useful when dealing with large datasets that are updated periodically.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when there is a need to regularly update data stored in files within a folder. For instance, a company may receive daily sales reports in CSV format that need to be analyzed. By using this transformation, the company can set up an incremental refresh process that only loads new or updated files, reducing the load on the system and improving performance.

### Core M Functions Used
`Folder.Files`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Folder.Files("C:\Path\To\Folder"),
    // Example of selecting specific rows
    SelectedRows = Table.SelectRows(Source, each [Name] = "SpecificFile.csv")
    // Further transformations...
in
    SelectedRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Folder.Files**: This function is used to read the files from a specified folder. It returns a table with file metadata, including file names, paths, and content.
2. **Filtering and Selection**: You can then filter the files based on specific criteria, such as date modified or file name, using functions like `Table.SelectRows` or `Table.SelectColumns`.
3. **Incremental Refresh**: To optimize for incremental refresh, you can use parameters and query folding to minimize the data that needs to be refreshed. This might involve using a date parameter to only load files modified within a certain timeframe.
4. **Data Loading**: Finally, you can load the selected files into your Power Query pipeline for further analysis and transformation.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*