# Connect to PostgreSQL with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to PostgreSQL while adhering to GDPR/Privacy constraints. It ensures that sensitive data is handled appropriately, and only authorized data is accessed and processed.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when connecting to a PostgreSQL database that contains sensitive customer information. For instance, a company may need to extract customer data for analytics purposes while ensuring that personally identifiable information (PII) is properly masked or anonymized to comply with GDPR regulations.

### Core M Functions Used
`Table.SelectRows`, `Sql.Database`

### Code Source Execution
```powerquery
let
    // Define PostgreSQL connection parameters
    server = "your_server_name",
    database = "your_database_name",
    username = "your_username",
    password = "your_password",
    // Establish connection to PostgreSQL database
    postgresql_db = Sql.Database(server, database, [username = username, password = password]),
    // Assume a table named 'customers' with sensitive data
    customers_table = postgresql_db{[Name = "customers"]}[Data],
    // Apply GDPR/Privacy constraints by selecting only non-sensitive columns
    filtered_customers = Table.SelectRows(customers_table, each [age > 18 and country = "EU"])
    // Further transformations or processing can be applied here
in
    filtered_customers
```

### Example Execution Logic Step-By-Step
This transformation works by first establishing a connection to a PostgreSQL database using the Sql.Database function. It then selects a specific table (e.g., 'customers') and applies a filter using Table.SelectRows to ensure that only rows meeting certain GDPR/Privacy criteria are included. For example, it might select only customers over 18 years old and located in the EU. The result is a table that can be used for further analysis or processing while respecting privacy constraints.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*