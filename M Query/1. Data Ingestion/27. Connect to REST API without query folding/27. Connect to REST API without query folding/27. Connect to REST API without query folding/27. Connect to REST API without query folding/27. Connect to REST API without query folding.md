# Connect to REST API without query folding

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to a REST API without query folding, allowing for flexible data retrieval and processing.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when connecting to a REST API that does not support query folding, such as retrieving data from a third-party service that requires a full dataset to be fetched.

### Core M Functions Used
`Web.Contents`

### Code Source Execution
```powerquery
let
    url = "https://api.example.com/data",
    data = Web.Contents(url),
    json = Json.Document(data)
    in
    json
```

### Example Execution Logic Step-By-Step
The transformation works by using the Web.Contents function to fetch data from the REST API. The response is then converted to JSON format using Json.Document. This allows for further processing and transformation of the data within the Power Query pipeline.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*