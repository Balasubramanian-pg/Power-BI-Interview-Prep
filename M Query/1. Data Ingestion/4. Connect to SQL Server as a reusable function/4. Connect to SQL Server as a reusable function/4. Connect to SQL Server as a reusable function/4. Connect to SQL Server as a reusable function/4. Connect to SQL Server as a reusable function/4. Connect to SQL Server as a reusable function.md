# Connect to SQL Server as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to SQL Server as a reusable function, enabling efficient data retrieval and transformation.

### Typical Scenario
In a real data pipeline, this transformation would be used when there is a need to frequently connect to a SQL Server database to fetch data for analysis or reporting. For instance, a business intelligence application might use this function to fetch sales data from a SQL Server database on a daily basis.

### Core M Functions Used
`Sql.Database`

### Code Source Execution
```powerquery
let
    sqlServerDatabase = Sql.Database("your_server_name", "your_database_name"),
    yourTable = sqlServerDatabase{[Name="your_table_name"]}[Data]
    in
    yourTable
```

### Example Execution Logic Step-By-Step
This transformation typically works by first establishing a connection to the SQL Server database using the Sql.Database function. Then, it selects the specific table from the database that is required for analysis or reporting. The data is then fetched from the table and can be further transformed or analyzed as needed.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*