# Connect to MySQL for large datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to a MySQL database for handling large datasets. It enables data retrieval and transformation from MySQL databases.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a business needs to analyze large volumes of data stored in a MySQL database. For instance, an e-commerce company might use this to connect to their MySQL database to retrieve sales data for analysis.

### Core M Functions Used
`MySql.Database`

### Code Source Execution
```powerquery
let
    server = "your_server_name",
    database = "your_database_name",
    username = "your_username",
    password = "your_password"
    in
    MySql.Database(server, database, [UserName = username, Password = password])
```

### Example Execution Logic Step-By-Step
This transformation typically works by first establishing a connection to the MySQL database using the MySql.Database function. It then retrieves data from the specified database. The connection parameters such as server name, database name, username, and password need to be provided. Once connected, you can navigate through the database schema to select the desired tables or data for transformation.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*