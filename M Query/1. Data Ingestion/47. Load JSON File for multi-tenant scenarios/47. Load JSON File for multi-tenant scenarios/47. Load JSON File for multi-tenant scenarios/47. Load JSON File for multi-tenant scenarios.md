# Load JSON File for Multi-Tenant Scenarios

**Description:**
This transformation loads a JSON file and processes it for multi-tenant scenarios, allowing for the extraction and manipulation of tenant-specific data. It enables data engineers to handle multiple tenants' data stored in a single JSON file, making it easier to manage and analyze. The process involves reading the JSON file, selecting relevant data, and transforming it into a usable format.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a company has multiple clients (tenants) and stores their data in a centralized JSON file. For instance, a SaaS provider might store customer data in a single JSON file, and data engineers need to extract and process data for each client separately. This transformation would allow them to load the JSON file, identify the tenant-specific data, and transform it for further analysis or reporting.

### Core M Functions Used
`Json.Document`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Json.Document(File.Contents("C:\path\to\file.json")),
    // Assume 'Source' is a JSON object with tenant data
    TenantData = Source[TenantData],
    // Convert JSON object to table
    TenantTable = Table.FromList(TenantData, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    // Select rows for a specific tenant
    SelectedRows = Table.SelectRows(TenantTable, each [Column] = "Tenant1")
    // Further transformations...
in
    SelectedRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Read JSON File**: The process starts by reading the JSON file using the `File.Contents` function, which reads the file contents.
2. **Parse JSON**: The `Json.Document` function is then used to parse the JSON file into a Power Query object.
3. **Extract Tenant Data**: Assuming the JSON object contains a list or table of tenant data, this data is extracted.
4. **Convert to Table**: The tenant data is then converted into a table using `Table.FromList` or similar functions.
5. **Select Tenant Data**: Using `Table.SelectRows`, specific rows corresponding to a tenant are selected based on a condition (e.g., a tenant ID).
6. **Further Transformations**: Finally, further transformations can be applied to the selected data as needed, such as filtering, grouping, or joining with other tables.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*