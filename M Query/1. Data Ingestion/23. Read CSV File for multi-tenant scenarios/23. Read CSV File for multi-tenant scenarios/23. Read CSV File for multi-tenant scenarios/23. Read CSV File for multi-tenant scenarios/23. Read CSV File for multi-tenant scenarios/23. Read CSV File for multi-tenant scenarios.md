# Read CSV File for Multi-Tenant Scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to read a CSV file in a multi-tenant environment, allowing for the separation and processing of data based on different tenants or clients.

### Typical Scenario
In a real data pipeline, this transformation would be used when you have a single CSV file containing data for multiple clients or tenants, and you need to process the data separately for each tenant. For example, a service provider has a CSV file with customer data from various clients, and they want to load this data into separate databases or reports for each client.

### Core M Functions Used
`Table.SelectRows`, `Csv.Document`

### Code Source Execution
```powerquery
let
    Source = Csv.Document(File.Contents("C:\Path\To\MultiTenantData.csv"), [Delimiter=",", Encoding=1252]),
    #"Promoted Headers" = Table.PromoteHeaders(Source, [PromoteAllScalars=true]),
    #"Filtered Rows" = Table.SelectRows(#"Promoted Headers", each [Tenant] = "Tenant1")
in
    #"Filtered Rows"
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Read the CSV File**: The `Csv.Document` function is used to read the contents of the CSV file located at "C:\Path\To\MultiTenantData.csv".
2. **Promote Headers**: The `Table.PromoteHeaders` function is then used to promote the first row of the data to headers, assuming the first row contains column names.
3. **Filter Rows by Tenant**: The `Table.SelectRows` function is applied to filter the rows based on the 'Tenant' column. For example, you might filter the data for a specific tenant, say "Tenant1".
4. **Process Data**: After filtering, you can further process the data as needed, such as transforming columns, aggregating data, or loading it into a database.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*