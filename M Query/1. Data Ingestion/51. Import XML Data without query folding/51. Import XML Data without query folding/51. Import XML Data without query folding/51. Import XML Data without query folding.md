# Import XML Data without Query Folding

**Description:**
This use case demonstrates how Power Query (M) can be used to import XML data without query folding, allowing for flexible and efficient data processing.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with XML data sources that do not support query folding, such as when fetching data from a web service or reading XML files from a file system.

### Core M Functions Used
`Xml.Document`, `Table.FromList`

### Code Source Execution
```powerquery
let
    xml = Xml.Document(File.Contents("C:\Path\To\Your\File.xml")),
    data = xml[Root]\"data\"\[\"item\"]\n    in
    Table.FromList(data, Splitter.SplitByNothing(), {"Item"})
```

### Example Execution Logic Step-By-Step
The transformation works by first loading the XML file using the Xml.Document function. Then, it navigates to the desired node (in this case, 'data' and 'item') and uses Table.FromList to convert the XML nodes into a table.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*