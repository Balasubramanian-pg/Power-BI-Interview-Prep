# Connect to SQL Server with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to SQL Server while adhering to GDPR/Privacy constraints. It ensures that sensitive data is handled appropriately, and only authorized personnel have access to it.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a company needs to extract data from SQL Server for analytics or reporting purposes while ensuring compliance with GDPR and other privacy regulations. For instance, a European-based company might use this transformation to extract customer data for marketing analysis while protecting personally identifiable information.

### Core M Functions Used
`Table.SelectRows`, `Sql.Database`

### Code Source Execution
```powerquery
let
    sqlServerDatabase = Sql.Database("sqlserver", "database", [Authentication = "Database", Username = "username", Password = "password"]),
    dataTable = sqlServerDatabase{[Name = "tablename"]}[Data],
    filteredData = Table.SelectRows(dataTable, each [column] = "value")
in
    filteredData
```

### Example Execution Logic Step-By-Step
This transformation typically works by first establishing a connection to the SQL Server database using the Sql.Database function. Then, it selects the specific table to extract data from. To apply GDPR/Privacy constraints, it uses the Table.SelectRows function to filter the data based on predefined conditions, ensuring that only authorized data is extracted and processed further in the pipeline.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*