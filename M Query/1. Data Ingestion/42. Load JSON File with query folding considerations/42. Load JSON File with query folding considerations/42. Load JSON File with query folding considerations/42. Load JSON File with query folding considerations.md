# Load JSON File with Query Folding Considerations

**Description:**
This transformation loads a JSON file while considering query folding, which enables efficient data retrieval by minimizing the amount of data that needs to be processed.

### Typical Scenario
In a real data pipeline, this transformation would be used when ingesting data from a JSON file stored in a data lake or cloud storage, and the goal is to filter or aggregate data during the loading process to reduce the amount of data that needs to be transferred and processed.

### Core M Functions Used
`Json.Document`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Json.Document(File.Contents("C:\path\to\file.json")),
    // Assume we have a JSON array of objects
    data = Source,
    // Example of applying a filter
    filteredData = Table.SelectRows(data, each [columnName] > 0)
    in
    filteredData
```

### Example Execution Logic Step-By-Step
The transformation works as follows: First, the Json.Document function is used to read the JSON file into Power Query. If the JSON file contains an array of objects, it is automatically converted into a table. Then, Table.SelectRows is used to filter the rows based on a condition, demonstrating query folding where the filter is applied at the source level if possible, reducing the data that needs to be loaded and processed.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*