# Connect to SQL Server for Large Datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to connect to SQL Server for handling large datasets. It enables efficient data retrieval and processing from SQL Server databases.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with large volumes of data stored in SQL Server. For instance, a company may need to analyze sales data stored in SQL Server on a daily basis. By connecting to SQL Server using Power Query, the company can efficiently retrieve and process the data for analysis.

### Core M Functions Used
`Sql.Database`

### Code Source Execution
```powerquery
let
    sqlServerDatabase = Sql.Database("your_server_name", "your_database_name"),
    yourTable = sqlServerDatabase{[Name="your_table_name"]}[Data]
    in
    yourTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows: First, you establish a connection to the SQL Server database using the Sql.Database function. Then, you navigate to the specific table you want to retrieve data from. Finally, you extract the data from the table for further processing in Power Query.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*