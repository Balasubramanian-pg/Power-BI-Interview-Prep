# Filter Based On Compliance Flags with GDPR/Privacy constraints

**Description:**
This transformation filters data based on compliance flags, specifically GDPR/Privacy constraints, to ensure that sensitive information is handled according to regulatory requirements. It is commonly used in data pipelines where data privacy and compliance are critical. The transformation helps in masking or removing sensitive data that is subject to GDPR or other privacy laws.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a company needs to ensure that personal data of EU citizens is processed in compliance with GDPR regulations. For instance, when preparing a dataset for analytics or reporting, the company might need to filter out or anonymize certain fields that could identify individuals, such as names, addresses, or phone numbers, based on the GDPR compliance flags.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", "johndoe@example.com", true},
        {"Jane", "Doe", "janedoe@example.com", false}
    }, {"First Name", "Last Name", "Email", "GDPR Flag"}),
    FilteredRows = Table.SelectRows(Source, each ["GDPR Flag"] = false)
    in
    FilteredRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Source**: Start with a table that contains personal data along with a compliance flag (e.g., GDPR Flag).
2. **Filtering**: Use the `Table.SelectRows` function in Power Query (M language) to filter the table rows based on the compliance flag.
3. **Condition**: Specify a condition in the `Table.SelectRows` function to only include rows where the GDPR Flag is set to false, indicating that the data is not subject to GDPR constraints.
4. **Result**: The resulting table will contain only the rows that do not have GDPR restrictions, ensuring that the data processed is compliant with regulatory requirements.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*