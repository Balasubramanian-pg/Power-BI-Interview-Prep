# Mask Sensitive Columns as a reusable function

**Description:**
This transformation masks sensitive information in specified columns, ensuring data privacy and security. It is particularly useful when dealing with confidential data that should not be exposed to unauthorized users. By creating a reusable function, users can easily apply this transformation across multiple queries and data sources.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when handling customer data that includes sensitive information such as credit card numbers, social security numbers, or passwords. For instance, a data engineer working on a customer analytics project might need to mask credit card numbers to ensure that only authorized personnel have access to this sensitive information.

### Core M Functions Used
`Table.SelectRows`, `List.Transform`, `Text.Replace`

### Code Source Execution
```powerquery
let
    MaskSensitiveData = (data as table, columnName as text, replacementText as text) =>
    Table.TransformColumns(data, {{columnName, each replacementText}}),
    data = Table.FromRows({
        {"John", "1234-5678-9012-3456", "john@example.com"},
        {"Jane", "9876-5432-1098-7654", "jane@example.com"}
    }, {"Name", "CreditCard", "Email"}),
    maskedData = MaskSensitiveData(data, "CreditCard", "************")
in
    maskedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Define a reusable function `MaskSensitiveData` that takes a table, a column name, and a replacement text as parameters.
    2. Use `Table.TransformColumns` to replace the values in the specified column with the replacement text.
    3. Apply this function to a sample table containing sensitive data.
    4. The function masks the sensitive information in the specified column, in this case, the credit card numbers.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
