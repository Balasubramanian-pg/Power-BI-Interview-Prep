# Detect PII Patterns with Query Folding Considerations

**Description:**
This transformation detects Personally Identifiable Information (PII) patterns in data while considering query folding for efficient data processing. It helps in identifying sensitive information such as names, addresses, and social security numbers. This is crucial for data privacy and compliance with regulations like GDPR.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from various sources, such as customer information, financial transactions, or healthcare records. For instance, a data engineer might use this transformation to detect and mask PII in customer data before loading it into a data warehouse for analytics.

### Core M Functions Used
`Table.SelectRows`, `List.Contains`, `Text.Contains`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "123 Main St", "123-45-6789"},
        {"Jane", "456 Elm St", "987-65-4321"}
    }, {"Name", "Address", "SSN"}),
    PII_Patterns = {
        {"Name", "Text.Contains", "any"},
        {"SSN", "Text.Contains", "-"}
    },
    Detect_PII = Table.TransformColumns(Source, {
        {"Name", each if List.Contains(PII_Patterns, {"Name", "Text.Contains", "any"}) then "PII Detected" else _},
        {"SSN", each if Text.Contains(_, "-") then "PII Detected" else _}
    })
    in
    Detect_PII
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The M script starts by ingesting data from a source, for example, a table with columns for Name, Address, and SSN.
    2. **Define PII Patterns**: It then defines common PII patterns to look for, such as specific text strings or formats (e.g., the presence of a hyphen in a SSN).
    3. **Apply PII Detection**: The script applies these patterns to the ingested data using functions like `Text.Contains` to search for the patterns in the text columns.
    4. **Transform Data**: If a PII pattern is detected, the data is transformed to indicate that PII has been detected (e.g., by replacing the value with "PII Detected").
    5. **Query Folding**: Where possible, the operations are folded back to the source system to minimize the amount of data transferred and processed, improving performance.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*