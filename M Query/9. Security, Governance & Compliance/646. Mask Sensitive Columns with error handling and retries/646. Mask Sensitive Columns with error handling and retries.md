# Mask Sensitive Columns with Error Handling and Retries

**Description:**
This transformation masks sensitive columns in a Power Query data pipeline while incorporating error handling and retries. It ensures data protection and reliability by securely handling potential errors during the masking process.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with sensitive information such as personal identifiable information (PII) like social security numbers, credit card numbers, or health records. For instance, a data engineer working on a project that involves processing customer data must ensure that sensitive columns are masked to protect customer privacy.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `List.Mask`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "123-45-6789", "johndoe@example.com"},
        {"Jane", "987-65-4321", "janedoe@example.com"}
    }, {"Name", "SSN", "Email"}),
    MaskedSSN = try
        Table.TransformColumns(Source, {{"SSN", each List.Mask(_, "*", 3)}})
    catch
        (e) =>
            Error.Message("Error masking SSN: " & e.Message),
    RetryCount = 3,
    RetryMaskedSSN = List.Mask(MaskedSSN[SSN], "*", 3),
    FinalTable = Table.TransformColumns(MaskedSSN, {{"SSN", each RetryMaskedSSN}})
    in
        FinalTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: The process begins with a table containing sensitive information, such as names, social security numbers (SSN), and email addresses.
    2. **Masking Sensitive Columns**: The `Table.TransformColumns` function is used with a custom function to mask the SSN column. The `List.Mask` function replaces all but the last three characters of each SSN with asterisks (*).
    3. **Error Handling**: A `try-catch` block is implemented to catch any errors that occur during the masking process. If an error occurs, an error message is generated.
    4. **Retries**: A retry mechanism is introduced to handle transient errors. The `RetryCount` variable determines the number of retry attempts.
    5. **Final Transformation**: The transformed table with masked SSNs is then output, ensuring that sensitive information is protected while allowing for the controlled use of the data.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*