# Encrypt At-Rest Metadata with GDPR/Privacy constraints

**Description:**
This transformation ensures that sensitive metadata is encrypted and stored in compliance with GDPR and other privacy regulations, protecting personal data from unauthorized access.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when storing customer information, such as names, addresses, and identification numbers, to prevent data breaches and ensure regulatory compliance.

### Core M Functions Used
`Table.SelectRows`, `BinaryEncrypt`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"CustomerID", "Name", "Address"},
        {"123", "John Doe", "123 Main St"},
        {"456", "Jane Doe", "456 Elm St"}
    }),
    // Assume BinaryEncrypt is a custom function for encryption
    EncryptedData = Table.TransformColumns(Source, {{"Name", BinaryEncrypt}, {"Address", BinaryEncrypt}}),
    EncryptedTable = Table.SelectRows(EncryptedData, each true)
    in
    EncryptedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. Load the metadata into a Power Query table.
2. Use the BinaryEncrypt function to encrypt sensitive columns (e.g., Name, Address).
3. Select the rows to be encrypted using Table.SelectRows.
4. The encrypted data is then stored at rest, protected from unauthorized access.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
