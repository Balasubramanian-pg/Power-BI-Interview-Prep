# Encrypt At-Rest Metadata without query folding

**Description:**
This use case demonstrates how Power Query (M) can be used for Encrypt At-Rest Metadata without query folding. It ensures that metadata remains encrypted when stored, enhancing data security and compliance with data protection regulations.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when handling sensitive metadata that needs to be stored securely. For instance, in a data warehousing scenario, metadata such as data source credentials, data lineage, or data quality metrics might need to be encrypted at rest to prevent unauthorized access.

### Core M Functions Used
`Table.SelectRows`, `Binary.Buffer`, `Crypto.Transform`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"data1", "metadata1"},
        {"data2", "metadata2"}
    }, {"Data", "Metadata"}),
    // Assume a function to encrypt the metadata
    EncryptMetadata = (metadata as text) =>
        let
            encrypted = Crypto.Transform(metadata, "AES", "mysecretkey")
        in
            encrypted,
    EncryptedMetadata = Table.TransformColumns(Source, {{"Metadata", each EncryptMetadata(_)}})
    in
        EncryptedMetadata
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: The process begins with a data source that contains metadata.
    2. **Encryption Function**: An M function, `EncryptMetadata`, is defined to encrypt the metadata using a specified encryption algorithm (e.g., AES).
    3. **Apply Encryption**: The `Table.TransformColumns` function is used to apply the `EncryptMetadata` function to the metadata column of the table.
    4. **Encrypted Output**: The resulting table contains the encrypted metadata, ensuring that it is protected at rest.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
