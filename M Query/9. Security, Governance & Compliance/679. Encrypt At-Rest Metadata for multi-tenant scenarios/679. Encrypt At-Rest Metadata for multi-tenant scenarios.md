# Encrypt At-Rest Metadata for multi-tenant scenarios

**Description:**
This transformation achieves encryption of metadata at rest for multi-tenant scenarios, ensuring data security and compliance. It protects sensitive information by encrypting metadata, making it inaccessible to unauthorized users. This is particularly crucial in multi-tenant environments where data from different tenants is stored and processed together.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a company offers a multi-tenant data processing service. For instance, a cloud-based data analytics platform serving multiple clients would need to ensure that each client's metadata is encrypted and isolated from others. This transformation would be applied during the data ingestion or preprocessing phase to ensure that metadata, such as client names, dataset descriptions, and other sensitive information, are encrypted before being stored.

### Core M Functions Used
`Table.SelectRows`, `List.Transform`, `Text.Encoding`

### Code Source Execution
```powerquery
let
    // Sample table with metadata
    MetadataTable = Table.FromList({
        {"Tenant1", "Description1"},
        {"Tenant2", "Description2"}
    }, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    // Define a function to encrypt text
    EncryptText = (text as text) =>
        let
            // Assuming a simple encryption function for demonstration
            encrypted = Text.Replace(text, "", "")
        in
            encrypted,
    // Apply encryption to metadata
    EncryptedMetadata = Table.TransformColumns(MetadataTable, {{"Description", each EncryptText(_)}})
    // Example of applying encryption
in
    EncryptedMetadata
```

### Example Execution Logic Step-By-Step
This transformation works by first defining a function to encrypt text. In a real-world scenario, this function would use a secure encryption algorithm. The metadata table is then transformed by applying this encryption function to the relevant columns. The result is a new table with the metadata encrypted. Here's a step-by-step breakdown:
    1. **Define Metadata Table**: Create a table containing the metadata.
    2. **Define Encryption Function**: Implement a function that takes text as input and returns the encrypted text.
    3. **Apply Encryption**: Use Table.TransformColumns to apply the encryption function to the desired columns.
    4. **Output Encrypted Table**: The resulting table with encrypted metadata is then used in the data pipeline.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*