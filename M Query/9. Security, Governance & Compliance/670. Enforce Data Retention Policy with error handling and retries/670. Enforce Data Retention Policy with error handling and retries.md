# Enforce Data Retention Policy with Error Handling and Retries

**Description:**
This transformation ensures data compliance by enforcing a data retention policy. It handles errors and implements retries to guarantee data consistency. The policy defines data lifespan and handles data removal or archival based on the defined criteria.

### Typical Scenario
In a real-world data pipeline, this transformation would be used to automate data management tasks, such as removing outdated data or archiving it for long-term storage. For instance, a company may have a policy to retain customer transaction data for 7 years. This transformation would ensure that data older than 7 years is automatically removed or archived.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `Number.Retries`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"DataID", "DataDate"},
        {"1", #date(2022,1,1)},
        {"2", #date(2023,1,1)}
    }),
    // Define data retention policy (e.g., 1 year)
    RetentionPeriod = #duration(1, 0, 0, 0),
    // Calculate data age
    DataAge = Table.AddColumn(Source, "DataAge", each Duration.FromDateTime(DateTime.LocalNow(), [DataDate])),
    // Filter data based on retention policy
    FilteredData = Table.SelectRows(DataAge, each [DataAge] <= RetentionPeriod),
    // Error handling and retries
    RetryCount = 3,
    RetryPolicy = (x as any) =>
        try
            x
        catch
            (e as Exception) =>
                if RetryCount > 0 then
                    RetryPolicy(x)
                else
                    error e
in
    FilteredData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Define Data Source**: The M script starts by defining a sample data source, `Source`, which contains a table with data IDs and dates.
    2. **Define Data Retention Policy**: A data retention period is defined (e.g., 1 year).
    3. **Calculate Data Age**: A new column, `DataAge`, is added to calculate the age of each data record.
    4. **Filter Data**: The `Table.SelectRows` function is used to filter data that is within the retention period.
    5. **Error Handling and Retries**: The script includes a retry policy to handle any errors during execution, allowing for a specified number of retries.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
