# Log Access For Compliance for Multi-Tenant Scenarios

**Description:**
This transformation is used to ensure compliance with log access regulations in multi-tenant environments. It enables the extraction and filtering of relevant log data for auditing and reporting purposes. By applying specific filters and transformations, this use case helps organizations meet their compliance requirements.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when an organization needs to provide log data for compliance audits. For instance, a cloud service provider may need to furnish logs for a specific tenant to demonstrate adherence to regulatory requirements. The transformation would be applied to extract the required log data, filter it according to the tenant's needs, and format it for reporting.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformColumns`, `Table.FilterRows`

### Code Source Execution
```powerquery
let
    // Sample log data
    LogData = Table.FromRows({
        {"Tenant1", "2022-01-01", "Access Granted"},
        {"Tenant2", "2022-01-02", "Access Denied"},
        {"Tenant1", "2022-01-03", "Access Granted"}
    }, {"Tenant", "Date", "Access Status"}),
    // Filter logs for Tenant1
    FilteredLogs = Table.SelectRows(LogData, each [Tenant] = "Tenant1")
    in
    FilteredLogs
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The log data is ingested into a Power Query table.
    2. **Filtering**: The Table.SelectRows function is used to filter the log data for a specific tenant.
    3. **Transformation**: Additional transformations, such as date formatting or data type conversions, can be applied using functions like Table.TransformColumns.
    4. **Output**: The filtered and transformed log data is then output for reporting or further analysis.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
