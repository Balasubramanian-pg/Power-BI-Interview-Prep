# Encrypt At-Rest Metadata optimized for incremental refresh

**Description:**
This use case demonstrates how Power Query (M) can be used to optimize the encryption of metadata at rest, particularly in scenarios where data is incrementally refreshed. The goal is to ensure that metadata remains encrypted while allowing for efficient updates to the data.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a business needs to ensure that metadata, such as data source credentials or sensitive information, is encrypted and protected at rest. For example, a company using Power BI or Azure Data Factory for data processing might implement this transformation to secure their data sources while still allowing for incremental updates to the data.

### Core M Functions Used
`Table.SelectRows`, `Binary.Buffer`, `Crypto.Transform`

### Code Source Execution
```powerquery
let
    // Assume 'data' is the input table
    data = YourTableHere,
    // Define a function to encrypt data
    encryptData = (data as table) =>
        let
            // Convert to binary
            binaryData = Table.ToBinary(data),
            // Encrypt binary data
            encryptedData = Crypto.Transform(binaryData, "AES", "YourEncryptionKey")
        in
            encryptedData,
    // Apply encryption
    encryptedData = encryptData(data)
    // Perform incremental refresh logic here
in
    encryptedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Input**: The process starts with an input table that requires metadata encryption.
2. **Conversion to Binary**: The input data is converted into binary format to prepare it for encryption.
3. **Encryption**: The binary data is then encrypted using a specified encryption algorithm (e.g., AES) and a provided encryption key.
4. **Incremental Refresh**: After encryption, the transformation logic includes incremental refresh capabilities. This allows for the efficient updating of data without re-encrypting the entire dataset.
5. **Output**: The encrypted data is then output, ready for storage or further processing.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
