# Merge Columns Optimized for Incremental Refresh

**Description:**
This transformation merges columns in a Power Query table optimized for incremental refresh, allowing for efficient data updates.

### Typical Scenario
In a real-world data pipeline, this transformation is used when you need to combine data from multiple sources, and then refresh the data incrementally without reloading the entire dataset.

### Core M Functions Used
`Table.SelectRows`, `Table.CombineColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", 25},
        {"Jane", "Doe", 30}
    }, {"FirstName", "LastName", "Age"}),
    MergedColumns = Table.CombineColumns(Source, {"FirstName", "LastName"}, "Merged")
    in
    MergedColumns
```

### Example Execution Logic Step-By-Step
The transformation works by first loading the data into a Power Query table. Then, it uses the Table.CombineColumns function to merge the specified columns. In this example, the 'FirstName' and 'LastName' columns are merged into a new column called 'Merged'. This approach allows for efficient merging of columns, which is particularly useful when working with large datasets and incremental refresh.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*