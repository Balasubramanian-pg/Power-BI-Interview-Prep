# Create Index Column optimized for incremental refresh

**Description:**
This transformation creates an index column in a Power Query table optimized for incremental refresh. It enables efficient data updates by generating a unique index for each row, facilitating the identification of new or modified records during refresh operations.

### Typical Scenario
In a real-world data pipeline, this transformation is used when dealing with large datasets that are periodically updated. For instance, in a sales data warehouse, an index column can be added to track changes in sales records over time, allowing for efficient incremental refreshes of the data.

### Core M Functions Used
`Table.AddIndexColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Name", "Age"},
        {"John", 25}, 
        {"Mary", 31}, 
        {"David", 42} 
    }),
    AddedIndex = Table.AddIndexColumn(Source, "Index", 1, 1)
in
    AddedIndex
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. The source table is defined with a set of rows.
2. The Table.AddIndexColumn function is applied to the source table, adding a new column named 'Index'.
3. The 'Index' column starts from 1 and increments by 1 for each row, effectively creating a unique identifier for each record.
4. This index column allows for efficient tracking of rows during incremental refresh operations, enabling the identification of new or modified records.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
