# Create Index Column for Multi-Tenant Scenarios

**Description:**
This transformation creates an index column for multi-tenant data scenarios, allowing for efficient data processing and analysis. It enables the differentiation of data records across multiple tenants.

### Typical Scenario
In a real-world data pipeline, this transformation is used when dealing with multi-tenant data sources, such as customer-specific data in a SaaS application. For instance, when combining data from various clients into a single dataset, an index column can be created to track and manage data records for each tenant.

### Core M Functions Used
`Table.AddIndexColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Tenant", "Data"},
        {"Tenant1", "Data1"},
        {"Tenant1", "Data2"},
        {"Tenant2", "Data3"}
    }),
    AddedIndex = Table.AddIndexColumn(Source, "Index", 1, 1)
    in
    AddedIndex
```

### Example Execution Logic Step-By-Step
The transformation works by first loading the multi-tenant data into a Power Query table. Then, it utilizes the Table.AddIndexColumn function to add a new index column. This index column assigns a unique integer to each row within the table, starting from 1 and incrementing by 1 for each row. The result is a table with an additional index column that can be used for data analysis, filtering, or joining with other tables.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
