# Extract Substring for Large Datasets

**Description:**
This transformation extracts a specific substring from a large dataset. It is commonly used for data cleansing and processing. The goal is to retrieve a subset of characters from a string column.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when dealing with log files or text data where a specific piece of information needs to be extracted, such as extracting a date or ID from a string.

### Core M Functions Used
`Text.BetweenDelimiters`, `Text.Substring`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Hello, World!"},
        {"This is a test"}
    }, {"Text"}),
    Extracted = Table.AddColumn(Source, "Extracted", each Text.BetweenDelimiters([Text], " ", "!"))
    in
    Extracted
```

### Example Execution Logic Step-By-Step
The transformation works by applying a function to each row in the table. For example, using `Text.BetweenDelimiters`, you specify the delimiters to extract the substring between them. Alternatively, `Text.Substring` can be used to extract a substring by specifying the start and length of the substring.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
