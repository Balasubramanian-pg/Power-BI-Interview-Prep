# Extract Substring with Error Handling and Retries

**Description:**
This transformation extracts a substring from a given text string while incorporating error handling and retries. It ensures robustness in data processing by managing potential errors that may occur during the extraction process. This is particularly useful when dealing with large datasets that may contain inconsistent or erroneous data.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when extracting specific information from text data, such as parsing dates, IDs, or other relevant details. For instance, in a customer data pipeline, you might need to extract a specific substring from a customer's address field to categorize them by region. The error handling and retries ensure that the pipeline doesn't fail entirely due to a few malformed entries.

### Core M Functions Used
`Text.Middle`, `try`, `catch`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Hello World"},
        {"Power Query"},
        {"Substring Extraction"}
    }, {"Text"}),
    ExtractSubstring = Table.AddColumn(Source, "Substring", each try Text.Middle([Text], 0, 5) catch [Error = "Error occurred"])
    in
    ExtractSubstring
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: Start with a table containing a text column.
    2. **Add Column**: Use the `Table.AddColumn` function to create a new column.
    3. **Extract Substring**: Within the new column, use `Text.Middle` to extract a substring.
    4. **Error Handling**: Wrap the `Text.Middle` function in a `try`/`catch` block to handle any errors that occur during extraction. If an error occurs, catch it and return a custom error message.
    5. **Retries**: While M doesn't directly support retries in the traditional sense, you can implement a retry mechanism using a custom function with recursive calls or a loop, ensuring that transient errors are handled gracefully.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*