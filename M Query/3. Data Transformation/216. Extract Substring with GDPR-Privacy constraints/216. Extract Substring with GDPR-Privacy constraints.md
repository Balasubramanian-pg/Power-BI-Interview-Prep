# Extract Substring with GDPR/Privacy constraints

**Description:**
This transformation achieves extracting specific substrings from a data field while adhering to GDPR/privacy constraints, ensuring sensitive information is handled appropriately. It allows for targeted data masking or redaction.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with personal data that must be anonymized. For instance, extracting a specific part of an email address or phone number for analysis while keeping the rest private.

### Core M Functions Used
`Text.Middle`, `Text.Start`, `Text.End`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John.Doe@example.com"},
        {"Jane.Smith@example.com"}
    }, {"Email"}),
    Extracted = Table.TransformColumns(Source, {{"Email", each Text.Middle(_, 0, 5)"}})
    in
    Extracted
```

### Example Execution Logic Step-By-Step
The transformation works by first loading the data into Power Query. Then, it uses the Text.Middle function to extract a specified substring from each row in the 'Email' column. For example, extracting the first 5 characters of each email address. This approach ensures that only non-sensitive parts of the data are exposed for analysis, adhering to GDPR/privacy requirements.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*