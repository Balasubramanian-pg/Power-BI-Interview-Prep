# Create Calculated Column for Multi-Tenant Scenarios

**Description:**
This transformation achieves the creation of a calculated column in Power Query (M) for multi-tenant scenarios, allowing for dynamic data segregation and analysis across different tenants.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with multi-tenant data sources, such as a single database serving multiple clients or organizations. For instance, a company providing a SaaS product might use this transformation to create tenant-specific calculated columns for customized reporting and analytics.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Tenant", "Value"},
        {"Tenant1", 100},
        {"Tenant2", 200},
        {"Tenant1", 300},
        {"Tenant2", 400}
    }),
    AddedCustom = Table.AddColumn(Source, "CalculatedColumn", each if [Tenant] = "Tenant1" then [Value] * 2 else [Value] * 3)
    in
    AddedCustom
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: Start with a table containing tenant data and values.
    2. **Select Rows**: Use `Table.SelectRows` if needed to filter or select specific rows or columns.
    3. **Add Column**: Utilize `Table.AddColumn` to create a new calculated column.
    4. **Conditional Logic**: Within the new column, apply conditional logic (e.g., `if` statement) to perform calculations based on the tenant.
    5. **Result**: The resulting table includes the original data plus the new calculated column, segregated by tenant.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
