# Transpose Table with Query Folding Considerations

**Description:**
This transformation is used to transpose a table while considering query folding, which enables efficient data processing by pushing operations back to the source.

### Typical Scenario
In a real data pipeline, this transformation is useful when dealing with data sources that have a large number of columns but a small number of rows, and the goal is to rotate the data for better analysis or reporting.

### Core M Functions Used
`Table.Transpose`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, 2, 3},
        {"B", 4, 5, 6},
        {"C", 7, 8, 9}
    }, {"Column1", "Column2", "Column3", "Column4"}),
    TransposedTable = Table.Transpose(Source),
    RenamedColumns = Table.RenameColumns(TransposedTable, {{"Column1", "A"}, {"Column2", "B"}, {"Column3", "C"}})
    in
    RenamedColumns
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The source table is defined with rows and columns.
    2. The Table.Transpose function is used to transpose the table, swapping rows with columns.
    3. The columns are then renamed for clarity and readability.
    This process enables the data to be restructured for better analysis or reporting.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*