# Merging Columns without Query Folding in Power Query

**Description:**
This transformation merges two or more columns in a table without relying on query folding, which is useful when working with data sources that do not support query folding. It allows for more flexible data manipulation and transformation. This is particularly useful when you need to combine data from multiple columns into a single column.

### Typical Scenario
In a real-world data pipeline, you might have a table with separate columns for first and last names, and you want to create a full name column by combining these. This transformation is especially useful when dealing with data sources that do not support query folding, such as certain web APIs or data sources with complex authentication mechanisms.

### Core M Functions Used
`Table.SelectRows`, `Table.CombineColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", 25},
        {"Jane", "Smith", 30}
    }, {"FirstName", "LastName", "Age"}),
    Combined = Table.CombineColumns(Source, {"FirstName", "LastName"}, "Concat", "FullName")
    in
    Combined
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. You start with a table that has separate columns for the data you want to merge.
    2. You use the `Table.CombineColumns` function to specify which columns to merge and how to merge them.
    3. The `Table.CombineColumns` function takes the source table, specifies the columns to combine (in this case, "FirstName" and "LastName"), and defines the combination method (in this case, simple concatenation).
    4. The result is a new table with the merged data in a single column.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
