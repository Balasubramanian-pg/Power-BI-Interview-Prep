# Create Index Column with GDPR/Privacy constraints

**Description:**
This transformation creates an index column in a Power Query table while adhering to GDPR/privacy constraints. It ensures that sensitive information is properly handled and anonymized. This is particularly useful when dealing with personal data that requires protection.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting customer data that needs to be pseudonymized for analytics purposes. For instance, when creating a dataset for customer behavior analysis, an index column can be added to uniquely identify each record without exposing sensitive personal information.

### Core M Functions Used
`Table.AddIndexColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe"},
        {"Jane", "Doe"}
    }, {"FirstName", "LastName"}),
    AddedIndex = Table.AddIndexColumn(Source, "Index", 1, 1)
    in
    AddedIndex
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. **Data Ingestion**: Start with a table containing personal data.
2. **Adding Index Column**: Use the `Table.AddIndexColumn` function to create a new column that assigns a unique index to each row.
3. **GDPR/Privacy Compliance**: Ensure that the index column does not expose any sensitive information and that the data is processed in accordance with GDPR guidelines.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*