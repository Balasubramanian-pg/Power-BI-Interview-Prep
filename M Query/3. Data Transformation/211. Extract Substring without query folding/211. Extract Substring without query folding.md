# Extract Substring without Query Folding

**Description:**
This transformation extracts a specific substring from a text column in a Power Query table without relying on query folding. It is useful for manipulating text data that doesn't support folding operations.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when dealing with text data that requires specific substring extraction, such as extracting a specific part of a URL or parsing a custom ID format.

### Core M Functions Used
`Text.Middle`, `Text.Start`, `Text.End`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Hello World"},
        {"Power Query"},
        {"Substring Example"}
    }, {"Text"}),
    ExtractedSubstring = Table.TransformColumns(Source, {{"Text", each Text.Middle(_, 7, 5)}})
    in
    ExtractedSubstring
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 1) The Source table is created with a text column. 2) The Table.TransformColumns function is used to apply the Text.Middle function on the 'Text' column. 3) The Text.Middle function takes the text, starts at the 7th character, and extracts a substring of length 5. 4) The resulting table, ExtractedSubstring, contains the extracted substrings.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*