# Create Index Column as a Reusable Function in Power Query

**Description:**
This transformation creates a new index column in a table, which can be used for various purposes such as row numbering, sorting, and grouping. It is a reusable function that can be easily integrated into a Power Query pipeline. The index column can be used to keep track of the original row order or to create a unique identifier for each row.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when there is a need to process data in a specific order or to create a unique identifier for each row. For example, when loading data from a database, an index column can be added to keep track of the original row order, which can be useful for auditing or debugging purposes.

### Core M Functions Used
`Table.AddIndexColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Name", "Age"},
        {"John", 25},
        {"Mary", 31},
        {"David", 42}
    }),
    AddedIndex = Table.AddIndexColumn(Source, "Index", 1, 1)
    in
    AddedIndex
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The `Table.FromRows` function is used to create a sample table.
    2. The `Table.AddIndexColumn` function is used to add a new index column to the table.
    3. The `AddedIndex` variable holds the resulting table with the added index column.
    The resulting table will have an additional column named 'Index' with values starting from 1 and incrementing by 1 for each row.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
