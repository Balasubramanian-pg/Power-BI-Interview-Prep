# Extract Substring with Query Folding Considerations

**Description:**
This transformation extracts a substring from a string column in a table, considering query folding for optimal performance. It is useful for data cleansing and processing. The goal is to derive a new column by extracting a specific part of an existing string column.

### Typical Scenario
In a real data pipeline, this transformation can be used when loading data from a database or file where string columns need to be processed to extract specific information, such as extracting a year from a date string or getting a first name from a full name column.

### Core M Functions Used
`Text.Middle`, `Table.TransformColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Hello World"},
        {"Power Query"},
        {"Query Folding"}
    }, {"Text"}),
    ExtractSubstring = Table.TransformColumns(Source, {{"Text", each Text.Middle(_, 1, 5)}})
    in
    ExtractSubstring
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 1) The Source table is created with a single column 'Text' containing string values. 2) The Table.TransformColumns function is used to apply the Text.Middle function on each row of the 'Text' column. 3) The Text.Middle function takes three arguments: the string, the starting position, and the length of the substring to extract. In this case, it extracts a substring starting from the second character (index 1) with a length of 5 characters. 4) The result is a new table with the transformed 'Text' column containing the extracted substrings.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*