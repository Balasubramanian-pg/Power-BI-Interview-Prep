# Transpose Table with GDPR/Privacy constraints

**Description:**
This transformation achieves data restructuring while adhering to GDPR/privacy constraints. It enables the pivoting of data for analysis or reporting purposes, ensuring sensitive information is handled appropriately.

### Typical Scenario
In a real-world data pipeline, this transformation is used when there is a need to analyze or report data that is currently structured in a way that does not support the required analysis. For instance, a dataset containing customer information across multiple columns (e.g., Customer1, Customer2) needs to be transposed to a format where each row represents a single customer, while ensuring that personally identifiable information is properly anonymized or aggregated to comply with GDPR/privacy regulations.

### Core M Functions Used
`Table.SelectRows`, `Table.Transpose`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "New York"},
        {"Mary", 31, "Los Angeles"},
        {"David", 42, "Chicago"}
    }, {"Name", "Age", "City"}),
    TransposedTable = Table.Transpose(Source),
    PromotedHeaders = Table.PromoteHeaders(TransposedTable, [PromoteAllScalars=true])
    in
    PromotedHeaders
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The source table is ingested into Power Query.
    2. **Transpose Operation**: The Table.Transpose function is applied to the source table. This operation swaps the rows with columns.
    3. **Header Promotion**: After transposition, the Table.PromoteHeaders function is used to promote the first row of the transposed table into headers.
    4. **GDPR/Privacy Compliance**: During this process, any personally identifiable information (PII) should be handled according to GDPR/privacy regulations, such as anonymization or aggregation, to ensure data protection.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
