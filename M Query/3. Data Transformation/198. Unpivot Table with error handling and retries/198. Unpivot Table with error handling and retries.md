# Unpivot Table with Error Handling and Retries

**Description:**
This transformation achieves the unpivoting of a table while incorporating error handling and retries, ensuring robustness in data processing. It allows for the conversion of data from a pivoted format to a more normalized form, handling potential errors that may arise during the process.

### Typical Scenario
In a real-world data pipeline, this transformation is particularly useful when dealing with large datasets that are subject to occasional data quality issues. For instance, when extracting data from an external source that may intermittently provide malformed data, this approach ensures that the pipeline can gracefully handle such errors and continue processing once the issues are resolved.

### Core M Functions Used
`Table.SelectRows`, `Table.Unpivot`, `try`, `catch`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, 2, 3},
        {"B", 4, 5, 6},
        {"C", 7, 8, 9}
    }, {"Category", "Jan", "Feb", "Mar"}),
    Unpivot = try Table.Unpivot(Source, "Attribute", "Values", {"Jan", "Feb", "Mar"})
    catch (e) =>
        let
            ErrorMessage = "Error occurred during unpivoting: " & e.Message,
            RetryCount = 3,
            RetryAttempt = if RetryCount > 0 then
                // Implement retry logic here
                Source
            else
                error ErrorMessage
        in
            RetryAttempt
in
    Unpivot
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Source**: The process begins with a table that has a pivoted format, where certain columns represent values that need to be normalized.
2. **Unpivot Operation**: The `Table.Unpivot` function is used within a `try` block to attempt the unpivoting of the table. This operation converts the pivoted columns into two new columns: one for the attribute and one for the value.
3. **Error Handling**: A `catch` block is used to handle any errors that occur during the unpivoting process. If an error occurs, it captures the error message.
4. **Retry Mechanism**: Optionally, a retry mechanism can be implemented. This could involve re-attempting the unpivoting operation a specified number of times if it fails, to handle transient errors.
5. **Result**: The final result is a table in a more normalized form, with error handling and optional retries ensuring the process's robustness.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
