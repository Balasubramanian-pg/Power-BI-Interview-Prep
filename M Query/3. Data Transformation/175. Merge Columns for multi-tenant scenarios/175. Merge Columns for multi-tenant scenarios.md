# Merging Columns for Multi-Tenant Scenarios in Power Query

**Description:**
This transformation merges columns in a Power Query pipeline to support multi-tenant data scenarios, enabling the combination of data from different sources into a unified view.

### Typical Scenario
In a real-world data pipeline, this transformation is used when dealing with multi-tenant data sources, such as customer-specific data stored in separate tables or files. For instance, a service provider may have data for multiple clients stored in distinct columns, and merging these columns enables unified reporting and analysis across all clients.

### Core M Functions Used
`Table.CombineColumns`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Tenant1", "Data1"},
        {"Tenant2", "Data2"}
    }, {"Tenant", "Data"}),
    MergedColumns = Table.CombineColumns(Source, {"Tenant", "Data"}, "Merged")
    in
    MergedColumns
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Source**: Start with a table containing separate columns for each tenant's data.
2. **Select Relevant Data**: Use `Table.SelectRows` to filter or select specific rows if needed.
3. **Merge Columns**: Utilize `Table.CombineColumns` to merge the selected columns into a unified format.
4. **Result**: The resulting table has a combined view of the data, facilitating further analysis or processing.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*