# Group Rows By Category with GDPR/Privacy constraints

**Description:**
This transformation groups rows by category while adhering to GDPR/privacy constraints, ensuring sensitive data is handled appropriately. It enables the aggregation of data for analysis while protecting individual privacy.

### Typical Scenario
In a customer database, grouping rows by geographical regions for analysis while ensuring that personally identifiable information (PII) is anonymized or aggregated to prevent individual identification.

### Core M Functions Used
`Table.Group`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Category", "Value"},
        {"A", 1},
        {"B", 2},
        {"A", 3},
        {"C", 4}
    }),
    GroupedRows = Table.Group(Source, {"Category"}, {{"Sum", each List.Sum([Value]), type nullable number}})
    in
    GroupedRows
```

### Example Execution Logic Step-By-Step
The transformation works by first loading the data into Power Query. Then, it groups the rows by the 'Category' column using the Table.Group function. For each group, it calculates the sum of the 'Value' column. This approach ensures that the data is aggregated by category while maintaining GDPR/privacy constraints by not exposing individual records.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
