# Create Index Column for Large Datasets

**Description:**
This transformation adds a new index column to a large dataset, allowing for easier data manipulation and analysis. It is particularly useful when working with big data, as it enables efficient data processing and sorting. The index column can be used as a unique identifier for each row, making it easier to perform data operations.

### Typical Scenario
In a real-world data pipeline, creating an index column is essential when dealing with large datasets. For instance, when processing customer data, an index column can be added to uniquely identify each customer record. This enables efficient data sorting, filtering, and merging with other datasets.

### Core M Functions Used
`Table.AddIndexColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Name", "Age"},
        {"John", 25},
        {"Mary", 31},
        {"David", 42}
    }),
    AddedIndex = Table.AddIndexColumn(Source, "Index", 1, 1)
    in
    AddedIndex
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The `Table.FromRows` function is used to create a sample table.
    2. The `Table.AddIndexColumn` function is then applied to the table, adding a new index column named "Index".
    3. The index column starts from 1 and increments by 1 for each row.
    4. The resulting table with the added index column can then be used for further data processing and analysis.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*