# Group Rows By Category for large datasets

**Description:**
This transformation groups rows by a specific category, allowing for efficient analysis and processing of large datasets. It enables data aggregation, filtering, and sorting, making it easier to extract insights from complex data.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with large datasets that require grouping and aggregation, such as sales data grouped by region or product category.

### Core M Functions Used
`Table.Group`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Category", "Value"},
        {"A", 10},
        {"B", 20},
        {"A", 30},
        {"B", 40}
    }),
    GroupedRows = Table.Group(Source, {"Category"}, {{"Total Value", each List.Sum([Value]), type nullable number}})
    in
    GroupedRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
    1. The data is loaded into a table. 
    2. The Table.Group function is used to group the rows by the 'Category' column. 
    3. For each group, the List.Sum function is used to calculate the total value. 
    4. The resulting table contains the grouped rows with the calculated total value for each category.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*