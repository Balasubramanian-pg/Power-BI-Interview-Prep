# Create Index Column with Query Folding Considerations

**Description:**
This transformation adds a new index column to a table, allowing for efficient data retrieval and manipulation. It is particularly useful when working with large datasets and optimizing query performance. The index column can be used for sorting, filtering, and joining tables.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when extracting data from a database or a data warehouse. For instance, when retrieving sales data, an index column can be added to uniquely identify each sales record, making it easier to sort and filter the data.

### Core M Functions Used
`Table.AddIndexColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Name", "Age"},
        {"John", 25},
        {"Mary", 31},
        {"David", 42}
    }),
    AddedIndex = Table.AddIndexColumn(Source, "Index", 1, 1)
    in
    AddedIndex
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The `Table.FromRows` function is used to create a sample table.
    2. The `Table.AddIndexColumn` function is then used to add a new index column to the table, starting from 1 and incrementing by 1 for each row.
    3. The resulting table with the added index column is then returned.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*