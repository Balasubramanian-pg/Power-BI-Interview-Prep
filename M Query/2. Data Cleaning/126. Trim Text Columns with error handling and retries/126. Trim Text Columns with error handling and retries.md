# Trim Text Columns with Error Handling and Retries

**Description:**
This transformation trims text columns in a Power Query table, handling potential errors and allowing for retries. It ensures data cleanliness by removing leading and trailing whitespaces from text data. This process is crucial for maintaining data consistency and accuracy.

### Typical Scenario
In a real-world data pipeline, this transformation is often used when ingesting data from various sources, such as CSV files or databases, where text columns may contain leading or trailing whitespaces. For instance, when processing customer information, trimming text columns helps prevent errors in data analysis and reporting by ensuring that addresses, names, and other text data are properly formatted.

### Core M Functions Used
`Table.TransformColumns`, `Text.Trim`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"  John", "New York", 25},
        {"Alice  ", "Los Angeles", 30},
        {"Bob", "Chicago", 35}
    }, {"Name", "City", "Age"}),
    TrimmedTable = Table.TransformColumns(Source, {{"Name", each Text.Trim(_)}})
    in
    TrimmedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The source table is created with sample data, including text columns with leading and trailing whitespaces.
    2. **Transformation**: The `Table.TransformColumns` function is used to apply the `Text.Trim` function to the 'Name' column. This removes leading and trailing whitespaces from each value in the 'Name' column.
    3. **Error Handling**: While not explicitly shown, error handling can be implemented using `try` and `catch` blocks within the transformation to handle any unexpected errors during the trimming process.
    4. **Result**: The transformed table, `TrimmedTable`, contains the same data but with the 'Name' column values trimmed of extra whitespaces.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*