# Promote Headers with GDPR/Privacy constraints

**Description:**
This transformation promotes the first row of a table to headers while considering GDPR/privacy constraints. It ensures that sensitive information is handled properly during data processing. The transformation is useful for data pipelines that require header promotion with data protection regulations in mind.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from a source where the first row contains sensitive information that needs to be promoted to headers while ensuring compliance with GDPR/privacy regulations. For instance, when loading customer data from a CSV file, the pipeline would need to handle sensitive columns like names and addresses appropriately.

### Core M Functions Used
`Table.SelectRows`, `Table.PromoteHeaders`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", "john.doe@example.com"},
        {"Jane", "Doe", "jane.doe@example.com"}
    }, {"Column1", "Column2", "Column3"}),
    PromotedHeaders = Table.PromoteHeaders(Source, [PromoteAllScalars=true])
    in
    PromotedHeaders
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Ingest Data**: Load data from a source into a Power Query table.
    2. **Promote Headers**: Use the `Table.PromoteHeaders` function to promote the first row of the table to headers.
    3. **Handle Sensitive Data**: Apply GDPR/privacy constraints by selecting specific columns to keep or remove using `Table.SelectRows`.
    4. **Output**: The resulting table with promoted headers and sensitive data handled according to regulations.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
