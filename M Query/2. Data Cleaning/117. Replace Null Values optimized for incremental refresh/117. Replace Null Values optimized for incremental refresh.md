# Replace Null Values optimized for incremental refresh

**Description:**
This transformation replaces null values in a dataset, optimized for incremental refresh. It ensures data consistency and accuracy by handling null values efficiently. This is particularly useful when dealing with large datasets that are updated incrementally.

### Typical Scenario
In a real-world data pipeline, this transformation is used when a dataset is updated incrementally, and null values need to be replaced with meaningful data. For instance, in a sales database, if a customer's address is null, it can be replaced with a default value or a value from a previous record.

### Core M Functions Used
`Table.SelectRows`, `Table.ReplaceValue`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, 10, null},
        {2, 20, "B"},
        {3, 30, null}
    }, {"ID", "Value", "Category"}),
    ReplacedNulls = Table.ReplaceValue(Source, null, "Unknown", Replacer.ReplaceValue, {"Category"})
    in
    ReplacedNulls
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The source data is loaded into a table.
    2. The Table.ReplaceValue function is used to replace null values in the 'Category' column with a default value 'Unknown'.
    3. The resulting table with replaced null values is then used for further processing or analysis.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*