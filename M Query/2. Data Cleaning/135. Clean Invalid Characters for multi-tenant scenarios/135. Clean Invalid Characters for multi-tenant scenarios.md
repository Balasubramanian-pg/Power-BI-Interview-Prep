# Clean Invalid Characters for multi-tenant scenarios

**Description:**
This transformation is used to clean invalid characters from data in multi-tenant scenarios. It ensures data consistency and validity across different tenants. The transformation helps in removing or replacing characters that do not meet specific criteria, making the data reliable for analysis and processing.

### Typical Scenario
In a multi-tenant data environment, different tenants may have data with varying character sets. For instance, a tenant's data might contain special characters that are not allowed in another tenant's data schema. This transformation is crucial when integrating data from multiple tenants into a single pipeline, ensuring that the data conforms to a standardized format.

### Core M Functions Used
`Text.Replace`, `Text.Clean`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"InvalidChar1", "Tenant1"},
        {"InvalidChar2", "Tenant2"}
    }, {"Data", "Tenant"}),
    CleanData = Table.TransformColumns(Source, {{"Data", each Text.Clean(_)}})
    in
    CleanData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The source data is ingested into a Power Query table.
    2. **Character Identification**: Identify the invalid characters that need to be cleaned.
    3. **Text Cleaning**: Use the `Text.Clean` function to remove invalid characters from the data.
    4. **Data Transformation**: Apply the cleaning function to the relevant columns using `Table.TransformColumns`.
    5. **Output**: The cleaned data is then outputted for further processing or analysis.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
