# Replace Null Values as a Reusable Function in Power Query

**Description:**
This transformation replaces null values in a dataset with a specified value, ensuring data consistency and reliability. It is useful for handling missing data in a dataset. The function can be reused across different queries and data sources.

### Typical Scenario
In a real-world data pipeline, this transformation is often used when ingesting data from various sources that may contain null or missing values. For instance, when merging customer data from different systems, null values in fields like phone numbers or addresses can be replaced with default values or placeholders to facilitate data standardization and analysis.

### Core M Functions Used
`Table.SelectRows`, `Replacer.ReplaceValue`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, null, "John"},
        {2, "2022-01-02", "Jane"},
        {3, null, "Bob"}
    }, {"ID", "Date", "Name"}),
    ReplaceNulls = Table.ReplaceValue(Source, null, "Unknown", Replacer.ReplaceValue, {"Date"})
in
    ReplaceNulls
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Define the Source Table**: Create a table with sample data that includes null values.
    2. **Identify Columns with Nulls**: Determine which columns contain null values that need replacement.
    3. **Apply Replacement**: Use the `Table.ReplaceValue` function to replace nulls with a specified value (e.g., "Unknown").
    4. **Reusable Function**: Encapsulate this logic in a function that can be applied to different tables and columns as needed.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
