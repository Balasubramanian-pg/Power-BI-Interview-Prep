# Fill Down Missing Values with Error Handling and Retries

**Description:**
This transformation fills down missing values in a Power Query table with error handling and retries. It ensures data consistency by propagating the last observed value to subsequent rows until a new value is encountered. This process is crucial for maintaining data integrity during data processing pipelines.

### Typical Scenario
In a real-world data pipeline, this transformation is often used when dealing with time-series data or sequential data where values are occasionally missing. For instance, in financial data processing, stock prices might be missing for certain dates. The Fill Down Missing Values transformation ensures that these missing prices are filled with the last known price, enabling accurate calculations and analyses.

### Core M Functions Used
`Table.SelectRows`, `Table.FillDown`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, null}, 
        {"A", null, null}, 
        {"A", 3, null},
        {"B", 4, null},
        {"B", null, null},
        {"B", 6, null}
    }, {"Category", "Value1", "Value2"}),
    FilledDown = Table.FillDown(Source, {"Value1", "Value2"})
    in
    FilledDown
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
    1. **Identify Missing Values**: The process starts with a table that contains missing values represented as null. 
    2. **Select Relevant Columns**: Identify the columns that require fill down operations (e.g., "Value1" and "Value2").
    3. **Apply Fill Down**: Use the Table.FillDown function in Power Query (M language) to fill down the missing values in the specified columns. This function propagates the last observed value to subsequent rows until a new value is encountered.
    4. **Error Handling and Retries**: Implement error handling to catch any exceptions that may occur during the fill down process. If an error occurs, the process can be retried or handled appropriately to ensure data consistency and integrity.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*