# Trim Text Columns with GDPR/Privacy constraints

**Description:**
This transformation trims text columns in a dataset while adhering to GDPR/privacy constraints. It ensures that sensitive information is handled appropriately, reducing the risk of data exposure. By trimming text columns, unnecessary personal data can be removed, helping organizations comply with data protection regulations.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when processing customer data for analytics purposes. For instance, when ingesting customer feedback data, the transformation would trim text columns like 'Customer Comments' or 'Feedback' to remove leading/trailing whitespaces and sensitive information, ensuring that only relevant and anonymized data is stored for further analysis.

### Core M Functions Used
`Table.SelectRows`, `Text.Trim`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "  Hello World  "}, 
        {"Jane", "Hi"}
    }, {"Name", "Comments"}),
    TrimmedComments = Table.TransformColumns(Source, {{"Comments", each Text.Trim(_)}})
    in
    TrimmedComments
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The script starts by ingesting data from a source, typically a table or a query.
    2. **Column Selection**: It selects the specific columns that require trimming, often using functions like `Table.SelectRows` or `Table.TransformColumns`.
    3. **Trim Operation**: For each selected column, the `Text.Trim` function is applied to remove leading and trailing whitespaces.
    4. **Data Transformation**: The transformed data, with trimmed text columns, is then outputted for further processing or storage, ensuring compliance with GDPR/privacy constraints.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
