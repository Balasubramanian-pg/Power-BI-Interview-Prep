# Remove Unnecessary Columns with Query Folding Considerations

**Description:**
This transformation removes unnecessary columns from a table while considering query folding. It optimizes data retrieval by minimizing the amount of data transferred and processed. This is particularly useful when working with large datasets or when the data source supports query folding.

### Typical Scenario
In a real-world data pipeline, this transformation is used when connecting to a database that contains a large table with many columns, but only a subset of columns is needed for analysis. For example, when retrieving sales data from a database, you might only need columns for date, product, and sales amount, and can remove other columns like customer details or inventory levels.

### Core M Functions Used
`Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Column1", "Column2", "Column3"}, 
        {"Value1", "Value2", "Value3"}
    }),
    RemovedColumns = Table.SelectColumns(Source, {"Column1", "Column3"})
    in
    RemovedColumns
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The source table is defined with multiple columns.
    2. The Table.SelectColumns function is used to select only the necessary columns.
    3. The resulting table contains only the selected columns, achieving the goal of removing unnecessary columns while considering query folding for optimization.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*