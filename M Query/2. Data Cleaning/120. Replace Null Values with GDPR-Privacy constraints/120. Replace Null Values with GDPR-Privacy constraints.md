# Replace Null Values with GDPR/Privacy Constraints in Power Query

**Description:**
This transformation replaces null values in a dataset with GDPR/privacy constraints to ensure data anonymization and compliance. It is particularly useful when handling sensitive data that must be protected under privacy regulations. By replacing null values with specific constraints, data can be rendered more secure and compliant with standards like GDPR.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with user data that contains null values which could potentially reveal sensitive information. For instance, in a customer database, replacing null values in fields like 'email' or 'phone number' with '[REDACTED]' or '[PRIVATE]' ensures that even if data is anonymized, it still adheres to privacy laws and regulations, preventing any possible identification of individuals.

### Core M Functions Used
`Table.SelectRows`, `Table.ReplaceValue`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, null},
        {"Jane", null, "jane@example.com"},
        {"Bob", 40, null}
    }, {"Name", "Age", "Email"}),
    ReplaceNullsWithPrivacyConstraint = Table.ReplaceValue(Source, null, "[PRIVATE]", Replacer.ReplaceValue, {"Age", "Email"})
    in
    ReplaceNullsWithPrivacyConstraint
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: Start with a table that contains null values in columns where sensitive information is stored.
    2. **Identify Null Values**: Use Power Query's M language to identify null values in specific columns.
    3. **Replace Null Values**: Utilize the `Table.ReplaceValue` function to replace identified null values with a GDPR/privacy constraint, such as '[PRIVATE]'.
    4. **Output**: The resulting table will have null values replaced, ensuring that the data is more secure and compliant with privacy regulations.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*