# Clean Invalid Characters with GDPR/Privacy constraints

**Description:**
This transformation achieves cleaning invalid characters from data while adhering to GDPR/privacy constraints, ensuring data validity and compliance.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when handling user data that contains special or invalid characters which could compromise data integrity or violate privacy laws. For instance, when processing customer information from various sources, it's essential to clean and standardize the data to prevent errors in downstream applications.

### Core M Functions Used
`Text.Replace`, `Text.Clean`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", "johndoe@example.com"},
        {"Jane", "Doe", "janedoe@example.com"}
    }, {"First Name", "Last Name", "Email"}),
    CleanEmail = Table.TransformColumns(Source, {{"Email", each Text.Clean(_)}}),
    ValidEmail = Table.SelectRows(CleanEmail, each Text.Contains(_[Email], "@"))
    in
    ValidEmail
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Ingestion**: The source data is ingested into Power Query.
    2. **Character Cleaning**: The 'Text.Clean' function is applied to the relevant columns (e.g., email addresses) to remove any invalid characters.
    3. **Data Validation**: The 'Table.SelectRows' function is used with a condition to filter out rows that do not meet specific criteria, such as ensuring email addresses contain the '@' symbol.
    4. **Output**: The cleaned and validated data is then outputted for further use in the data pipeline.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*