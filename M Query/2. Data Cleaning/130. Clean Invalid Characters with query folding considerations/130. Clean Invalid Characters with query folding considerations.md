# Cleaning Invalid Characters with Query Folding Considerations

**Description:**
This transformation removes invalid characters from a data source while considering query folding for efficient data processing. It ensures that the data is clean and consistent for further analysis. The process involves identifying and replacing or removing characters that do not meet specific criteria.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when integrating data from multiple sources, some of which may contain invalid or inconsistent characters. For instance, when loading customer data from a legacy system, the data might contain special characters or non-standard text that needs to be cleaned to ensure compatibility with modern data processing tools.

### Core M Functions Used
`Table.SelectRows`, `Text.Replace`, `Text.Clean`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", "johndoe@example.com"},
        {"Jane", "Doe", "janedoe@example.com"}
    }, {"First Name", "Last Name", "Email"}),
    CleanEmail = Table.TransformColumns(Source, {{"Email", each Text.Clean(_, {"!", "@", "#", "$", "%"})}}),
    ValidatedRows = Table.SelectRows(CleanEmail, each Text.Contains(_[Email], "@"))
    in
    ValidatedRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: The process starts with a data source, for example, a table with customer information.
    2. **Identify Invalid Characters**: Determine which characters are considered invalid (e.g., special characters in email addresses).
    3. **Cleaning**: Use M functions like `Text.Clean` or `Text.Replace` to remove or replace these invalid characters.
    4. **Query Folding**: Where possible, apply transformations in a way that allows query folding, which can significantly improve performance by pushing operations back to the source system.
    5. **Validation**: Finally, validate the cleaned data to ensure it meets the required standards (e.g., ensuring email addresses contain '@').

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*