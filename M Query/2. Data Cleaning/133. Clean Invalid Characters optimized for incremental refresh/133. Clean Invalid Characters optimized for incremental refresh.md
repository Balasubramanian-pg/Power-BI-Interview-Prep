# Clean Invalid Characters optimized for incremental refresh

**Description:**
This transformation removes invalid characters from a dataset, ensuring data quality and consistency, particularly useful in incremental refresh scenarios where data is constantly updated.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with user-generated content or data aggregated from multiple sources, where invalid characters can cause errors in downstream processes or analytics.

### Core M Functions Used
`Text.Replace`, `Text.Clean`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Hello World!", "2022-01-01"},
        {"Hello# World!", "2022-01-02"},
        {"Hello World!", "2022-01-03"}
    }, {"Text", "Date"}),
    CleanText = Table.TransformColumns(Source, {{"Text", each Text.Clean(_)}}),
    ReplaceInvalidChars = Table.TransformColumns(CleanText, {{"Text", each Text.Replace(_, "#", "")}})
    in
    ReplaceInvalidChars
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. **Data Ingestion**: The script starts by ingesting data from a source, which could be a database, file, or another data source.
2. **Text Cleaning**: It then uses the `Text.Clean` function to remove any invalid characters from the text columns. This function removes non-ASCII characters and control characters.
3. **Specific Character Replacement**: For characters that are known to be problematic but not removed by `Text.Clean`, it uses `Text.Replace` to specifically target and replace these characters.
4. **Data Transformation**: The cleaned data is then transformed into a usable format, which could involve converting data types, filtering, or aggregating data.
5. **Incremental Refresh**: The transformation is optimized for incremental refresh by only processing new or changed data, making it efficient for large datasets that are constantly updated.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*