# Remove Unnecessary Columns as a Reusable Function

**Description:**
This transformation removes unwanted columns from a table, optimizing data structure for better performance and focused analysis. It helps in streamlining data by eliminating unnecessary information, making it easier to work with.

### Typical Scenario
In a real data pipeline, this transformation is used when dealing with large datasets where not all columns are relevant for analysis or reporting. For instance, when extracting data from a database, there might be technical or audit columns that are not needed for business insights.

### Core M Functions Used
`Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Column1", "Column2", "Column3"},
        {"Value1", "Value2", "Value3"}
    }, {"Column1", "Column2", "Column3"}),
    RemovedColumns = Table.SelectColumns(Source, {"Column1", "Column2"})
    in
    RemovedColumns
```

### Example Execution Logic Step-By-Step
The transformation works by first defining a source table. Then, it uses the Table.SelectColumns function to specify which columns to keep. In this example, 'Column1' and 'Column2' are selected, effectively removing 'Column3' from the table. This step can be wrapped in a reusable function to easily apply it across different tables or data sources.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*