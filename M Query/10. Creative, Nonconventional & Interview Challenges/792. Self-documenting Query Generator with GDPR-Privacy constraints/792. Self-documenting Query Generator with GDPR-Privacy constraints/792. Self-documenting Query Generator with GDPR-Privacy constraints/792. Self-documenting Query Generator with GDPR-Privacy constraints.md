# Self-documenting Query Generator with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used for generating self-documenting queries while adhering to GDPR/privacy constraints. It allows for the creation of transparent and compliant data pipelines.

### Typical Scenario
In a real data pipeline, this transformation would be used when extracting personal data for analytics purposes while ensuring that all operations are GDPR compliant. For instance, a company might use this to generate queries that pseudonymize personal identifiable information (PII) during data extraction.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformColumns`, `List.Distinct`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", 25},
        {"Jane", "Doe", 30},
        {"Sam", "Smith", 35}
    }, {"FirstName", "LastName", "Age"}),
    PseudonymizedData = Table.TransformColumns(Source, {{"FirstName", each "Pseudonym" & _}, {"LastName", each "Pseudonym" & _}}),
    FilteredData = Table.SelectRows(PseudonymizedData, each [Age] > 30)
    in
    FilteredData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: Start with a table containing personal data.
    2. **Pseudonymization**: Use `Table.TransformColumns` to pseudonymize sensitive columns (e.g., first and last names).
    3. **Filtering**: Apply filters using `Table.SelectRows` to select only required rows based on conditions (e.g., age > 30).
    4. **Documentation**: The M code itself serves as documentation, showing each step of data processing.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*