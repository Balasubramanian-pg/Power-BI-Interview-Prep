# Generate Art Patterns From Numbers with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to generate art patterns from numbers while adhering to GDPR/privacy constraints. It showcases a creative application of data transformation while ensuring sensitive information is protected.

### Typical Scenario
In a real-world data pipeline, this transformation could be used in a marketing analytics scenario where personal data needs to be anonymized and used to create visually appealing patterns for campaign analysis, ensuring that no individual data points can be identified.

### Core M Functions Used
`Table.SelectRows`, `List.Transform`, `Number.Mod`

### Code Source Execution
```powerquery
let
    // Sample data
    data = #
        "1" = 10,
        "2" = 20,
        "3" = 30,
        "4" = 40,
        "5" = 50
    ,
    // Convert data to table
    dataTable = Table.FromList(data, Splitter.SplitByNothing(), {"Values"}),
    // Apply transformation to generate art pattern
    artPattern = Table.TransformColumns(dataTable, {{"Values", each Number.Mod(_, 10)"}}),
    // Anonymize data (example: bucketing)
    anonymizedData = Table.TransformColumns(artPattern, {{"Values", each if _ < 5 then "Low" else "High"}})
    // Final result
    in
    anonymizedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Start with a sample data table containing numeric values.
    2. Convert the data into a table format that Power Query can process.
    3. Apply a transformation to the numeric values to generate an art pattern (e.g., using Number.Mod to create a cyclic effect).
    4. Implement GDPR/privacy constraints by anonymizing the data, for example, by bucketing the values into ranges.
    5. The final output is a table that represents an art pattern derived from the original numbers, while ensuring the data remains anonymous and protected.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*