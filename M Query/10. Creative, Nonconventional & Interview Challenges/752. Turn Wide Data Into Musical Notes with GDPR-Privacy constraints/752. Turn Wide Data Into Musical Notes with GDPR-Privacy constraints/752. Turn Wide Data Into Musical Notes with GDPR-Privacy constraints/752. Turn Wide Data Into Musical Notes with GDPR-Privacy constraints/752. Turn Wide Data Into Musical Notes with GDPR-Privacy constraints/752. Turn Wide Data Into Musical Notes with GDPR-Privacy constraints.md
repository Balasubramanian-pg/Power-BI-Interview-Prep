# Transforming Wide Data into Musical Notes under GDPR/Privacy Constraints

**Description:**
This transformation takes wide data and converts it into musical notes while adhering to GDPR and privacy constraints. It ensures that sensitive information is protected during the conversion process.

### Typical Scenario
In a music streaming service, user listening habits are collected and stored in a wide data format. To protect user privacy and comply with GDPR, the service wants to convert this data into musical notes for analysis without exposing individual user data.

### Core M Functions Used
`Table.SelectRows`, `List.Transform`, `Text.Upper`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"User1", 10, 20, 30},
        {"User2", 40, 50, 60},
        {"User3", 70, 80, 90}
    }, {"User", "Note1", "Note2", "Note3"}),
    // Select rows where User column does not contain sensitive information
    FilteredRows = Table.SelectRows(Source, each Text.StartsWith([User], "User")),
    // Transform note columns into musical notes
    MusicalNotes = List.Transform({"Note1", "Note2", "Note3"}, each "C" & Text.Upper(_)),
    // Add musical notes to the table
    Result = Table.TransformColumns(FilteredRows, MusicalNotes)
    in
    Result
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Ingestion**: The wide data is ingested into Power Query.
2. **Filtering**: The Table.SelectRows function is used to filter out rows that may contain sensitive information.
3. **Transformation**: The List.Transform function is used to convert the note columns into musical notes.
4. **Addition of Musical Notes**: The transformed musical notes are added to the filtered table.
5. **Output**: The final table with musical notes, adhering to GDPR and privacy constraints, is outputted.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*