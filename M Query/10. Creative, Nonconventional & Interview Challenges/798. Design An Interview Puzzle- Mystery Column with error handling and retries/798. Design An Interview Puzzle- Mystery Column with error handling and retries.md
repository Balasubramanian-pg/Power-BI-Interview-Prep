# Design An Interview Puzzle: Mystery Column with Error Handling and Retries

**Description:**
This transformation is used to create a puzzle for an interview process. It involves designing a mystery column with error handling and retries to assess problem-solving skills.

### Typical Scenario
In a real-world data pipeline, this transformation can be used to create a data quality check that requires the data analyst to identify and fix errors in a dataset.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"A", 1, "a"},
        {"B", 2, "b"},
        {"C", 3, "c"}
    }, {"Column1", "Column2", "Column3"}),
    AddMysteryColumn = Table.AddColumn(Source, "MysteryColumn", each try [Column2] + [Column3] catch (e) => "Error"),
    RetryMysteryColumn = Table.TransformColumns(AddMysteryColumn, {{"MysteryColumn", each try _ catch (e) => "Retry"}})
    in
    RetryMysteryColumn
```

### Example Execution Logic Step-By-Step
The script works as follows:
1. A sample table is created with three columns.
2. A new column called 'MysteryColumn' is added, which attempts to add 'Column2' and 'Column3'.
3. If an error occurs during the addition (e.g., due to data type mismatch), the 'try-catch' block catches the error and returns 'Error'.
4. The 'RetryMysteryColumn' step retries the 'MysteryColumn' calculation and returns 'Retry' if an error still occurs.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*