# Create A Procedural Fake Dataset as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to create a procedural fake dataset as a reusable function. It allows for the generation of sample data for testing, development, or demonstration purposes. The transformation enables users to create a dataset with specific characteristics, such as data types and structures, that can be used to mimic real-world data.

### Typical Scenario
In a real data pipeline, this transformation would be used when a team needs to test a data processing workflow or develop a new data model without having access to the actual data. For instance, a data engineer might use this transformation to create a fake dataset that mimics the structure and content of a customer database, allowing them to test data ingestion, processing, and analytics workflows.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Define a function to generate a fake dataset
    GenerateFakeDataset = () as table =>
    let
        // Define the columns and data types for the fake dataset
        Columns = {"Name", "Age", "City"},
        Data = {
            {"John", 25, "New York"},
            {"Jane", 30, "London"},
            {"Bob", 35, "Paris"}
        }
    in
        Table.FromList(Data, Splitter.SplitByNothing(), Columns),

    // Call the function to generate the fake dataset
    FakeDataset = GenerateFakeDataset()
    
    // Return the fake dataset
    in
    FakeDataset
```

### Example Execution Logic Step-By-Step
Here's a step-by-step explanation of how the script works:

1. The script defines a function called `GenerateFakeDataset` that returns a table.
2. Inside the function, it defines the columns and data types for the fake dataset.
3. It then defines the data for the fake dataset, which is a list of lists, where each inner list represents a row in the dataset.
4. The `Table.FromList` function is used to create a table from the data, using the `Splitter.SplitByNothing()` function to specify that the data should be split into separate columns.
5. The function is then called to generate the fake dataset, which is returned as a table.

This script can be used as a starting point for creating more complex fake datasets, and can be modified to suit specific needs.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*