# Create A Procedural Fake Dataset for large datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to generate a large dataset with fake data for testing or demonstration purposes. It allows users to create a dataset with a similar structure and data distribution to real data, but with fictional information. This can be particularly useful for testing data pipelines, demonstrating data analysis techniques, or creating sample datasets for training or testing.

### Typical Scenario
In a real-world scenario, a data engineer might need to create a large dataset with fake data to test a data pipeline or demonstrate a data analysis technique. For example, a company might want to test its data processing pipeline with a large dataset of customer information, but without using real customer data. In this case, a procedural fake dataset can be created using Power Query (M) to generate a large dataset with fictional customer information.

### Core M Functions Used
`List.Generate`, `Table.FromList`, `Text.FromText`

### Code Source Execution
```powerquery
let
    // Define the number of rows to generate
    rowsToGenerate = 10000,

    // Define the columns to generate
    columns = {"Name", "Age", "City"},

    // Generate the data
    data = List.Generate(
        () => {"John", 25, "New York"},
        each _,
        each {
            Text.FromText("Name " & Number.ToText(_[index])),
            Int32.FromText("Age " & Number.ToText(_[age] + 1)),
            Text.FromText("City " & Number.ToText(_[city] + 1))
        },
        [Count = rowsToGenerate]
    ),

    // Convert the data to a table
    table = Table.FromList(data, Splitter.SplitByNothing(), columns)

in
    table
```

### Example Execution Logic Step-By-Step
The script works as follows:
1. The number of rows to generate is defined.
2. The columns to generate are defined.
3. The data is generated using List.Generate, which creates a list of lists, where each sublist represents a row of data.
4. The data is then converted to a table using Table.FromList.
5. The table is then returned as the result.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*