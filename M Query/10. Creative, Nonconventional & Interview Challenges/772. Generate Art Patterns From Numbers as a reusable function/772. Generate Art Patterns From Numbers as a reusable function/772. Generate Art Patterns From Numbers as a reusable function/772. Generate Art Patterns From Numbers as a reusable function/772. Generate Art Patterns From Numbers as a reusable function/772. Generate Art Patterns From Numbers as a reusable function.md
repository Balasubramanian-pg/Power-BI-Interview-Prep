# Generate Art Patterns From Numbers as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to generate art patterns from numbers as a reusable function. It allows users to create visually appealing patterns using numerical data. The transformation achieves this by leveraging specific M functions to manipulate and transform the data into a pattern.

### Typical Scenario
In a real data pipeline, this transformation can be used in a data visualization workflow where users want to create artistic representations of numerical data, such as generating QR codes or other graphical patterns from data.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
No M code example provided in the source.
```

### Example Execution Logic Step-By-Step
The transformation typically works by first taking a table of numbers as input. It then applies a series of M functions, such as Table.SelectRows, to manipulate the data into the desired pattern. For example, users can select specific rows or columns, apply mathematical operations, or use conditional logic to create the pattern. The resulting table can then be visualized as an artistic pattern.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*