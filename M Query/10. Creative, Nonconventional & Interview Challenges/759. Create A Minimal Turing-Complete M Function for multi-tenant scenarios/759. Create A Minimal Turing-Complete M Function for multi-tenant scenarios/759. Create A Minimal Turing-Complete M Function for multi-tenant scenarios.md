# Create A Minimal Turing-Complete M Function for multi-tenant scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to create a minimal Turing-complete M function for multi-tenant scenarios. It enables flexible data transformations and computations within Power Query pipelines.

### Typical Scenario
In a real data pipeline, this transformation would be used when handling data from multiple tenants, each requiring custom data processing and transformations. For instance, a data pipeline serving multiple clients, each with unique data schemas and transformation requirements, can utilize a Turing-complete M function to dynamically adapt to these needs.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Example M code for a minimal Turing-complete function
    Factorial = (n) =>
        if n = 0 then 1
        else n * Factorial(n - 1),
    // Usage example
    result = Factorial(5)
in
    result
```

### Example Execution Logic Step-By-Step
The transformation works by defining a recursive function, such as a factorial function, which is a common example of a Turing-complete function. This function takes an input number 'n' and returns its factorial. The function uses recursion to achieve this: if 'n' is 0, it returns 1; otherwise, it multiplies 'n' by the factorial of 'n-1'. This process repeats until 'n' equals 0, demonstrating a basic form of computation within Power Query (M).

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*