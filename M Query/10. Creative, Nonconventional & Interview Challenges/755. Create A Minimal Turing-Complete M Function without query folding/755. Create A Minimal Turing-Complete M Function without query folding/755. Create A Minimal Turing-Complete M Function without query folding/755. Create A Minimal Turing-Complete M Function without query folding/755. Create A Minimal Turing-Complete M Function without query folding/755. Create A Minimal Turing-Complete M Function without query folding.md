# Create A Minimal Turing-Complete M Function without query folding

**Description:**
This use case demonstrates how Power Query (M) can be used to create a minimal Turing-complete M function without query folding. It showcases the capability to perform computations that are theoretically capable of solving any problem that can be solved by a Turing machine, within the constraints of Power Query's execution model.

### Typical Scenario
In a real data pipeline, this transformation could be used when you need to perform complex data manipulations that cannot be achieved through standard Power Query functions. For instance, you might need to implement a custom algorithm for data processing that involves recursive operations or conditional logic that depends on the state of the data being processed.

### Core M Functions Used
`Recursive functions`, `Lambda functions`

### Code Source Execution
```powerquery
let
    // Define a minimal Turing-complete M function
    Factorial = (n) =>
        if n = 0 then 1 else n * @Factorial(n - 1),
    // Example usage
    result = @Factorial(5)
    in
        result
```

### Example Execution Logic Step-By-Step
The transformation works by defining a recursive function, `Factorial`, which takes an integer `n` as input. If `n` equals 0, the function returns 1; otherwise, it calls itself with the argument `n-1` and multiplies the result by `n`. This process continues until `n` reaches 0, at which point the recursion stops, and the results are propagated back up the call stack. The example usage demonstrates how to call this function with the argument 5 to compute the factorial of 5.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*