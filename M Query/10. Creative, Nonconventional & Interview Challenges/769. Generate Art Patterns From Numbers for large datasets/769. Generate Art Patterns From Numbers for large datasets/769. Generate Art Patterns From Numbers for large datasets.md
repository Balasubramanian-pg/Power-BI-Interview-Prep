# Generate Art Patterns From Numbers for Large Datasets

**Description:**
This transformation uses Power Query (M) to generate art patterns from numbers, making it suitable for large datasets. It helps in creating visually appealing patterns that can be used for various purposes such as data visualization, art, or design. The transformation takes a list of numbers as input and produces a graphical pattern as output.

### Typical Scenario
In a real-world data pipeline, this transformation can be used in a data visualization project where a company wants to create unique and creative visualizations for their customers. For instance, a marketing team can use this transformation to generate art patterns from customer data, such as sales numbers or website traffic, to create engaging and interactive dashboards.

### Core M Functions Used
`Table.SelectRows`, `List.Transform`, `List.Zip`

### Code Source Execution
```powerquery
let
    // Sample list of numbers
    numbers = {1, 2, 3, 4, 5},
    
    // Generate art pattern
    artPattern = List.Transform(numbers, each Number.ToText(_, "0")){
        1 = "*",
        2 = "**",
        3 = "***",
        4 = "****",
        5 = "****"
    },
    
    // Convert to table
    artPatternTable = Table.FromList(artPattern, Splitter.SplitByNothing(), {"Art Pattern"})
in
    artPatternTable
```

### Example Execution Logic Step-By-Step
Here's a step-by-step explanation of how the script works:
    1. The script starts by defining a sample list of numbers.
    2. It then uses the `List.Transform` function to generate an art pattern from the numbers. In this example, each number is converted to a string and then replaced with a corresponding pattern of asterisks (*).
    3. The `List.Transform` function applies a transformation to each element of the input list.
    4. The resulting art pattern is then converted to a table using the `Table.FromList` function.
    5. Finally, the script returns the art pattern table.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*