# Create A Minimal Turing-Complete M Function with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to create a minimal Turing-complete M function while adhering to GDPR/privacy constraints. It showcases the ability to transform data in a way that respects privacy regulations.

### Typical Scenario
In a real data pipeline, this transformation would be used when handling sensitive data that requires anonymization or pseudonymization to comply with GDPR. For instance, in a customer data processing pipeline, personal identifiable information (PII) needs to be protected.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Example M code for creating a minimal Turing-complete function
    // This function takes a table and a condition as input
    MinimalTuringFunction = (table as table, condition as function) =>
    Table.SelectRows(table, condition),
    // Usage example
    data = Table.FromList({{"John", 25}, {"Jane", 30}}, {"Name", "Age"}),
    result = MinimalTuringFunction(data, each [Age] > 25)
    in
    result
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. It defines a minimal Turing-complete M function, `MinimalTuringFunction`, which takes a table and a condition as input.
2. Inside this function, it uses `Table.SelectRows` to filter the input table based on the provided condition.
3. The condition is a function that specifies the filtering criteria.
4. The example usage demonstrates how to apply this function to a sample table, filtering rows where the 'Age' is greater than 25.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*