# Create A Minimal Turing-Complete M Function for large datasets

**Description:**
This use case demonstrates how Power Query (M) can be used to create a minimal Turing-complete M function for handling large datasets. It showcases the capability of M functions in data transformation and processing. The goal is to provide a basic yet powerful function that can be used in various data pipeline scenarios.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with large datasets that require complex data processing and transformation. For instance, when extracting data from multiple sources, performing data cleansing, and then loading the transformed data into a target system, a minimal Turing-complete M function can be utilized to handle the data processing tasks efficiently.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Define a minimal Turing-complete M function
    TuringCompleteFunction = (inputTable as table) =>
    let
        // Simple transformation example: select rows where column "Value" is greater than 10
        transformedTable = Table.SelectRows(inputTable, each [Value] > 10)
    in
        transformedTable,

    // Example usage
    inputTable = Table.FromList({{1, 10}, {2, 20}, {3, 5}}, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    result = TuringCompleteFunction(inputTable)
    in
        result
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Define the M Function**: A minimal Turing-complete M function, `TuringCompleteFunction`, is defined. This function takes an input table and applies a transformation to it.
    2. **Apply Transformation**: Inside the function, `Table.SelectRows` is used to select rows from the input table where the value in the "Value" column is greater than 10.
    3. **Example Usage**: An example input table is created with sample data.
    4. **Execute the Function**: The `TuringCompleteFunction` is applied to the example input table, and the result is returned.
    This demonstrates how a basic M function can be used for data transformation, showcasing its potential for more complex processing tasks.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*