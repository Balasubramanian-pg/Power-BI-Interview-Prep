# Design An Interview Puzzle: Mystery Column optimized for incremental refresh

**Description:**
This transformation is used to optimize the Mystery Column for incremental refresh in Power Query. It allows for efficient data processing and refresh by leveraging Power Query's M language. The transformation enables the creation of a mystery column that can be incrementally refreshed, making it suitable for large datasets.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with large datasets that require frequent updates. For instance, in a sales data pipeline, the Mystery Column might be used to track sales performance over time. As new sales data becomes available, the incremental refresh capability ensures that the Mystery Column is updated efficiently, without requiring a full data reload.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"David", 42, "UK"}
    }, {"Name", "Age", "Country"}),
    SelectRows = Table.SelectRows(Source, each "Age" > 30)
    in
    SelectRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: The process begins with a data source, such as a table or query, that contains the necessary data.
    2. **M Function Application**: The `Table.SelectRows` M function is applied to the data source. This function filters the rows based on a condition, in this case, `"Age" > 30`.
    3. **Filtering**: The function filters the rows, only including those where the age is greater than 30.
    4. **Result**: The resulting table contains only the rows that meet the specified condition, effectively creating the Mystery Column optimized for incremental refresh.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*