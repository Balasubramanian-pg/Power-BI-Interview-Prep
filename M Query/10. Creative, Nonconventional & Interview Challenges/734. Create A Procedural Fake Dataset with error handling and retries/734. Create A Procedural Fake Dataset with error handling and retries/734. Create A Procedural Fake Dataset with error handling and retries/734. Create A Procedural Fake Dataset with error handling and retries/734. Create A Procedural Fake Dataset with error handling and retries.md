# Create A Procedural Fake Dataset with error handling and retries

**Description:**
This transformation achieves creating a procedural fake dataset with robust error handling and retry mechanisms. It allows for the generation of sample data while ensuring data integrity through error handling. This is particularly useful in data testing and development environments.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when developing or testing ETL processes. For instance, when building a new data mart, a team might need a large volume of fake data to test data transformations, validate data quality, and ensure that the ETL process can handle various data scenarios without breaking.

### Core M Functions Used
`Table.SelectRows`, `try`, `catch`, `Number.RandomInteger`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows(List.Repeat({try
        Number.RandomInteger(1, 100),
        Text.Random("abc")
    catch [Error] => {null, null}] , 100)),
    Retry = (attempt as any) => try attempt catch [Error] => @Retry(attempt),
    Data = Table.SelectRows(Source, each not ( _[Column1] = null and _[Column2] = null ))
    in
    Data
```

### Example Execution Logic Step-By-Step
The script works as follows:
1. **Generate Fake Data**: It starts by generating a table with 100 rows of fake data. Each row contains two columns: a random integer between 1 and 100, and a random text string.
2. **Error Handling**: The data generation is wrapped in a try-catch block. If an error occurs during data generation (for example, if the random number or text generation fails), the catch block returns {null, null}.
3. **Retry Mechanism**: A retry function is defined to attempt data generation again if an error occurs.
4. **Data Selection**: Finally, it selects rows from the generated table where at least one of the columns is not null, effectively filtering out any rows that had errors during generation.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*