# Create A Procedural Fake Dataset with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to create a procedural fake dataset while adhering to GDPR/privacy constraints. It ensures that sensitive information is protected and only anonymized or fictional data is used for testing and development purposes.

### Typical Scenario
In a real data pipeline, this transformation would be used when developing or testing data models, ETL processes, or machine learning algorithms. For instance, a data engineer might need to create a large dataset that mimics the structure and distribution of real customer data without exposing actual sensitive information.

### Core M Functions Used
`Table.SelectRows`, `List.Generate`, `Text.Random`

### Code Source Execution
```powerquery
let
    // Define a function to generate random names
    GenerateName = () => Text.Random(10, {"A".."Z"}),
    // Define a function to generate random emails
    GenerateEmail = () => Text.Combine({GenerateName(), "@example.com"}),
    // Generate a table with fake data
    FakeData = Table.FromList(
        List.Generate(
            () => [Name = GenerateName(), Email = GenerateEmail()],
            each _,
            each [Name = GenerateName(), Email = GenerateEmail()],
            100
        ),
        {"Name", "Email"}
    )
in
    FakeData
```

### Example Execution Logic Step-By-Step
This transformation typically works by defining functions to generate random or fictional data for specific fields, such as names or emails. It then uses these functions within a loop or list generation to create a table with the desired number of rows. Each row contains fake data that adheres to GDPR/privacy constraints, ensuring that no real personal data is used.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*