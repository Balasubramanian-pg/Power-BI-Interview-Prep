# Generate Art Patterns From Numbers for Multi-Tenant Scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to generate art patterns from numbers, particularly in multi-tenant scenarios where data needs to be visually represented in a unique and scalable way.

### Typical Scenario
In a real data pipeline, this transformation would be used when there is a need to create visually distinct reports or dashboards for different clients or tenants, using numerical data to generate unique art patterns.

### Core M Functions Used
`Table.SelectRows`, `List.Generate`, `Number.Mod`

### Code Source Execution
```powerquery
let
    // Sample table with numbers
    Numbers = Table.FromList({1, 2, 3, 4, 5}, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    // Generate art pattern
    ArtPattern = List.Generate(
        () => 1,
        each _ < 10,
        each _ + 1
    ),
    // Convert to table
    ArtPatternTable = Table.FromList(ArtPattern, Splitter.SplitByNothing(), null, null, ExtraValues.Error)
    // Apply transformation
    in
    ArtPatternTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Start with a table of numbers.
    2. Use List.Generate to create a list of numbers that will form the basis of the art pattern.
    3. Convert this list into a table.
    4. Apply a transformation to this table to generate the art pattern, potentially using functions like Number.Mod to create a repeating or cyclical pattern.
    5. The resulting table can then be used in a report or dashboard to provide a unique visual representation of the data for each tenant.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*