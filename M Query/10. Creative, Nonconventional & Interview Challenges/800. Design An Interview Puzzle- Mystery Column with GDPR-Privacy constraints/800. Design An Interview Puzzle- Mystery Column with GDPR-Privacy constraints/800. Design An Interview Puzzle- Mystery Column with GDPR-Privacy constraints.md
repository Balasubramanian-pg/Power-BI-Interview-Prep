# Design An Interview Puzzle: Mystery Column with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used for designing an interview puzzle while adhering to GDPR/Privacy constraints. It involves creating a transformation that can handle sensitive data in a secure manner.

### Typical Scenario
In a real data pipeline, this transformation would be used when dealing with sensitive information that needs to be anonymized or pseudonymized to comply with GDPR or other privacy regulations. For instance, in a data warehousing scenario where personal identifiable information (PII) needs to be protected.

### Core M Functions Used
`Table.SelectRows`, `Table.TransformColumns`, `List.Distinct`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", "johndoe@example.com"},
        {"Jane", "Doe", "janedoe@example.com"}
    }, {"FirstName", "LastName", "Email"}),
    AnonymizedEmail = Table.TransformColumns(Source, {{"Email", each "XXXXXX"}}),
    MaskedData = Table.SelectRows(AnonymizedEmail, each true)
    in
    MaskedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Source data is ingested into a table.
    2. The 'Email' column is transformed to mask sensitive information, replacing actual emails with 'XXXXXX'.
    3. The Table.SelectRows function is then used to filter or select rows based on a condition (in this case, selecting all rows).
    This approach ensures that sensitive data like emails are anonymized, making it safer to use in interview puzzles or other scenarios where data privacy is a concern.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*