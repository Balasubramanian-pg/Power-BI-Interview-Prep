# Design An Interview Puzzle: Mystery Column with Query Folding Considerations

**Description:**
This use case demonstrates how Power Query (M) can be used to solve an interview puzzle involving a mystery column, taking into account query folding considerations. It showcases the transformation of data to reveal a hidden or mystery column based on specific conditions or logic. This is particularly useful in data analysis and puzzle-solving scenarios where not all data is visible or directly accessible.

### Typical Scenario
In a real-world data pipeline, this transformation could be used in a data analysis task where a column of data is encrypted or hidden. For instance, a company might have customer data where one column represents encrypted customer satisfaction scores. Analysts could use this transformation to decode the mystery column based on given conditions, thereby gaining insights into customer satisfaction.

### Core M Functions Used
`Table.SelectRows`, `Table.AddColumn`, `List.Transform`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, 1},
        {"Mary", 31, 2},
        {"David", 42, 3}
    }, {"Name", "Age", "Score"}),
    // Assume the mystery column is a transformation of the 'Score' column
    MysteryColumn = Table.AddColumn(Source, "Mystery", each [Score] * 2)
    in
    MysteryColumn
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Data Source**: Start with a table that contains a hidden or mystery column. In this example, we assume the mystery column is derived from the 'Score' column.
    2. **Transformation Logic**: Apply a transformation to the 'Score' column to derive the mystery column. For instance, we might decide to multiply the score by 2.
    3. **Implementation in M**: Use Power Query M functions like `Table.AddColumn` to add a new column (the mystery column) that applies the transformation logic to each row.
    4. **Query Folding Considerations**: When working with large datasets, consider query folding to optimize performance. This involves pushing transformations back to the source system where possible, to reduce the amount of data that needs to be processed in-memory.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*