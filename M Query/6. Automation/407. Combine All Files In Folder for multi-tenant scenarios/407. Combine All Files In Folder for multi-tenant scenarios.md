# Combine All Files In Folder for Multi-Tenant Scenarios

**Description:**
This transformation combines data from all files within a specified folder, making it particularly useful for multi-tenant scenarios where data is segregated by tenant and stored in separate files.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from a multi-tenant application where each tenant's data is stored in a separate file within a folder. For instance, a SaaS application stores customer data in a folder with each customer's data in a separate file. The data team needs to aggregate this data for analysis or reporting.

### Core M Functions Used
`Folder.Files`, `Table.Combine`

### Code Source Execution
```powerquery
let
    Source = Folder.Files("C:\Path\To\Folder"),
    CombinedData = Table.Combine(Source, "Data"),
    // Assuming the data is in a column named 'Data'
    // Adjust column name as necessary
    RenamedColumns = Table.RenameColumns(CombinedData, {{"Name", "FileName"}})
    // Rename columns as necessary
in
    RenamedColumns
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Folder.Files**: This function is used to get a list of files in a specified folder. It returns a table with information about each file, including its path and name.
2. **Table.Combine**: This function is then used to combine the data from each file into a single table. It assumes that the data to be combined is in a column named 'Data' in each file's table representation.
3. **Renaming Columns**: Optionally, columns can be renamed for clarity or to match a required schema. For example, renaming the 'Name' column to 'FileName' to better reflect its content.
4. **Adjustments**: Depending on the exact structure of the files and the desired output, additional steps such as filtering, expanding, or transforming specific columns may be necessary.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
