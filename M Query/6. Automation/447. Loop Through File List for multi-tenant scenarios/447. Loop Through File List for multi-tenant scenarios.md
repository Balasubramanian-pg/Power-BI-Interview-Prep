# Loop Through File List for Multi-Tenant Scenarios

**Description:**
This use case demonstrates how Power Query (M) can be used to loop through a list of files in a multi-tenant environment, enabling data processing and transformation for multiple tenants.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with multiple clients or tenants who have their own set of files for data processing. For instance, a business that provides data analytics services to various clients might need to process client-specific files stored in a shared or separate location.

### Core M Functions Used
`Table.SelectRows`, `List.Files`, `List.Transform`

### Code Source Execution
```powerquery
let
    // Assume this is your list of files
    fileList = {"file1.csv", "file2.csv"},
    // Define a function to process each file
    processFile = (file as text) =>
        let
            // Read the file
            fileContents = Table.FromRows(List.Files(file){
                [Content]}) in
            fileContents,
    // Apply the function to each file
    result = List.Transform(fileList, processFile) in
    result
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Define a list of files**: Identify and list all the files you need to process.
2. **Create a function to process each file**: This function reads a file and performs any necessary transformations.
3. **Apply the function to each file**: Use List.Transform to apply your function to each file in the list.
4. **Combine results**: Depending on your needs, you might need to combine the results from each file into a single table.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
