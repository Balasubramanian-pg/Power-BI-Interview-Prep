# Build Reusable Function without query folding

**Description:**
This use case demonstrates how Power Query (M) can be used to build a reusable function without query folding, allowing for flexible data transformations.

### Typical Scenario
In a real data pipeline, this transformation would be used when you need to perform complex data manipulation that cannot be achieved through query folding, such as applying custom business logic that requires processing data in a specific way.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
// no snippet mapped
```

### Example Execution Logic Step-By-Step
The transformation typically works by defining a custom function in Power Query (M) that encapsulates the required data transformation logic. This function can then be applied to different data sources or tables as needed, allowing for reusable and flexible data processing.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*