# Combine All Files In Folder with Error Handling and Retries

**Description:**
This transformation combines all files in a specified folder, handling potential errors and allowing for retries. It is useful for data pipelines that aggregate data from multiple files, ensuring robustness and reliability.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when ingesting data from a folder containing multiple files, such as daily or monthly reports. For instance, a data engineer might need to combine sales data from multiple CSV files in a folder, ensuring that the process can handle missing files or read errors.

### Core M Functions Used
`Folder.Files`, `Table.Combine`, `ErrorHandling`

### Code Source Execution
```powerquery
let
    Source = Folder.Files("C:\Path\To\Folder"),
    CombineFiles = Table.Combine( Source, "Content" )
    in
    CombineFiles
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Folder.Files**: This function is used to get a list of files in the specified folder.
2. **Table.Combine**: This function combines the contents of all files listed in the previous step into a single table.
3. **Error Handling**: Implement try-catch blocks to catch and handle errors during file reading and combination, allowing for retries if needed.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
