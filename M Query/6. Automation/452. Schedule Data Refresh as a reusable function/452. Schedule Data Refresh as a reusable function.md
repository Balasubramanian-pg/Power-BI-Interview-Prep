# Schedule Data Refresh as a reusable function

**Description:**
This use case demonstrates how Power Query (M) can be used to schedule data refresh as a reusable function. It allows for the automation of data refresh at regular intervals, ensuring that the data remains up-to-date. This is particularly useful for reports and dashboards that rely on frequently updated data.

### Typical Scenario
In a real data pipeline, this transformation would be used when there is a need to periodically refresh data from a source, such as when data is updated daily or weekly. For instance, a company may want to schedule a daily refresh of sales data to ensure that their reports and dashboards reflect the latest sales figures.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
Not provided in the source.
```

### Example Execution Logic Step-By-Step
The transformation typically works by creating a function that can be reused across different queries. This function would take parameters such as the data source, refresh interval, and any other relevant settings. Inside the Power Query pipeline, this function would be used to refresh the data at the specified intervals, using functions like Table.SelectRows to filter and manipulate the data as needed.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*