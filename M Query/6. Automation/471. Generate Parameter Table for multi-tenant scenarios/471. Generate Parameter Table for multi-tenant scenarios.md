# Generate Parameter Table for Multi-Tenant Scenarios

**Description:**
This transformation generates a parameter table that can be used to handle multi-tenant data scenarios. It allows for the creation of a dynamic table that can be used to filter or parameterize data based on different tenant requirements. This is particularly useful in data pipelines where data needs to be segregated or processed differently for various clients or tenants.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when you have a multi-tenant data source and need to dynamically filter or process data for each tenant. For example, in a SaaS application, you might have data for multiple customers (tenants) and need to generate reports or analytics for each customer separately. The parameter table generated by this transformation can be used to drive dynamic filtering or parameterization of the data.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Assuming a list of tenant IDs
    tenantIds = {"Tenant1", "Tenant2", "Tenant3"},
    // Generate a table with tenant IDs
    parameterTable = Table.FromList(tenantIds, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    // Rename the column to something meaningful
    parameterTable = Table.RenameColumns(parameterTable, {{"Column1", "TenantId"}})
    // Optionally add more columns or processing here
in
    parameterTable
```

### Example Execution Logic Step-By-Step
This transformation works as follows:
1. **Define Tenant IDs**: Start by defining a list of tenant IDs. This could be a static list or dynamically generated from another data source.
2. **Generate Parameter Table**: Use the `Table.FromList` function to create a table from the list of tenant IDs. This will create a simple table with one column.
3. **Rename Column**: Rename the column to something meaningful, such as 'TenantId'.
4. **Optional Processing**: You can optionally add more columns or processing steps to the table as needed. For example, you might add a column for tenant names or other metadata.
5. **Use in Data Pipeline**: The resulting parameter table can then be used in your data pipeline to dynamically filter or parameterize data for each tenant.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*