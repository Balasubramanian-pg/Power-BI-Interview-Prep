# Combine All Files In Folder with Query Folding Considerations

**Description:**
This use case demonstrates how Power Query (M) can be used to combine all files in a folder while considering query folding. It allows for efficient data processing by leveraging the capabilities of the source database or file system. This transformation is useful when dealing with large datasets that are split across multiple files.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from a folder containing multiple CSV or Excel files, each representing a portion of the overall dataset. For instance, a company might store monthly sales data in separate files, and combining these files into a single dataset for analysis would require this transformation.

### Core M Functions Used
`Folder.Files`, `Table.Combine`

### Code Source Execution
```powerquery
let
    Source = Folder.Files("C:\Path\To\Folder"),
    CombinedData = Table.Combine(Source, "Data"),
    // Assuming each file has a 'Data' table
    Result = CombinedData
in
    Result
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. The `Folder.Files` function is used to get a list of files in the specified folder.
2. The `Table.Combine` function is then used to combine the data from each file into a single table.
3. The combined table is then returned as the result.

This approach allows for efficient data processing by leveraging the capabilities of the source database or file system, and it handles large datasets by processing them in chunks.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*