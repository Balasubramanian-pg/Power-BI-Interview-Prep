# Build Reusable Function with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to build reusable functions while adhering to GDPR/privacy constraints. It enables data engineers to create flexible and compliant data transformations.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when there is a need to anonymize or pseudonymize personal data for analytics or reporting purposes while ensuring compliance with GDPR and other privacy regulations.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
// no snippet mapped
```

### Example Execution Logic Step-By-Step
The transformation typically works by first defining a reusable function that takes in a table and applies GDPR/privacy constraints, such as filtering out sensitive information or anonymizing personal data. This function can then be applied to various tables within the Power Query pipeline. For instance, the Table.SelectRows function can be used to select specific rows that meet certain conditions, effectively reducing the data to only what is necessary for analysis while maintaining privacy.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
