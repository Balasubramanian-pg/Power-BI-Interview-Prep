# Loop Through File List with GDPR/Privacy Constraints in Power Query

**Description:**
This transformation enables looping through a list of files while applying GDPR/privacy constraints, ensuring sensitive data is handled appropriately. It allows for selective processing of files based on specific conditions, such as data protection regulations. This is particularly useful in data pipelines where privacy and compliance are critical.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from multiple files that contain sensitive information. For instance, a company may need to process employee data from various HR files while ensuring that only authorized personnel have access to certain files and that the data is processed in compliance with GDPR. The transformation would help in dynamically selecting files for processing based on predefined criteria, such as file names, dates, or specific data attributes.

### Core M Functions Used
`Table.SelectRows`, `Folder.Files`, `List.SelectMany`

### Code Source Execution
```powerquery
let
    // Source folder path
    sourceFolder = "C:\SourceFiles",

    // Get list of files
    filesList = Folder.Files(sourceFolder),

    // Define GDPR/privacy constraints
    constrainedFiles = Table.SelectRows(filesList, each [Name] = "AllowedFile.csv"),

    // Process files
    processedData = Table.Combine(List.SelectMany(constrainedFiles, each Table.FromRows({_, "Processed"}))),

    // Final output
    result = processedData
in
    result
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Source Folder Path**: Define the path to the folder containing the files to process.
2. **Get List of Files**: Use `Folder.Files` to get a list of files in the specified folder.
3. **Apply GDPR/Privacy Constraints**: Use `Table.SelectRows` to filter the files based on GDPR/privacy constraints, such as selecting only files with specific names or attributes.
4. **Process Files**: Loop through the filtered files, process them as needed (e.g., load into a table), and combine the results.
5. **Final Output**: Output the processed data, ensuring that only authorized files have been processed and that the data is handled in compliance with GDPR.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*