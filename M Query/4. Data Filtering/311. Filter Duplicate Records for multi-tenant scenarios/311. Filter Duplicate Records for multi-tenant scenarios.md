# Filter Duplicate Records for Multi-Tenant Scenarios

**Description:**
This transformation removes duplicate records from a dataset in multi-tenant scenarios, ensuring data integrity and accuracy. It helps in identifying and eliminating redundant data entries based on specific columns or criteria.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when integrating data from multiple sources for business intelligence or analytics. For instance, when combining customer data from different subsidiaries, duplicates may arise due to variations in data entry or system discrepancies. This transformation ensures that each customer record is unique, enhancing data quality and reliability for analysis.

### Core M Functions Used
`Table.Distinct`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"TenantID", "RecordID", "Data"},
        {"T1", "R1", "Sample"},
        {"T1", "R1", "Sample"},
        {"T2", "R2", "Another Sample"}
    }),
    RemovedDuplicates = Table.Distinct(Source, {"TenantID", "RecordID"})
    in
    RemovedDuplicates
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Identify Duplicate Criteria**: Determine which columns define a duplicate record (e.g., TenantID and RecordID).
    2. **Apply Table.Distinct Function**: Use the Table.Distinct function in Power Query (M) to remove duplicate rows based on the specified columns.
    3. **Review and Validate**: Ensure that the resulting table contains unique records based on the defined criteria, thereby filtering out duplicates effectively.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
