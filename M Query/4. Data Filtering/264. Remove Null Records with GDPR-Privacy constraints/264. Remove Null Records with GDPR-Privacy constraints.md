# Remove Null Records with GDPR/Privacy constraints

**Description:**
This transformation removes records with null values while adhering to GDPR/privacy constraints, ensuring sensitive data is handled appropriately. It is crucial for data anonymization and compliance with privacy regulations. By filtering out null records, it helps in maintaining data quality and integrity.

### Typical Scenario
In a real-world data pipeline, this transformation is used when processing user data that must comply with GDPR or similar privacy laws. For instance, when aggregating user information from various sources, null values might represent missing personal data. Removing these records ensures that only complete and accurate data is processed further, reducing the risk of data breaches and non-compliance.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "john@example.com"},
        {"Jane", null, "jane@example.com"},
        {null, 30, "bob@example.com"}
    }, {"Name", "Age", "Email"}),
    RemovedNulls = Table.SelectRows(Source, each [Name] <> null and [Age] <> null and [Email] <> null)
    in
    RemovedNulls
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Source**: Start with a table that contains user data, including names, ages, and email addresses. Some records may have null values.
2. **Filtering Null Records**: Use the `Table.SelectRows` function in Power Query (M) to filter out rows where any of the required fields (Name, Age, Email) are null.
3. **Result**: The resulting table will only include records where all necessary information is present, ensuring that no sensitive or null data is processed further.
This step-by-step process ensures data quality and compliance with privacy regulations.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*