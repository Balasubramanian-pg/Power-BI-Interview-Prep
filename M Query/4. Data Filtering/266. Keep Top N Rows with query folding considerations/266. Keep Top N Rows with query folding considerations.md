# Keep Top N Rows with Query Folding Considerations

**Description:**
This transformation is used to limit the number of rows in a table to a specified number (N) from the top. It is particularly useful when dealing with large datasets where only a subset of rows is needed for analysis or processing. By considering query folding, this transformation can push the operation back to the source, improving performance by reducing the amount of data that needs to be transferred and processed.

### Typical Scenario
In a real data pipeline, this transformation would be used when fetching data from a large database or data warehouse. For instance, when analyzing sales data, you might only need the top 10 best-selling products out of thousands. By applying 'Keep Top N Rows' early in the pipeline with query folding, you can significantly reduce the volume of data processed in subsequent steps, leading to faster and more efficient data analysis.

### Core M Functions Used
`Table.SelectRows`, `Table.TopRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"Bob", 42, "Mexico"},
        {"Alice", 35, "USA"},
        {"Mike", 28, "Canada"}
    }, {"Name", "Age", "Country"}),
    TopRows = Table.TopRows(Source, 3)
    in
    TopRows
```

### Example Execution Logic Step-By-Step
The transformation 'Keep Top N Rows' works by first establishing a connection to the data source. It then applies a filter or a selection to only include the top N rows as specified by the user. In Power Query (M), this can be achieved using the `Table.TopRows` function, which returns a table with the top N rows from the original table. For example, if you have a table with 100 rows and you want to keep only the top 10 rows based on a specific order (e.g., sorted by a date column), you would use `Table.TopRows` with N=10. This operation can often be folded back to the source, meaning the database or data source itself can perform the filtering, which is more efficient than bringing all 100 rows into Power Query and then filtering.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*