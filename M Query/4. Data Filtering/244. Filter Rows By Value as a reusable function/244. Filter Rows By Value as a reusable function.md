# Filter Rows By Value as a Reusable Function

**Description:**
This transformation allows you to filter rows in a Power Query table based on a specific value. It is useful for data cleansing and data transformation tasks where certain conditions need to be met for data to be included in the output. The function can be reused across different queries and data sources.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when you need to exclude or include specific data records based on certain criteria. For example, you might want to filter out rows where a specific column has a null or missing value, or where it matches a certain condition.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", 25, "USA"},
        {"Mary", 31, "Canada"},
        {"Peter", 35, "USA"}
    }, {"Name", "Age", "Country"}),
    FilteredRows = Table.SelectRows(Source, each [Country] = "USA")
    in
    FilteredRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. You start with a table that has multiple rows and columns.
    2. You define a condition or criteria that you want to use to filter the rows.
    3. You use the `Table.SelectRows` function in Power Query (M) to create a new table that only includes rows where the specified condition is met.
    4. The resulting table is then used for further data transformation or analysis.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
