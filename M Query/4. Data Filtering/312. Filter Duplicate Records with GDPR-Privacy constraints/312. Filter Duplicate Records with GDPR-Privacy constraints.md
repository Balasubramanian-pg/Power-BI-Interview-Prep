# Filter Duplicate Records with GDPR/Privacy constraints

**Description:**
This transformation filters out duplicate records from a dataset while adhering to GDPR/privacy constraints. It ensures that personal data is protected by removing or anonymizing duplicate entries, thereby reducing the risk of data exposure. This process is crucial in data preprocessing for analytics, reporting, and machine learning tasks.

### Typical Scenario
In a real-world data pipeline, this transformation is used when processing customer data for marketing analysis. For instance, a company may have multiple entries for the same customer due to different data sources. By filtering these duplicates, the company can ensure compliance with GDPR and other privacy laws, while also improving the accuracy of their marketing analytics.

### Core M Functions Used
`Table.SelectRows`, `Table.Distinct`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"CustomerID", "Name", "Email"},
        {1, "John Doe", "john.doe@example.com"},
        {2, "Jane Doe", "jane.doe@example.com"},
        {1, "John Doe", "john.doe@example.com"}
    }),
    RemovedDuplicates = Table.Distinct(Source, {"CustomerID"})
    in
    RemovedDuplicates
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Identify Duplicate Records**: Determine the criteria for what constitutes a duplicate record, such as a combination of 'CustomerID', 'Name', and 'Email'.
    2. **Apply Table.Distinct Function**: Use the Table.Distinct function in Power Query (M) to remove duplicate rows based on the specified criteria.
    3. **Select Unique Rows**: The function returns a new table with unique values for the specified columns, effectively filtering out duplicates.
    4. **Integrate into Power Query Pipeline**: This step can be integrated into a larger Power Query pipeline for data transformation and analysis, ensuring that the data processed downstream does not contain duplicates, thereby adhering to GDPR/privacy constraints.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*