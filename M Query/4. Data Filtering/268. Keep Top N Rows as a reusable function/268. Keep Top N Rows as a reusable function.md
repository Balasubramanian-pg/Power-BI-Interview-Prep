# Keep Top N Rows as a reusable function

**Description:**
This transformation allows users to keep only the top N rows of a table based on a specified column. It is useful for data sampling, data filtering, and data aggregation. The transformation can be reused across different tables and data sources.

### Typical Scenario
In a real-world data pipeline, this transformation can be used to limit the number of rows returned in a report or dashboard, such as showing the top 10 products by sales revenue.

### Core M Functions Used
`Table.SelectRows`, `Table.TopN`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, "John", 25},
        {2, "Jane", 30},
        {3, "Bob", 35},
        {4, "Alice", 20}
    }, type table[ID = number, Name = text, Age = number]),
    KeepTopRows = Table.TopN(Source, 2, "Age")
in
    KeepTopRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
    1. The source table is defined with columns 'ID', 'Name', and 'Age'.
    2. The Table.TopN function is used to keep only the top N rows based on the 'Age' column.
    3. The resulting table contains only the top 2 rows with the highest age values.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*