# Keep Top N Rows Optimized for Incremental Refresh

**Description:**
This transformation allows you to retain only the top N rows from a dataset, which is particularly useful when working with large datasets and incremental data refreshes. It helps in optimizing data processing and storage by focusing on the most relevant or recent data points.

### Typical Scenario
In a real-world data pipeline, this transformation can be used when you need to analyze or process only the most recent data, such as the top 100 sales transactions from the last month. For instance, in an e-commerce platform, you might want to keep only the top 10 products by sales for the last quarter to analyze best-sellers.

### Core M Functions Used
`Table.SelectRows`, `Table.TopRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, "John", 25},
        {2, "Alice", 30},
        {3, "Bob", 35}
    }, {"ID", "Name", "Age"}),
    TopRows = Table.TopRows(Source, 2)
    in
    TopRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
    1. **Data Source**: You start with a data source, which could be a table or a query.
    2. **Apply Transformation**: You apply the 'Keep Top N Rows' transformation by specifying the number of rows (N) you want to keep.
    3. **Filtering**: Power Query (M) uses functions like `Table.TopRows` to filter the data and return only the specified number of rows from the top of your dataset.
    4. **Output**: The output will be a new table containing only the top N rows as per your specification.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
