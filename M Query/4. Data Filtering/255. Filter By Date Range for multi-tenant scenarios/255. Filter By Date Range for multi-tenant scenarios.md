# Filter By Date Range for Multi-Tenant Scenarios

**Description:**
This transformation filters data by a specific date range, which is particularly useful in multi-tenant scenarios where data from different tenants needs to be analyzed within specific time frames.

### Typical Scenario
In a multi-tenant data pipeline, a business analyst needs to generate reports for different clients, each requiring data within a specific date range. For instance, Client A may need data from January 2022 to June 2022, while Client B needs data from July 2022 to December 2022.

### Core M Functions Used
`Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Tenant", "Date", "Value"},
        {"Tenant1", #date(2022, 1, 1), 10},
        {"Tenant1", #date(2022, 6, 1), 20},
        {"Tenant2", #date(2022, 7, 1), 30},
        {"Tenant2", #date(2022, 12, 1), 40}
    }),
    FilteredRows = Table.SelectRows(Source, each [Date] >= #date(2022, 1, 1) and [Date] <= #date(2022, 6, 30))
    in
    FilteredRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. The source data is a table with columns for Tenant, Date, and Value.
    2. The Table.SelectRows function is used to filter the rows based on a condition.
    3. The condition specified is that the Date column must be within the range of January 1, 2022, and June 30, 2022.
    4. The resulting table, FilteredRows, contains only the rows where the date falls within the specified range.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*