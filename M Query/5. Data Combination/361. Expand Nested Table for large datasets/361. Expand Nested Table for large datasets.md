# Expand Nested Table for large datasets

**Description:**
This transformation is used to expand nested tables in large datasets, making it easier to work with complex data structures. It allows for more efficient data processing and analysis by flattening nested data.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when dealing with large datasets that contain nested tables, such as data from a database or API. For example, when processing customer data that includes orders with multiple items, expanding the nested table of items allows for easier analysis and reporting.

### Core M Functions Used
`Table.SelectRows`, `Table.ExpandTableColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", {"Item1", "Item2"}}, 
        {"Jane", {"Item3", "Item4"}}
    }, {"Name", "Items"}),
    ExpandedItems = Table.ExpandTableColumn(Source, "Items", {"Items"})
    in
    ExpandedItems
```

### Example Execution Logic Step-By-Step
The transformation works by first creating a table with a nested column. The Table.ExpandTableColumn function is then used to expand the nested column into separate rows. For example, given a table with a column 'Items' that contains lists of items, the transformation would create new rows for each item in the list, effectively flattening the nested data structure.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*