# Expand Nested Table with Error Handling and Retries in Power Query

**Description:**
This transformation expands a nested table within a Power Query pipeline while incorporating error handling and retries. It ensures robustness by managing potential errors during the expansion process, making it reliable for handling complex data structures. The use of retries adds an extra layer of resilience, allowing the process to automatically reattempt failed operations.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when dealing with data sources that contain nested tables. For instance, when integrating data from a web API that returns a JSON object with nested arrays, this transformation can be used to flatten the data into a more usable format. This is particularly useful in data integration and ETL (Extract, Transform, Load) processes where data consistency and reliability are paramount.

### Core M Functions Used
`Table.SelectRows`, `Table.ExpandTableColumn`, `try-catch blocks for error handling`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"ID", "Name", "Address"}, 
        {1, "John Doe", "123 Main St"},
        {2, "Jane Doe", "456 Elm St"}
    }),
    ExpandedTable = Table.ExpandTableColumn(Source, "Address", {"Street", "City"}, {"Street", "City"}),
    RetryCount = 3,
    Attempt = (attempt as number) =>
        try
            ExpandedTable
        catch
            (e as text) =>
                if attempt < RetryCount then
                    Attempt(attempt + 1)
                else
                    error "Failed to expand table after " & RetryCount & " retries."
    in
        Attempt(0)
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Source Table Creation**: A sample table is created with a nested structure.
    2. **Expansion Attempt**: The code attempts to expand the nested table using `Table.ExpandTableColumn`.
    3. **Error Handling**: A try-catch block is used to catch any errors during expansion.
    4. **Retry Mechanism**: If an error occurs, the code retries the expansion up to a predefined number of times (`RetryCount`).
    5. **Successful Expansion**: If the expansion is successful, the transformed table is returned; otherwise, an error message is displayed after all retries have failed.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
