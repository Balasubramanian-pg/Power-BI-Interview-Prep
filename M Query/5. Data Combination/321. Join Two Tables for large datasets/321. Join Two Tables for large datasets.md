# Join Two Tables for Large Datasets

**Description:**
This transformation joins two large datasets based on a common column, enabling combined analysis and reporting. It is commonly used for integrating data from multiple sources. The resulting joined table provides a unified view, facilitating data analysis and insights.

### Typical Scenario
In a real-world data pipeline, this transformation is used when combining customer information from a CRM system with sales data from an ERP system to analyze customer purchasing behavior.

### Core M Functions Used
`Table.Join`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Table1 = Table.FromRows({
        {"ID", "Name"}, 
        {1, "John"}, 
        {2, "Jane"}
    }),
    Table2 = Table.FromRows({
        {"ID", "Sales"}, 
        {1, 100}, 
        {2, 200}
    }),
    JoinedTable = Table.Join(Table1, Table2, "ID")
in
    JoinedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. Two tables, Table1 and Table2, are created with sample data.
2. The Table.Join function is used to join these tables on the 'ID' column.
3. The resulting JoinedTable contains columns from both tables, matched by the 'ID' column.
4. This joined table can then be used for further analysis, such as filtering, grouping, or visualization.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*