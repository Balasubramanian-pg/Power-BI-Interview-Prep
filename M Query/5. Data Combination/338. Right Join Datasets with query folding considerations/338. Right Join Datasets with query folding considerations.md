# Right Join Datasets with Query Folding Considerations in Power Query (M)

**Description:**
This transformation achieves a right join between two datasets while considering query folding for optimization. It ensures that all records from the right table are included, and matching records from the left table are added. This is particularly useful for combining data from different sources while preserving all records from one of the sources.

### Typical Scenario
In a real data pipeline, this transformation would be used when you need to combine customer information from a CRM system with transaction data from a sales database. You want to ensure that all customers are included in the analysis, even if they don't have any transactions recorded.

### Core M Functions Used
`Table.Join`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    LeftTable = Table.FromList({
        {"ID", "Name"},
        {1, "John"},
        {2, "Jane"},
        {3, "Bob"}
    }, null, {"ID", "Name"}),
    RightTable = Table.FromList({
        {"ID", "Transaction"},
        {1, "Buy"},
        {1, "Sell"},
        {2, "Buy"}
    }, null, {"ID", "Transaction"}),
    JoinedTable = Table.Join(LeftTable, RightTable, "ID", JoinKind.RightOuter)
    in
    JoinedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Two tables, LeftTable and RightTable, are created with sample data.
    2. The Table.Join function is used with the JoinKind.RightOuter parameter to perform a right outer join on the two tables based on the 'ID' column.
    3. The result is a new table, JoinedTable, that includes all records from RightTable and matching records from LeftTable. If there are no matches, the result will contain null values for the LeftTable columns.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*