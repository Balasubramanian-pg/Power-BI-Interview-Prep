# Merge Sales With Customers as a reusable function

**Description:**
This transformation merges sales data with customer information to provide a comprehensive view of sales transactions, including customer details. It enables better analysis and reporting by combining relevant data sources. This function can be reused across different reports and dashboards.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a business needs to analyze sales performance in relation to customer demographics, preferences, or loyalty programs. For instance, a sales team might want to evaluate the effectiveness of a marketing campaign by analyzing sales data alongside customer information to identify trends and patterns.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
(// Sample M code for merging sales with customers
let
    // Assuming Sales and Customers are two tables
    Sales = Table.FromRows({
        {"SalesID", "Product", "CustomerID", "Amount"},
        {1, "Product A", 101, 100},
        {2, "Product B", 102, 200}
    }),
    Customers = Table.FromRows({
        {"CustomerID", "Name", "Email"},
        {101, "John Doe", "john.doe@example.com"},
        {102, "Jane Doe", "jane.doe@example.com"}
    }),
    // Perform an inner join on CustomerID
    MergedTable = Table.Join(Sales, Customers, "CustomerID", "CustomerID", JoinKind.Inner)
    in
    MergedTable)
```

### Example Execution Logic Step-By-Step
This transformation works by first loading the sales and customer data into Power Query. It then performs an inner join on the 'CustomerID' column, which is common to both tables. The result is a new table that includes all columns from both the sales and customers tables, effectively merging them based on the customer ID. This merged table can then be used for further analysis, such as filtering, grouping, or visualization.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*