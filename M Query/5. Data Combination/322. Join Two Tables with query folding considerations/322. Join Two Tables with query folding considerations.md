# Join Two Tables with Query Folding Considerations

**Description:**
This transformation joins two tables while considering query folding, which enables efficient data processing by pushing operations back to the source.

### Typical Scenario
In a real data pipeline, this transformation is used when combining data from multiple sources, such as merging customer information with order details, while optimizing performance by leveraging query folding.

### Core M Functions Used
`Table.Join`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Table1 = Table.FromRows({
        {"ID", "Name"},
        {1, "John"},
        {2, "Jane"}
    }),
    Table2 = Table.FromRows({
        {"ID", "Order"},
        {1, "Order1"},
        {2, "Order2"}
    }),
    JoinedTable = Table.Join(Table1, Table2, "ID")
in
    JoinedTable
```

### Example Execution Logic Step-By-Step
The transformation works by first defining two tables, Table1 and Table2. Then, it uses the Table.Join function to join these tables based on the 'ID' column. The query folding consideration allows the join operation to be optimized and pushed back to the source, improving performance.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
