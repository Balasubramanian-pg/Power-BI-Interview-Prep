# Combine Monthly Files for Multi-Tenant Scenarios

**Description:**
This transformation combines monthly files from multiple tenants into a single dataset. It enables the aggregation and analysis of data across different tenants, facilitating insights that can be applied universally or compared across tenants. The process involves merging data while handling potential differences in structure or content.

### Typical Scenario
In a real data pipeline, this transformation would be used when an organization manages data for multiple clients or business units (tenants) and needs to aggregate this data on a monthly basis for reporting or analysis. For instance, a service provider might have separate monthly financial files for each client, and they need to combine these files into a single dataset to analyze total spending trends across all clients.

### Core M Functions Used
`Table.Combine`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"Tenant1", "2022-01", 100},
        {"Tenant1", "2022-02", 120},
        {"Tenant2", "2022-01", 150},
        {"Tenant2", "2022-02", 180}
    }, {"Tenant", "Month", "Value"}),
    GroupedRows = Table.Group(Source, {"Tenant", "Month"}, {{"Total Value", each List.Sum([Value]), type nullable number}})
    in
    GroupedRows
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Data Source Definition**: Define a table with sample data representing monthly values for different tenants.
2. **Grouping**: Use `Table.Group` to group the data by 'Tenant' and 'Month', then calculate the total value for each group using `List.Sum`.
3. **Result**: The output is a table that shows the total value for each tenant per month, combining data from what were initially separate files.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
