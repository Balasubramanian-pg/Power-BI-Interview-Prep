# Full Outer Join with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used for Full Outer Join with GDPR/Privacy constraints, ensuring sensitive data is handled appropriately.

### Typical Scenario
In a real-world data pipeline, this transformation is used when combining data from two sources while adhering to GDPR/privacy constraints, such as when merging customer data from different systems while ensuring sensitive information is properly anonymized or protected.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    Table1 = Table.FromRows({
        {"CustomerID", "Name", "Email"},
        {1, "John Doe", "john.doe@example.com"},
        {2, "Jane Doe", "jane.doe@example.com"}
    }),
    Table2 = Table.FromRows({
        {"CustomerID", "OrderID", "OrderDate"},
        {1, 101, "2022-01-01"},
        {2, 102, "2022-01-15"},
        {3, 103, "2022-02-01"}
    }),
    FullOuterJoin = Table.Join(Table1, Table2, "CustomerID", "CustomerID", JoinKind.FullOuter)
    in
    FullOuterJoin
```

### Example Execution Logic Step-By-Step
The transformation works as follows: First, two tables (Table1 and Table2) are created with sample data. Then, a Full Outer Join is performed on these tables based on the 'CustomerID' column. The result is a new table that includes all rows from both tables, with null values in the columns where there are no matches.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*