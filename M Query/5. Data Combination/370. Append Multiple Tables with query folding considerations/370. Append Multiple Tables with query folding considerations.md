# Append Multiple Tables with Query Folding Considerations

**Description:**
This transformation allows you to combine multiple tables into one, taking into account query folding for optimized performance. It is particularly useful when dealing with large datasets and complex queries. By leveraging query folding, the transformation minimizes the amount of data that needs to be processed, resulting in improved efficiency.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when combining sales data from multiple regions. For instance, a company with separate tables for North, South, East, and West region sales data could use this transformation to append these tables into a single, unified sales data table for analysis and reporting.

### Core M Functions Used
`Table.Combine`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Define the tables
    Table1 = Table.FromRows({
        {"ID", "Name"}, 
        {1, "John"},
        {2, "Jane"}
    }),
    Table2 = Table.FromRows({
        {"ID", "Name"}, 
        {3, "Alice"},
        {4, "Bob"}
    }),
    // Append the tables with query folding considerations
    CombinedTable = Table.Combine({Table1, Table2})
    in
    CombinedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. Define the individual tables (e.g., Table1 and Table2) that need to be appended.
2. Use the Table.Combine function to combine these tables into a single table.
3. The Table.Combine function takes a list of tables as input and returns a new table that contains all the rows from the input tables.
4. By using query folding, the transformation optimizes performance by minimizing the amount of data that needs to be processed.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*