# Merge Fact And Dimension as a reusable function

**Description:**
This transformation merges fact and dimension tables as a reusable function in Power Query (M). It allows for efficient and flexible data modeling by combining related data from different sources. This function can be used to create a unified view of data for analysis and reporting.

### Typical Scenario
In a real data pipeline, this transformation would be used when there is a need to combine fact tables (containing measurable data) with dimension tables (containing descriptive data) to create a comprehensive data model. For example, in a sales analysis pipeline, a fact table containing sales data would be merged with a dimension table containing product information to provide a detailed view of sales by product.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    FactTable = Table.FromRows({
        {1, 100},
        {2, 200},
        {3, 300}
    }, {"ProductID", "Sales"}),
    DimensionTable = Table.FromRows({
        {1, "Product A"},
        {2, "Product B"},
        {3, "Product C"}
    }, {"ProductID", "ProductName"}),
    MergedTable = Table.Join(FactTable, DimensionTable, "ProductID", "ProductID", JoinKind.Inner)
    in
    MergedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Define the fact table and dimension table as input tables.
    2. Use the Table.Join function to merge the two tables based on a common key (ProductID).
    3. Specify the join kind (e.g., Inner, Left, Right, Full) to determine how to handle matching rows.
    4. The resulting merged table contains all columns from both the fact and dimension tables, enabling analysis and reporting on the combined data.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*