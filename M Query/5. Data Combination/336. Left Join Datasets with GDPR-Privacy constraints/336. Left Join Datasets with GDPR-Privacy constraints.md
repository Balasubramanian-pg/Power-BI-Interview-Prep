# Left Join Datasets with GDPR/Privacy constraints

**Description:**
This use case demonstrates how Power Query (M) can be used to perform a left join on datasets while adhering to GDPR/privacy constraints. It ensures that sensitive data is handled appropriately during the data transformation process.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when combining customer data from different sources while ensuring compliance with GDPR/privacy regulations. For instance, a company might need to join customer information from a CRM system with order data from an e-commerce platform, all while masking or anonymizing sensitive personal data.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    // Sample tables
    Table1 = Table.FromList({
        {"John", "john.doe@example.com"},
        {"Jane", "jane.doe@example.com"}
    }, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    Table2 = Table.FromList({
        {"John", "Order1"},
        {"Jane", "Order2"},
        {"Bob", "Order3"}
    }, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    // Left join with GDPR/privacy constraints
    JoinedTable = Table.Join(Table1, Table2, "Name", "Name", JoinKind.Left)
    in
    JoinedTable
```

### Example Execution Logic Step-By-Step
This transformation typically works as follows inside a Power Query pipeline:
    1. **Data Import**: Import the necessary datasets into Power Query.
    2. **Data Preparation**: Prepare the data by cleaning and transforming it as needed.
    3. **Applying GDPR/Privacy Constraints**: Apply functions like `Table.SelectRows` to filter or transform sensitive data according to GDPR/privacy requirements.
    4. **Left Join**: Perform a left join using the `Table.Join` function, specifying the join key and the type of join.
    5. **Data Anonymization/Masking**: If necessary, anonymize or mask sensitive data in the joined table to ensure compliance with regulations.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
