# Full Outer Join for Multi-Tenant Scenarios

**Description:**
This transformation achieves a full outer join for multi-tenant scenarios, combining data from multiple sources while preserving all records from each source.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when integrating customer data from multiple tenants, ensuring that all customer records are included, even if they don't have matching records in other tenants.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    Table1 = Table.FromRows({
        {"Tenant1", "Customer1"},
        {"Tenant1", "Customer2"},
        {"Tenant2", "Customer3"}
    }, {"Tenant", "Customer"}),
    Table2 = Table.FromRows({
        {"Tenant1", "Order1"},
        {"Tenant2", "Order2"},
        {"Tenant3", "Order3"}
    }, {"Tenant", "Order"}),
    FullOuterJoin = Table.Join(Table1, Table2, "Tenant", "Tenant", JoinKind.FullOuter)
    in
    FullOuterJoin
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. Two tables, Table1 and Table2, are created with sample data.
    2. The Table.Join function is used to perform a full outer join on the 'Tenant' column.
    3. The JoinKind.FullOuter parameter specifies that all records from both tables should be included, with nulls in the columns where there are no matches.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*