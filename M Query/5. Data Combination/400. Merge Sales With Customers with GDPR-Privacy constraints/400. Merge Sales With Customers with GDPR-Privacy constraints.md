# Merge Sales With Customers with GDPR/Privacy constraints

**Description:**
This transformation merges sales data with customer information while adhering to GDPR/privacy constraints. It ensures that sensitive customer data is protected and only authorized personnel have access to it. This is achieved through Power Query (M) functions that enable data masking and selective data exposure.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when a company needs to analyze sales data in conjunction with customer demographics, but is restricted by privacy laws such as GDPR. For instance, a marketing team might need to understand sales trends among different age groups or locations without exposing individual identities.

### Core M Functions Used
`Table.SelectRows`, `Table.Merge`

### Code Source Execution
```powerquery
let
    // Sample sales data
    salesData = Table.FromList({
        {1, "Product A", 100},
        {2, "Product B", 200},
        {3, "Product C", 300}
    }, null, {"SalesID", "Product", "Amount"}),
    // Sample customer data with GDPR constraints
    customerData = Table.FromList({
        {1, "John Doe", "john.doe@example.com"},
        {2, "Jane Doe", "jane.doe@example.com"},
        {3, "Sam Smith", "sam.smith@example.com"}
    }, null, {"CustomerID", "Name", "Email"}),
    // Apply GDPR/privacy constraints by selectively exposing customer data
    constrainedCustomerData = Table.SelectRows(customerData, each CustomerID = 1 or CustomerID = 3),
    // Merge sales data with constrained customer data
    mergedData = Table.Merge(salesData, constrainedCustomerData, {"SalesID", "CustomerID"}, "Left.Outer", "NewColumn")
    in
    mergedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Import Data**: Load both sales and customer data into Power Query.
2. **Apply GDPR Constraints**: Use `Table.SelectRows` to filter customer data, ensuring only authorized records are processed further.
3. **Merge Data**: Utilize `Table.Merge` to combine sales data with the filtered customer data based on matching IDs.
4. **Analyze Data**: The merged data can then be analyzed, respecting the privacy constraints imposed by GDPR.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*