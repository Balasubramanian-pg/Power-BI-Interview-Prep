# Merge Sales With Customers with Error Handling and Retries

**Description:**
This transformation merges sales data with customer data, handling potential errors and implementing retries for robust data integration.

### Typical Scenario
In a real-world data pipeline, this transformation is crucial when integrating sales and customer data from different sources, ensuring data consistency and reliability.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`, `try`, `catch`, `retry`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"SalesID", "CustomerID", "SalesAmount"},
        {1, 101, 100.00},
        {2, 102, 200.00},
        {3, 103, 300.00}
    }),
    Customers = Table.FromRows({
        {"CustomerID", "CustomerName"},
        {101, "John Doe"},
        {102, "Jane Smith"},
        {103, "Bob Johnson"}
    }),
    Merged = try
        Table.Join(Source, Customers, "CustomerID", "CustomerID", JoinKind.LeftOuter)
    catch
        (e) =>
            if Error.Message(e) = "Timeout" then
                retry(Merged, 3, 5000)
            else
                Error.Record(e),
    RetryPolicy = [MaxRetries = 3, Delay = 5000]
    in
        Merged
```

### Example Execution Logic Step-By-Step
This script works as follows:
1. **Source and Customers Tables Creation**: Two tables, `Source` (sales data) and `Customers` (customer data), are created from sample data.
2. **Merging Tables with Error Handling**: The `Table.Join` function is used within a `try` block to merge these tables on `CustomerID`. If an error occurs (e.g., a timeout), the `catch` block catches the error.
3. **Retry Policy**: If the error message indicates a timeout, the script retries the merge operation up to 3 times with a 5-second delay between retries.
4. **Error Handling**: If all retries fail or a non-timeout error occurs, the error is recorded and returned.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*