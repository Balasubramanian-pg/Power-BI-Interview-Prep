# Expand Nested Table as a Reusable Function in Power Query

**Description:**
This transformation allows users to expand nested tables within Power Query, making it easier to flatten complex data structures. It is particularly useful when dealing with data that contains nested or hierarchical structures, enabling further analysis and manipulation.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when ingesting data from a source that provides nested or hierarchical information, such as JSON or XML files. For instance, when retrieving customer data that includes addresses, orders, or other related nested information, this transformation helps in flattening the data for easier reporting and analysis.

### Core M Functions Used
`Table.SelectRows`, `Table.ExpandTableColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", "Doe", Table.FromRows({
            {"Street", "City", "State", "Zip"},
            {"123 Main St", "Anytown", "CA", "12345"}
        })},
        {"Jane", "Doe", Table.FromRows({
            {"Street", "City", "State", "Zip"},
            {"456 Elm St", "Othertown", "NY", "67890"}
        })}
    }, {"First Name", "Last Name", "Address"}),
    ExpandedAddress = Table.ExpandTableColumn(Source, "Address", {"Street", "City", "State", "Zip"})
    in
    ExpandedAddress
```

### Example Execution Logic Step-By-Step
The transformation works by first creating a table with a nested table column. Then, it uses the Table.ExpandTableColumn function to expand the nested table into separate columns. This process involves:
    1. Creating a sample table with nested structures.
    2. Identifying the column that contains the nested table.
    3. Applying Table.ExpandTableColumn to expand the nested table, specifying the columns to expand.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*