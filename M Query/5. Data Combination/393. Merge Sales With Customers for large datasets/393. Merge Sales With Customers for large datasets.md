# Merge Sales With Customers for Large Datasets

**Description:**
This transformation merges sales data with customer information for large datasets, enabling comprehensive analysis and insights. It combines relevant data from two sources, enhancing data quality and usability.

### Typical Scenario
In a real-world data pipeline, this transformation is used when analyzing sales performance. For instance, a company wants to evaluate sales trends and customer demographics together. By merging sales data with customer information, businesses can gain a deeper understanding of their customers' buying behaviors and preferences.

### Core M Functions Used
`Table.Join`, `Table.SelectColumns`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    // Load sales data
    salesData = Table.FromRows({
        {"SalesID", "Product", "Amount"},
        {1, "Product A", 100},
        {2, "Product B", 200},
        {3, "Product C", 300}
    }),
    // Load customer data
    customerData = Table.FromRows({
        {"CustomerID", "Name", "Email"},
        {101, "John Doe", "john.doe@example.com"},
        {102, "Jane Doe", "jane.doe@example.com"}
    }),
    // Merge sales with customers on a common column (e.g., CustomerID)
    mergedData = Table.Join(salesData, customerData, "CustomerID")
in
    mergedData
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Load Data**: Load sales and customer data into Power Query.
2. **Prepare Tables**: Ensure both tables are properly formatted.
3. **Merge Tables**: Use the `Table.Join` function to merge the tables based on a common column.
4. **Select Relevant Columns**: Use `Table.SelectColumns` to choose specific columns.
5. **Filter Rows**: Apply filters with `Table.SelectRows` if needed.
6. **Analyze Merged Data**: The merged data can then be analyzed for insights.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*