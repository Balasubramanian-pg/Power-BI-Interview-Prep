# Full Outer Join Optimized for Incremental Refresh

**Description:**
This transformation achieves a full outer join between two tables, optimized for incremental refresh. It allows for efficient updates by only refreshing new or changed data. This approach minimizes data processing and refresh times, making it suitable for large datasets. By leveraging Power Query's M language, users can create robust and scalable data pipelines.

### Typical Scenario
In a real-world data pipeline, this transformation is useful when integrating data from multiple sources, such as customer information and sales data. For instance, a company may have two tables: one containing customer details and the other containing sales transactions. A full outer join optimized for incremental refresh can be used to combine these tables, ensuring that all customers and sales transactions are included, even if there are missing records in either table. This is particularly useful in scenarios where data is constantly being updated, such as in e-commerce or finance.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    // Define the tables
    Table1 = Table.FromRows({
        {"CustomerID", "Name"}, 
        {1, "John"}, 
        {2, "Jane"}
    }),
    Table2 = Table.FromRows({
        {"CustomerID", "Sales"}, 
        {1, 100}, 
        {3, 200}
    }),
    // Perform full outer join
    JoinedTable = Table.Join(Table1, Table2, "CustomerID", "CustomerID", JoinKind.FullOuter)
    in
    JoinedTable
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
    1. **Define the Tables**: The M code starts by defining two tables, `Table1` and `Table2`, which contain customer information and sales data, respectively.
    2. **Perform Full Outer Join**: The `Table.Join` function is used to perform a full outer join on `Table1` and `Table2` based on the `CustomerID` column. This join includes all records from both tables, with null values in the columns where there are no matches.
    3. **Incremental Refresh**: To optimize for incremental refresh, the M code can be modified to use parameters and filters that only refresh new or changed data, reducing processing time and load on the data sources.
    4. **Result**: The resulting `JoinedTable` contains all customer information and sales data, with null values where there are no matches in either table.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*