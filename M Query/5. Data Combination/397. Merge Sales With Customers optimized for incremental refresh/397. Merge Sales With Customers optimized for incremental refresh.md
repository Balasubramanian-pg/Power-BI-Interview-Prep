# Merge Sales With Customers optimized for incremental refresh

**Description:**
This use case demonstrates how Power Query (M) can be used to merge sales data with customer data, optimized for incremental refresh. It allows for efficient updates to sales data by only refreshing new or changed customer information.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when there is a need to regularly update sales data with the latest customer information, but only the changes need to be processed, not the entire dataset.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`, `Table.SelectColumns`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {1, "John", "Doe"},
        {2, "Jane", "Doe"}
    }, {"CustomerID", "FirstName", "LastName"}, "Customers"),
    Sales = Table.FromRows({
        {1, 100},
        {1, 200},
        {2, 50}
    }, {"CustomerID", "SalesAmount"}, "Sales"),
    Merged = Table.Join(Sales, Source, "CustomerID", "CustomerID", JoinKind.LeftOuter)
    in
    Merged
```

### Example Execution Logic Step-By-Step
The transformation works as follows: 
1. Load customer and sales data into Power Query. 
2. Use Table.SelectRows to filter or select specific rows if needed. 
3. Merge the sales and customer tables on a common key (CustomerID) using Table.Join. 
4. Select and transform the required columns. 
5. For incremental refresh, use parameters and query folding to optimize performance.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
