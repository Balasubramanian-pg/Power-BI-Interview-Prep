# Expand Nested Table with Query Folding Considerations

**Description:**
This transformation expands a nested table while considering query folding, which is crucial for optimizing data retrieval and processing. It allows for more efficient data handling by minimizing the amount of data that needs to be transferred and processed. This is particularly useful when dealing with large datasets and complex data transformations.

### Typical Scenario
In a real-world data pipeline, this transformation is typically used when ingesting data from a database or API that returns nested or hierarchical data structures. For instance, when retrieving customer data that includes a list of orders, each with multiple order items, expanding the nested table allows for easier analysis and reporting of customer orders.

### Core M Functions Used
`Table.SelectRows`, `Table.ExpandListColumn`, `Table.ExpandRecordColumn`

### Code Source Execution
```powerquery
let
    Source = Table.FromRows({
        {"John", {"Order1", "Order2"}, {"Order1", {"Item1", "Item2"}}, {"Order2", {"Item3"}}}),
        {"Jane", {"Order3"}, {"Order3", {"Item4"}}},
        {"Bob", {}, {}, {}}},
        {"Name", "Orders", "Order Details"}),
    ExpandedOrders = Table.ExpandListColumn(Source, "Orders", {"Orders"}, {"Orders"}),
    ExpandedOrderDetails = Table.ExpandRecordColumn(ExpandedOrders, "Order Details", {"Order Details"}, {"Order Details.Orders", "Order Details.Items"})
    in
    ExpandedOrderDetails
```

### Example Execution Logic Step-By-Step
The transformation works as follows:
1. **Source Table Creation**: A sample table with nested structures is created.
2. **Expansion of List Column**: The `Table.ExpandListColumn` function is used to expand the list of orders for each customer into separate rows.
3. **Expansion of Record Column**: The `Table.ExpandRecordColumn` function is then used to further expand the order details, creating separate columns for order details and items.
4. **Result**: The final table has a flattened structure with all the nested information expanded into separate columns, making it easier to analyze.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
