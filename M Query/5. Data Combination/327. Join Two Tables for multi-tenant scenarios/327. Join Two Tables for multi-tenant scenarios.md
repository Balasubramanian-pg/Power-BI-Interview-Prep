# Join Two Tables for Multi-Tenant Scenarios

**Description:**
This transformation achieves joining two tables in a multi-tenant environment, enabling data consolidation and analysis across different tenants. It allows for efficient data processing and retrieval by combining data from multiple sources. This is particularly useful in scenarios where data is scattered across different tables or databases.

### Typical Scenario
In a real-world data pipeline, this transformation would be used when integrating data from multiple tenants, such as in a Software as a Service (SaaS) application. For instance, a company providing a multi-tenant CRM system might need to join customer data from different tenants to generate reports or perform data analysis.

### Core M Functions Used
`Table.SelectRows`, `Table.Join`

### Code Source Execution
```powerquery
let
    // Sample tables
    Table1 = Table.FromList({
        {"Tenant1", "Data1"},
        {"Tenant2", "Data2"},
        {"Tenant3", "Data3"}
    }, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    Table2 = Table.FromList({
        {"Tenant1", "AdditionalData1"},
        {"Tenant2", "AdditionalData2"},
        {"Tenant3", "AdditionalData3"}
    }, Splitter.SplitByNothing(), null, null, ExtraValues.Error),
    // Join the tables
    JoinedTable = Table.Join(Table1, Table2, "Tenant", JoinKind.Inner)
    in
    JoinedTable
```

### Example Execution Logic Step-By-Step
This transformation typically works as follows:
1. **Data Source Definition**: Define the two tables that need to be joined, e.g., Table1 and Table2.
2. **Specify Join Key**: Identify a common key (e.g., "Tenant") to join the tables.
3. **Apply Join Operation**: Use the Table.Join function to join the tables based on the specified key.
4. **Result**: The resulting joined table contains combined data from both tables, enabling further analysis or processing.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*