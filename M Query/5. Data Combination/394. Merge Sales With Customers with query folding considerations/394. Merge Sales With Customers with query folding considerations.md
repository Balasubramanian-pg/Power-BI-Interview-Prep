# Merge Sales With Customers with Query Folding Considerations

**Description:**
This transformation merges sales data with customer information while optimizing query performance through query folding. It enables efficient data retrieval by leveraging the capabilities of the source database. This approach is particularly useful when dealing with large datasets.

### Typical Scenario
In a real-world data pipeline, this transformation is used when combining sales transactions with customer details for analysis. For instance, a sales team needs to analyze transaction data alongside customer information to gain insights into customer purchasing behavior.

### Core M Functions Used
`Table.Join`, `Table.SelectRows`

### Code Source Execution
```powerquery
let
    Source = Sql.Database("server", "database"),
    Customers = Source{[Schema = "dbo", Name = "Customers"]}[Data],
    Sales = Source{[Schema = "dbo", Name = "Sales"]}[Data],
    Merged = Table.Join(Customers, Sales, {"CustomerID"}, {"CustomerID"}, JoinKind.LeftOuter)
    in
    Merged
```

### Example Execution Logic Step-By-Step
The transformation works as follows: First, it connects to a database source using Sql.Database. It then extracts the Customers and Sales tables. The Table.Join function is used to merge these tables based on the CustomerID column. The JoinKind.LeftOuter parameter ensures that all records from the Customers table are included, with matching records from the Sales table if available.

*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*
