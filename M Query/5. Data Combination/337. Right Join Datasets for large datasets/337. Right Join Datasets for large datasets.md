# Right Join Datasets for Large Datasets

**Description:**
This transformation achieves a right join between two large datasets in Power Query (M). It combines rows from both tables where the join condition matches, keeping all records from the right table and matching records from the left table.

### Typical Scenario
In a real data pipeline, this transformation is used when you need to combine data from two sources, ensuring that all records from the larger or dominant dataset are included, and matching records from the other dataset are added.

### Core M Functions Used
`Table.Join`

### Code Source Execution
```powerquery
let
    Table1 = Table.FromRows({
        {"A1", 1},
        {"A2", 2},
        {"A3", 3}
    }, {"Key", "Value"}),
    Table2 = Table.FromRows({
        {"A1", "Desc1"},
        {"A2", "Desc2"}
    }, {"Key", "Description"}),
    RightJoin = Table.Join(Table1, Table2, "RightOuter"
    )
    in
    RightJoin
```

### Example Execution Logic Step-By-Step
The transformation works by using the Table.Join function in Power Query (M). First, two tables are created, Table1 and Table2. Then, the Table.Join function is used with the 'RightOuter' parameter to perform a right outer join. This keeps all records from Table2 and matching records from Table1. The result is a new table that combines data from both sources based on the join condition.

---
*Auto-enriched via MQ Studio | meta-llama/llama-4-scout-17b*