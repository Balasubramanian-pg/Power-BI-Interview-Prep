# Nested JSON Event Schema

## Question
Given a dataset with nested JSON events, design a tabular schema that preserves queryability of nested arrays, retains lineage for upstream debugging, and minimizes repeated expansion costs.

## Answer
*Add your expert architecture/DAX/modeling solution here.*