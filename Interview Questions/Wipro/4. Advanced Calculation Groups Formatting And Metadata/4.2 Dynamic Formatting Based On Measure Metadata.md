# Dynamic Formatting Based On Measure Metadata

## Question
Implement a system where a single formatting calculation item changes numeric format (currency, percentage, integer) based on measure metadata stored in a connector table. Show how FORMATSTRING or dynamic format via calculation group can support this.

*Deliverable:* Calculation Group pseudo-DAX + formatting mapping table.

## Answer
*Add your expert DAX engineering solution here.*
