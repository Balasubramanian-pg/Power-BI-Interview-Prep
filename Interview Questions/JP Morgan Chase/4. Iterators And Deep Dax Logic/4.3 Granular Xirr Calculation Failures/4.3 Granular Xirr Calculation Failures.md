# Granular Xirr Calculation Failures

## Question
You are calculating the Internal Rate of Return (`XIRR`) for thousands of investment portfolios. `XIRR` fails or returns an error if a portfolio has zero cash flow on the exact dates evaluated, or if there are multiple cash flows on the same date. How do you construct a virtual table inside your measure to pre-aggregate cash flows safely before passing them to the native `XIRR` function?

## Answer
*Add your expert DAX/Modeling solution here.*