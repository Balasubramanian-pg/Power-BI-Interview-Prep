# Triangular Currency Conversion

## Question
We have a global transaction table in 20 local currencies, but our daily FX rate table only maps local currencies to USD. How would you write a DAX measure to allow a user to dynamically select *any* base currency (e.g., EUR) and view all global transactions converted to EUR on the fly, without physically materializing a cross-rate table?

## Answer
*Add your expert DAX/Modeling solution here.*