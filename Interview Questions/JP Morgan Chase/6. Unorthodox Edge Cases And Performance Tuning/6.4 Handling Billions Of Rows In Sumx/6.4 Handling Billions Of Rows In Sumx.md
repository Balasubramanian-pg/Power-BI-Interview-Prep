# Handling Billions Of Rows In Sumx

## Question
Your Daily Average Balance measure works logically but times out because `SUMX(Dates, [Running Total])` is iterating over a 100-million row fact table via context transition. How do you optimize this iteration using `SUMMARIZE` or `ADDCOLUMNS` to evaluate only over the specific grain needed, reducing iterator memory?

## Answer
*Add your expert DAX/Modeling solution here.*